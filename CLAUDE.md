# Agent Studio

- Connections can select one existing Claude/Codex account as a shared context source for other accounts of the same provider on the same execution environment. Separate (isolated) profiles default to this computer's CLI context, the environment's default CLI directory, resolved on the host without any connection record and stored implicitly; `sharedContext: 'none'` opts out and an explicit account source overrides it, while detected terminal logins never default to sharing and present the computer context as their own; choosing This account only on a terminal login converts that connection into a separate profile (profile isolated, sharedContext none, fromTerminalLogin marker, sign-in opened, native history transferred from the terminal directory, terminal untouched), and the marker stops detection from re-registering that login when the CLI reports no identity. Keep references explicit, reject cycles/chains and unavailable sources on the host, and preserve login/usage access when sharing is invalid. Share source instruction/memory entrypoints, native local skill discovery, and MCP server definitions (Claude user/local-scope servers via --mcp-config, Codex mcp_servers tables via -c overrides; definitions only, bounded, launch-time only, never persisted/relayed/serialized into identities) without copying credentials, MCP sign-in state or other settings. Apply changes to the next reply, invalidate inventories and affected parked processes, and retain native conversation history. Restricted background work never inherits sharing. See docs/SHARED-CONTEXT.md.

- Claude Fast mode and fallback models are optional per-conversation settings in Chat instructions. `/fast` opens those controls; `/fast on`, `/fast off`, and `/fast default` edit only the next-reply preference, never send a model message or restore History. Keep draft/attachments and captured reply settings intact. Fast mode uses a process-local `--settings` boolean with a usage-credit notice; omitted values retain the selected CLI profile's default. Pass up to three validated fallback aliases/IDs as one literal `--fallback-model` argument. Changes invalidate only this conversation's parked process while resuming its native session. Preserve both settings through save/export/relay/forks, reject them on other providers, and never inherit them in restricted background requests. Requested Fast mode does not prove provider eligibility or actual fast service. See docs/NATIVE-SESSIONS.md.

- Structured output is an optional per-conversation JSON Schema in Chat instructions for Claude/Codex. Validate bounded object schemas before launch, pass Claude --json-schema as a literal argument and Codex outputSchema on each turn/start, and consume only successful parent results. Preserve the schema with reply settings and exact JSON through history/export/relay/forks; render it as inert code. Schema changes invalidate Claude process reuse while resuming its native session; disabling omits the schema, compaction bypasses it, and restricted background work never inherits it. Missing, malformed and oversized structured results remain explicit failures. See docs/STRUCTURED-OUTPUT.md.

- Composer file mentions use the shared ChoicePicker at @ for Claude/Codex and app mentions at $ for Codex. Query file names on the selected execution host and folder; Codex uses fuzzyFileSearch and accessible enabled apps from app/list, while Claude inserts literal @path text. Selection only edits the draft. Keep native Codex mention identity with the current user message through save/export/relay, queues and retries; never replay older native mention inputs. Invalidate selected references on account/folder changes, retain draft metadata across Settings and queue restoration, discard stale searches, and keep catalogs transient. Discovery never reads file bodies, invokes app tools, starts a model turn or changes app configuration. See docs/CAPABILITIES.md.

- Sub-agent details stay inside Work history. Claude chats enable --forward-subagent-text and retain bounded child text by SDK UUID and parent_tool_use_id, excluding thinking and tool bodies. Codex subAgentActivity establishes child ownership; child message deltas reconcile with completed items and preserve native turn identity. Keep lifecycle duplicates from reviving finished children, retain nested attribution, and leave parent answer, reasoning, usage, plans and prompts separate. SendMessage exposes only recipient, summary and allowlisted type; ListAgents exposes bounded structured directory metadata without treating listed sessions as children. Persist optional messages through history/export/relay, with explicit truncation and incomplete-message notices. See docs/CAPABILITIES.md.

- Tool activity exposes bounded elapsed time and fixed progress/output/terminal-interaction indicators while a call runs, never raw MCP messages, output deltas, terminal input or process IDs; a finished call's result follows the tool results rule below. Advance quiet-tool timing on the execution host, retain reported completion duration, coalesce same-second progress, and bind signals to existing running operations with exact Codex thread/turn/item ownership and Claude parent attribution. Never revive completed calls or add parallel durations. Preserve optional timing/progress through history/export/relay, outside prompts; unfinished tools freeze with stopped/unconfirmed outcomes. The connection-scoped command/exec terminal API has no chat-owned sessions or controls; ignore its unowned output. See docs/CAPABILITIES.md.

- Live account usage captures Claude `rate_limit_event` and Codex `account/rateLimits/updated`/`account/updated` before parked-process output is dropped. Keep updates bounded, transient, revisioned and pinned to the selected connection/host; merge sparse fields without refreshing untouched windows, clear readings on account changes, reject stale polling results, and retain read-only fallback polling. Relay heartbeats carry only host-validated account metadata, never chat activity or persisted workspace data. Codex workspace notices are bounded inert text read through transport. Earned reset credits require a named-account confirmation, native core-window eligibility checks and one stable idempotency key for uncertain retries; refresh limits after confirmed redemption and never consume credits during inspection or automated real-provider QA. See docs/USAGE.md.

- Plan/Build mode lives beside the composer for Claude/Codex and applies to the next message, preserving draft and execution selection. Claude Plan uses native plan permissions; EnterPlanMode/ExitPlanMode require explicit run-owned parent approval through the question channel, with complete bounded plan text and no path reads or caller permission grants. Keep ordinary Build execution full-access, deny unapproved planning writes, close pending decisions on stop/loss, and include actual native mode in parked-process identity. Codex collaboration modes produce separate revisioned proposed-plan documents from plan items/item/plan/delta; completed item text is authoritative, parent-turn scoped, and independent of TODO progress. Preserve bounded plans/decisions through save/export/fork/relay, never infer implementation approval from a proposal or historical receipt. See docs/PLAN-MODE.md.

- Model context's Plugins tab manages installed Claude/Codex plugins through the selected host and CLI profile, with explicit known-plugin installation, details, enable/disable and confirmed removal. Marketplace browsing/administration and sharing remain outside the app. Codex skill toggles must validate the path against a fresh selected-folder catalog before skills/config/write. Temporary Claude plugin directories/zip URLs and Codex extra skill roots belong to one conversation/account on its execution host, remain session-only, and participate in parked-process reuse identity; configuration mutations invalidate source/composer caches and resume on the next reply without interrupting other running chats. Treat skills/changed as a bounded transient invalidation signal, never conversation content. Keep management arguments structured and return bounded metadata only. Claude evaluation is a separate explicit trusted action with a USD budget, local reports, no publishing/scaffolding/real-server grants, owned cancellation and bounded execution. Read-only inspection never installs, toggles, evaluates or starts a model reply. See docs/CONTEXT.md.

- MCP elicitation is a separate run-owned interaction for Claude `elicitation` control requests and Codex `mcpServer/elicitation/request`. Show the requesting server, bounded flat forms or an explicit URL-opening action, and explicit accept/decline/cancel controls. Preserve primitive types, enum values, optional fields, defaults and supported constraints; unsupported schemas fail explicitly. Never auto-submit a default or open a URL on receipt. Validate responses on the native host against the original request and exact run/connection; reject stale, foreign, duplicate and withdrawn requests, and close pending input on stop/process loss. Keep messages, schemas, URLs and form content transient; persist only bounded revisioned receipts, never replay them into prompts. Viewer reads and responses route to the owning host through authenticated jobs. URL acceptance does not prove external completion. Keep full-access chat execution unchanged. See docs/CAPABILITIES.md.

- Model context's MCPs tab offers explicit selected-profile management through transport. Inspection alone never starts OAuth or a model reply. Add/logout use the selected CLI and preserve other profiles; reject duplicate names and keep commands as structured arguments. Claude live reconnect/toggle/set-servers require the exact idle parked chat and native session lock, retaining agent_studio SDK tools. Codex reload acknowledges queued refresh, not connection success. Keep OAuth processes alive only while a bounded, selection-scoped operation waits for provider confirmation; match Codex completion/startup events by server and host-owned thread, and never infer authentication from a returned URL. Claude supports transient callback submission with CLI state validation and a Windows-local CLI login fallback. Cancel/expiry/close releases only owned processes. Never persist auth URLs, codes, tokens, management input, or raw diagnostics in workspace/history/context caches. See docs/CONTEXT.md.

- Windows startup verifies the physical app-data destination before constructing Tauri or reading saved state. Never bypass this guard, rewrite an installation identity, or remap accounts to accommodate MSIX redirection inherited from packaged launchers. From Codex, start the development frontend separately and use `npm run start:windows` for an already-built executable; use `-Executable` for an isolated QA build and `-CheckOnly` for a startup probe. Keep existing instances and both data copies intact. See docs/WINDOWS-STARTUP.md.

- Desktop updates use the Tauri updater with GitHub Releases. Only release installations (NSIS/MSI, AppImage, macOS app) check, 30 seconds after startup and every six hours, and download in the background; development, unbundled, deb and rpm builds report updates unavailable. Verify the updater signature and its signed version (`requireSignedVersion`) before offering Restart to update in the sidebar footer and Settings. Installing waits for replies running on this computer, saves the workspace first, and releases owned CLI work like the close button before the installer relaunches the app; an idle close installs without relaunching, and closing during a reply skips installation. Keep update status transient, desktop-only and routed through transport; the web viewer has no update controls. Merges to main publish `v<version>` with installers, signatures and latest.json only after verification, every platform build, and release-script signature/version checks pass; never replace a published release. Before each release merge to `main`, bump every manifest on `development` with `npm run release:version` (patch, minor or major as the changes warrant); a merge without a new version publishes nothing. Keep the signing key out of the repository, and use `--no-sign` for local installers. Tauri's NSIS installer closes every running process with the same executable name for the user, so native update QA must use `scripts/updates-native-smoke.mjs` with its separate identifier, product name, mainBinaryName and throwaway key. See docs/UPDATES.md.

- Settings ends with About on desktop and in the Viewer: the running version (the desktop app's native version through transport, the Viewer's build version) and the changelog from the root `CHANGELOG.md`, loaded on demand through the `virtual:changelog` Vite plugin (SvelteKit's dev server cannot serve root files as `?raw`), with the running release open and marked Current. Render entries as inert text with code spans, never HTML. `npm run release:version` adds each new version's section from feat/fix/perf commit subjects since the previous tag; edit it before committing. Unit tests require the newest section to match the app version, and the release job publishes that section as the GitHub and updater release notes through `node scripts/release.ts notes`. Keep `scripts/release.ts` free of static imports from `src`, because the VPS updater installs it alone. See docs/UPDATES.md.

- The production VPS installs each published GitHub release itself: `agent-studio-update.timer` runs `scripts/vps/update.ts` as root every five minutes, and GitHub holds no VPS credentials. Deploy only releases whose NSIS installer matches GitHub's digest and carries the trusted updater signature for its version (seeded at installation, rotated like installed apps). Build and test the tagged source as the unprivileged `agent-studio-build` user in a `systemd-run` sandbox without access to relay data, backups or secrets. Activate only while the relay runs and no reply is running (waiting at most two hours), after a private data backup, with a 401 health check and automatic rollback of release and data; then replace the public Windows download. Never downgrade, automatically retry an untrusted or rolled-back release, or let release code update the root-owned updater. Verify updater changes with `node scripts/vps/smoke.mjs` and reinstall with `scripts/vps/install.sh`. See docs/DEPLOYMENT.md.

- Hooks appear as metadata in Model context and as bounded, revisioned hook activity in Work history. Claude chats request --include-hook-events and decode hook_started, hook_progress and hook_response; hook responses may arrive without a start event. Discover selected-profile/project settings and enabled plugin hook definitions without executing them, and label discovery limits. Codex uses optional hooks/list with the selected cwd, preserves disabled and review-required trust states, and decodes hook/started, hook/completed and hookPrompt items. Buffer bounded startup hook metadata until the thread binding is verified, reject stale parent-turn and foreign-thread events, retain child attribution, and never revive completed hooks with late start/progress events. Keep commands, raw output, diagnostics, hashes and injected prompt text out of UI activity, context results, prompt replay and exports. Preserve lifecycle metadata through history/relay; missing completion stays unconfirmed.

- Compaction runs only in an idle, bound Claude or Codex conversation through the normal selected-host run transport and native session lock. Expose /compact and Compact context in usage details; preserve drafts when using the button, and reject attachments, unsupported providers, and account switches without a native session. Claude sends /compact with optional focus text on stdin; Codex calls thread/compact/start on the host-owned thread and waits for its turn lifecycle. Require a parent compact_boundary or contextCompaction/thread/compacted confirmation before reporting success, including empty Claude results. Retain bounded revisioned compaction markers through save/export/relay, deduplicate compatibility aliases, keep summaries and native IDs private, and show unconfirmed outcomes after stop/failure. Invalidate earlier context readings and growth trends without inventing token savings. Claude's per-conversation auto-compaction window accepts Automatic or 100,000–1,000,000 tokens, affects chat launches only, and participates in process reuse identity. See docs/NATIVE-SESSIONS.md.

- Rewind here (quiet below your message until hover or focus, always shown on touch screens) and /rewind return to before a chosen user message, preserving the removed suffix in optional conversation rewind data until the next send or retry. Keep Undo rewind available across saves, export, and relay, preserve drafts/settings/location, require an idle chat, and keep /compact unavailable while a rewind can be undone. Revision changes must prevent discarded messages from returning through stale checkpoints; reject only requests older than the host's saved revision or native binding, and carry a one-sided archive change instead of creating a conflict copy. A rewritten history closes the parked process and starts a fresh native session bootstrapped only from retained history, never resuming the old transcript or transferring it to another account; explain that prior native tool details are not carried over. Undo rewind resumes the bound session when the history again continues exactly what it received. Undo edits and /undo preview and confirm only host-recorded successful file edits. Accept conversation/run identities through transport, never caller-supplied paths or patches; scope private checkpoints to the executing host and working folder, compare every complete file before writing, reject later changes, private/unsupported paths and incomplete diffs, and retain idempotent receipts with interrupted-transaction recovery. Preserve the recorded response diff and show undone status; new agent context must acknowledge undone edits. Shell commands and external actions are outside file Undo. See docs/NATIVE-SESSIONS.md.

- Fork conversation is available in the chat toolbar and sidebar context menu; Fork from here branches through a finished assistant reply. Copy bounded portable history and attachments with fresh conversation/message IDs, preserving recorded reply metadata and the exact computer, folder, provider, and account. Whole forks retain next-reply settings; earlier-reply forks use that reply's captured settings. Stop before any running turn, close historical pending questions, keep the source, its native session, queue, and draft intact, and send nothing automatically. Save and enforce the workspace byte limit before opening the Active fork, with recoverable failure and workspace-change guards. Forks bootstrap a fresh native session from saved context on first send; never copy host bindings, credentials, native transcripts, or files. Project forks share their selected folder; Standalone forks receive a new per-chat folder. See docs/NATIVE-SESSIONS.md.

- Mid-turn steering uses a separate Steer now composer action for running Claude and Codex replies; Enter and Queue message retain their follow-up behavior. Send bounded plain text to the originating run and connection through transport, never stop/restart a reply or apply next-reply settings to steering. Codex uses turn/steer with the host-owned expectedTurnId; Claude sends a human stream-json message and confirms its UUID through --replay-user-messages. Record only acknowledged inputs as bounded per-reply steering history, preserve them through save/export/relay and retries, and include them as earlier user context when bootstrapping another native session. Keep failed or unconfirmed input in the draft; deduplicate request identities, reject stale/foreign runs, and do not leave unconfirmed Claude input executing in a parked process after turn completion. Queue images, skills, slash commands, and Gemini messages for a later turn. See docs/NATIVE-SESSIONS.md.

- A shared VPS hosts private workspaces selected only by authenticated pairing keys or sessions. Preserve the existing owner key/root data; put additional users' state, computers, jobs, presence, sessions, notifications, and badges in separate workspace contexts and directories. Never trust a requested workspace ID as authorization. Workspace administration in Settings requires a live server-private admin role. Enforce exactly one enabled admin workspace in the private version 2 registry; new workspaces start as members and creation cannot grant admin. Assigning Administrator to an existing enabled workspace atomically transfers the role and demotes the previous admin. Close its controls immediately and reject stale or concurrent former-admin mutations. Block direct demotion or disablement of the only admin. Upgrade version 1 under the registry lock, keeping the owner when already admin or otherwise the oldest enabled admin, demoting all others without changing keys, sessions, or chat files. Invalid version 2 roles fail closed. Ordinary chat/fleet/job APIs remain scoped even for the admin. Retain data on revocation. Show newly issued keys only transiently, never in workspace storage or logs; clear administration state on disconnect, scope changes, or permission loss. Keep the local CLI for role recovery with the same single-admin invariant. Browser cache and sync state require verified workspace identity; clear visible data on disconnect/change and reject stale-tab requests. Desktop pairing must reject another workspace before changing protected credentials or publishing local data; retain its binding across disconnect. See docs/PRIVATE-WORKSPACES.md.

- Codex and Claude chats expose an Agent Studio visualization tool through Codex dynamic tools and Claude's in-process SDK MCP protocol. Accept bounded parent tool submissions with id, title, and self-contained HTML; never load file paths from reply references or arbitrary tool output. Render accepted visuals between explanatory paragraphs using standalone top-level <!-- visualize:ID --> markers, with fallback below prose for older replies. Match chat typography and colors, use a transparent background with no outer card or repeated title bar, and size frames to content. Keep restart, source, download, modal, and side-panel access through quiet hover/focus controls visible on touch layouts. The sandbox can report only bounded height telemetry matched to its window and per-render token; accept no application commands. Bundle fonts as data without network privileges. Persist exact source and revisions in optional message visualizations through history/export/relay, retaining all visual IDs on checkpoints and replaying completed visual source only as earlier context. Keep source out of activity metadata and title requests. Claude stdin must remain open for SDK tool responses and delegated workflow completion; a result with no delegated work pending ends the reply and leaves the conversation's process parked for its next reply, and background shell servers must not keep a reply open. Preserve selected CLI/profile/cwd, other configured integrations, and owned-process cancellation. Visualization source is limited to 512 KB each, 12 and 2 MB total per reply, and 8 MB of source in request history. External resources and host interaction APIs remain unavailable.

Tauri 2 desktop app with Svelte 5, TypeScript, and a static SvelteKit frontend.

- Keep account credits inside the usage panel opened by any context or limit bar, with no separate Credits control in the bottom strip. Retain the credit reading in Connections. Display Codex account balances with exactly two decimal places, truncating further digits without changing stored precision. Show their estimated USD equivalent at US$0.04 per credit from the user's displayed purchase-pack prices, with the rate and checkout discount/tax caveat. Calculate from the full finite balance, retain unknown/unlimited/zero distinctions, and never substitute this estimate for provider-reported chat costs. See docs/USAGE.md.

- The expanded usage panel shows saved per-chat tokens and provider-estimated spend. Claude's total_cost_usd and modelUsage are running totals for the CLI process: record each reply's cost as the change from the parked process's last reported total (a fresh process starts at zero, since parked processes are terminated and never save a total to restore), leave it unknown when the total drops, take reasoning tokens from each turn's usage, and mark these readings scope "reply". Sum those reply costs once; label older saved Claude running totals as chat cost so far and exclude them from chat totals; Codex's app-server adapter resets token counters whenever a reply starts a new process (after idle expiry, changed settings, or a failure), so sum only explicitly scoped reply counts and mark older unscoped history incomplete. Keep native-session billing estimates separate from reply costs: read optional Codex `account/usage/read` with the loaded parent thread ID and convert safe integer credit/USD micros without an assumed exchange rate. An absent estimate remains unknown. Capture bounded before/after account readings through the exact run profile, with run identity and revisions, preserving them through history/export/relay and excluding them from prompts. Show their changes as potentially shared account observations, never exact per-chat limit use or billed spend; refuse quota deltas across resets/missing readings and never add overlapping account deltas into a chat charge. Exclude account-read overhead from completed reply timing. See docs/USAGE.md.

- End each completed, stopped, or failed assistant response with Files edited beside elapsed and cumulative AI time in the same compact footer row, followed by the reply's Plan, native workflow and Background work toggles when it has them. All of its disclosures start collapsed; clicking a label opens its own panel and closes any other, and clicking the open label collapses it again. Preserve file scope and list expansion when switching panels. Show five files initially per scope, with Show all files and Show fewer files controls for longer lists. Omit the routine response-scope, combined-chat, and coverage notes. Keep individual code diffs collapsed until opened. Label newly created files Added and existing files Edited. Added files omit per-file line-change counts and hunk coordinate headers, and are excluded from the summary line-change totals; retain their expandable source and unavailable-source notices; use project-relative paths or a shared recorded directory when clear, keep full paths on hover, and contain each file in a subtle theme-colored border. Its This response view shows that reply's recorded edits; All chat changes composes the latest recorded file state across the entire chat, including later replies, with reversions omitted. Expand individual files for inert, line-numbered code diffs and additions/deletions. Record only successful native Codex file-change and Claude file-edit results in bounded revisioned message data, separate from activity and prompt replay. Preserve them through save/export/relay, rejecting stale or mismatched run snapshots. Never read paths or infer changes from shell output or prose. Missing, private, oversized, malformed, or discontinuous diffs stay explicitly unavailable; unrecorded history is not zero changes. Keep draft, disclosure, keyboard, and narrow-screen behavior intact.

- New Standalone chats use a persistent per-conversation working folder on their selected execution host, including managed WSL. Preserve it across replies, retries, and restarts; keep account/profile settings, tools, and global hooks intact. Existing chats retain their original shared runtime and native session bindings. Store validated UUID-based working-folder bindings only in host-local app data, never accept arbitrary runtime paths or sync their files. Context and skill inspection must use the same bound folder and cache Standalone inventories per conversation. Retain files on chat deletion and keep restricted background runtimes separate. See docs/CAPABILITIES.md.

- Suppress native lifecycle cards and the chime only for the chat currently viewed in a visible, focused chat window. Consume suppressed events without replay on blur, and keep other-chat, hidden/unfocused, Connections/Settings, and manual test alerts. Report revisioned, per-window foreground chat leases through transport to the authenticated relay; suppress that chat's workspace PWA pushes before enqueue and retry delivery, while keeping every received worker push visible. Keep leases transient, bounded, session-aware, and short-lived; never change pending badge counts based on reading.

- Pending chat badges count all Active conversations without a running assistant reply, including completed, failed, stopped, and saved idle chats. Opening/reading a chat, selecting History, or searching must not change that total. Keep the sidebar badge separate from Active/History totals, hide/clear it at zero, and route app-icon updates through transport. Windows uses an application-owned numbered taskbar overlay (99+ above 99); macOS/Linux use native badge counts, and supported PWAs use the Badging API. Mobile lifecycle pushes carry the current relay count at delivery/retry; never increment badges per alert or send extra visible alerts just to change a badge. Notification cards and the custom chime must still trigger when another chat finishes while the app is focused. See docs/NOTIFICATIONS.md.

- Native desktop notifications and the custom chime default to enabled per installation; preserve saved Disable and Mute choices in Settings, independently of mobile push and relay pairing. Request macOS authorization before the first alert and respect the OS decision. Use fixed generic lifecycle alerts, device-local settings/receipts, and the bundled Agent Studio chime through native audio. Silence the OS notification sound (WinRT silent audio, Linux suppress-sound, macOS nil sound), with a mute and test control. Do not ring on startup history, duplicate checkpoints, background queries, or child questions. Route UI through transport, preserve drafts on clicks, keep errors separate from run/storage success, and retain current app-close behavior. See docs/NOTIFICATIONS.md.

- Mobile push notifications are opt-in per paired browser under Settings. Send generic alerts for completed, failed, stopped, and explicit parent question-tool updates through the relay, including desktop-started chats; normal prose questions are covered by reply completion. Keep notification content free of chat text/titles. Persist VAPID keys, session-bound subscriptions, deduplication receipts, and bounded retry queues only in private relay data. Validate push-service endpoints, revoke with browser sessions and pairing-key rotation, deduplicate workspace/job updates by run, and never replay imported history. Every worker push displays a visible alert; clicks open the matching chat after sync and preserve an existing draft. Desktop Tauri never registers a service worker. See docs/MOBILE.md.

- The relay server also hosts the installable mobile PWA at its origin root. Browser clients are controllers: route all execution, folder, model, context, and usage requests through the selected host using transport; never register a browser as a CLI computer or claim worker jobs there. Browser pairing uses the same-origin HttpOnly session endpoint, never persistent pairing keys. Cache only the public app shell, never relay APIs or commands; do not replay offline work. Keep the Viewer's workspace copy, sync checkpoint and backup in IndexedDB, never localStorage (about 5 MB per origin, less than two copies of a large workspace); move copies saved there by earlier releases without replacing newer data. Keep the mobile conversation drawer, touch controls, safe areas, and keyboard-height behavior usable without hiding history or settings. Build the frontend before server/PWA tests and serialize them with native builds. See docs/MOBILE.md.

- Read this file before changes. Keep UI components independent of desktop IPC; use `src/lib/transport.ts`.
- Follow incoming chat updates only while the reader is at the bottom. Scrolling up must preserve the reading position, including small wheel or touch movements near the end; returning to the bottom resumes following. Do not animate automatic scrolling on every streamed event. Keep the full conversation and expanded activity reachable above the composer. The message input rests at three lines and grows with its draft until the composer box reaches half the window (four lines on phones), then scrolls; size it with CSS `field-sizing: content`, not per-keystroke measuring, so engines without it keep the fixed three lines. A taller composer, smaller window or phone keyboard keeps a followed chat's end in view and leaves a reading position above the end in place; usage details opened above the composer stay within the remaining conversation, below the toolbar.
- Expanding or collapsing chat content (summaries, `aria-expanded` disclosures, tabs) keeps the visible content in place, or the activated control when a collapse removes that content. Near the end, add blank virtual space below the conversation instead of letting the shorter chat pull everything down. Scrolling up, new content, and a smaller viewport consume the space; it never extends scrolling past the held position, and switching conversations clears it. Toggles update following like scrolling: revealing content below the end stops following and returning to the end resumes it, while held space keeps the following state so new content fills the space before the chat follows. See docs/CAPABILITIES.md.
- Codex and Claude question tools display bounded parent questions inline, with single/multiple choices, free text, explicit submission and skip. Use native Codex requestUserInput, Claude AskUserQuestion, and the studio_ask_user dynamic/SDK tool. Keep answer transport pinned to the originating run, connection and request; never submit defaults, restart a reply, or turn answers into shell text. Preserve the composer draft and show recoverable delivery errors. Reject child, stale, duplicate, malformed and secret-input requests. Persist revisioned questions and submitted answers through history/export/relay, replaying only answered context. Close pending questions on stop, completion, expiry or process loss; startup history cannot regain a live answer channel. Keep the provider pipe open while questions wait, allow Claude/Codex runs and user-input waits without an elapsed-time cutoff, retain cancellation, and notify once per question without including its text. Antigravity's current print interface has no supported interactive question response protocol. See docs/CAPABILITIES.md.
- Multi-question calls show one question at a time with a progress counter, numbered navigation, and Back/Next controls. Allow browsing unanswered questions and revisiting answered ones before submission; retain choices and text between steps and relay updates. Require complete valid answers only when sending from the final step. Each question independently uses radios for one answer or checkboxes for multiple answers. Skip questions applies to the whole call. Hide submitted question forms without a Your answers card; retain their data in saved history, exports, and provider context. Single-question calls retain direct submission.
- The composer Templates library stores reusable plain-text prompts with named `{{field name}}` inputs in optional workspace v3 `inputTemplates`. Support create, edit, confirmed deletion, repeated fields, multiline values, and a message preview. Insert into message appends to the existing draft and focuses it; never submit, start a run, restore a History chat, or change attachments/settings on insertion. Keep fill values temporary, preserve draft text on cancel/failure, cap the combined message at 30,000 characters, and close the dialog on conversation/workspace changes or leaving chat. Sync/export definitions within the authenticated workspace, retain divergent edits as conflict copies, and reject stale editor saves. See docs/CAPABILITIES.md.
- Unsent composer drafts are device-local and keyed per conversation, and per location for new chats (each folder, each computer's Standalone location, or the computer while no location is available). Switching chats or locations, Connections/Settings, forks, notifications, reloads and app restarts keep them; sending, steering, queuing or clearing removes the draft, and deleting a conversation locally or through sync deletes its draft. A new chat's folder/computer change and `/new` carry the text, keeping an existing destination draft first. Save only text and mention identity through transport, never images (kept in memory for the session): desktop `drafts.json` in app data via async native commands, the Viewer in workspace-scoped browser storage cleared with its cache on sign-out, expiry or workspace change. Never put drafts in the workspace, exports or relay sync. Throttle saves while typing and flush on blur/hide/pagehide, title-bar close and update restart; merge per key by timestamp with other tabs, bound to 100 drafts/2 MB, and surface read/save failures. Restore mentions only while their chat, account and folder scope matches. See docs/CAPABILITIES.md.
- The composer offers leading `/` commands and skills through the shared ChoicePicker, with arrows, Enter/Tab completion, Escape dismissal, and touch selection. App shortcuts open the existing controls and preserve remaining draft text. Discover Claude commands from selected-profile initialization and Codex skills from its enabled CLI catalog through the existing context transport/cache. Keep catalog metadata session-only, scoped to connection/model/computer/environment/folder; skip unavailable connections and ignore late selections. Claude commands retain literal arguments on stdin. Save Codex skill name/path metadata with user messages and send only the current invocation as a native skill input; never replay earlier skill inputs or treat selection as a confirmed invocation. Unknown, disabled, unavailable, or unsupported persistent-session commands keep the draft with an explanation; do not silently send them as prose. See docs/CAPABILITIES.md.
- Keep healthy PWA connections quiet: omit the persistent connected banner and its empty row in installed apps. Retain setup/offline/server-error notices, installation access in browser tabs, and detailed sync status in Connections.
- Label the web/PWA logo and installed app as Agent Studio Viewer; retain the native Agent Studio name. Install app opens a chooser with Viewer (PWA) and, on desktop browsers, a clearly labelled Windows desktop installer when available. Phones/tablets only offer Viewer. Keep unsupported-browser guidance and one-shot install prompts across login. Publish installers only through the relay's fixed download allowlist in a dedicated public directory, outside the frontend shell cache and private workspace data; do not offer missing packages or imply Windows packages run natively on other operating systems.
- A paired PWA is connected to the relay, independently of whether an execution computer is online. Show offline computers in the chat picker, selected computer label, and sidebar, using the owning host's heartbeat for both Desktop and its managed WSL distributions. Skip model/usage queries to unavailable remote connections, retain saved choices, and refresh models when the selected connection returns. Treat the relay's `host_offline` rejection as availability state; retain warnings for actual model-query failures on an available connection.
- PWA pairing must survive app updates and relay restarts. Persist only keyed browser-session ID digests in the relay data directory, outside public files and workspace exports; retain the data directory and pairing key across releases. Renew active sessions with their HttpOnly/SameSite/Secure protections, persist explicit disconnects, and invalidate sessions on pairing-key rotation. Retry temporary startup outages without requiring another pairing or replaying commands.
- Style the UI through the token design system in docs/DESIGN-SYSTEM.md. Semantic tokens live only in `src/lib/theme.css` (graphite surfaces, one lime accent, Geist Sans/Mono); shared primitives live in `src/lib/styles.css`; components use tokens, never raw colors or uppercase micro-labels. Dark is the default; Settings → Appearance offers Dark, Light and System per device (local storage `agent-studio.theme`, root `data-theme`), and visualization frames receive the resolved theme. Overlays fade with opacity only so measured layout never moves. The brand mark is Agent A (`BrandMark.svelte`); regenerate every app, web and PWA icon with `node scripts/brand-icons.mjs`. Never pair `:has()` with a universal selector such as `.app-shell:has(…) *`, which searches the whole app on every style recalculation, including each hover transition frame; mark app-wide states such as drag cursors with a root class.
- Keep collapsed chat content out of the page: Work history, tool groups and calls, sub-agent messages and results, Files edited and each file diff render on first expansion through `revealedDisclosures` (`src/lib/disclosures.ts`, whose summary click renders before the disclosure opens so held reading positions measure the contents) and stay mounted afterwards to keep nested expansion and file scope. Keep the `styles.css` rule that makes closed `<details>` content `display: none`: Chromium's default `content-visibility: hidden` makes a text selection across it restyle all of it at once, which froze long chats for seconds. See docs/VERIFICATION.md.
- Always use the design-system `ChoicePicker.svelte` for dropdowns, including forms and management pages. Do not add native HTML `select` elements. Use its `field` variant for full-width form controls and retain keyboard navigation and accessible labels.
- New-chat Agent choices belong to the selected computer, including before folder selection. Exclude connections confirmed unavailable even when their provider was previously selected; leave the options empty when no CLI is available and show the muted placeholder No agents. Show Select agent when choices exist but none is selected, never a literally blank trigger or another option presented as selected. Existing conversations retain their fixed agent label.
- Desktop opens directly in chat. Web/PWA opens on workspace sign-in and keeps the chat shell, Connections, Settings, and dialogs unmounted until the server verifies the session and workspace. Sign in with the private workspace key on the current origin; restore valid sessions automatically and retry temporary startup outages. Sign out, session expiry/revocation, and cross-tab workspace changes return to login and clear drafts/dialogs. Retain Install app before sign-in. Center the new-conversation welcome vertically in the space between the toolbar and composer, keep it free of suggestion cards and omit the CLI login/execution subtitle from the composer. Keep the sidebar compact: a small Agent Studio wordmark without a tagline, conversation search and Active/History groups, and the Plug button for Connections beside the gear button for Settings in the footer. The entire logo, including its mark and text, is a window drag region. Keep the small conversation and folder plus actions and Ctrl+N. Do not reintroduce Overview, a Workflows navigation page, the large New conversation button, or the Personal workspace footer.
- Keep agent setup, computers, accounts, and relay management together in the Connections view, retaining the Plug icon. Keep notifications, workspace administration, and the workspace data/export section in a separate Settings view opened by the gear button beside Plug and by the /settings command; Connections shows no notification, administration, or export sections. Always use the shared dropdown component. Opening Connections or Settings and returning through the logo or the same footer button must preserve the open chat and draft.
- Organize Connections by computer. Each computer contains its environments, provider CLIs, and account connections; filter connections to that computer. Shared account labels may appear on each computer where they are connected, but never mix another computer's connections into its card. Keep naming, account setup, and relay pairing in contextual dialogs. Explain that existing CLI login shares the environment's login/settings while a separate profile uses the same CLI with independent login/settings; relay sync never copies credentials.
- Keep computer cards concise: no nested environment cards (Here/Managed here), repeated environment/Connected rows, or CLI setup accordions. Show installation state as Installed/Not installed/unknown without repeating the computer name, and expose Open sign-in directly beside account actions. Account naming and editing have no Personal/Work purpose controls or badges; retain legacy stored purpose metadata when editing old accounts.
- Provider cards use a compact header with installation status, a single-line CLI path with the full path on hover, and account rows with Chat and Open sign-in. The account settings icon opens a contextual Manage account dialog; never expand rename/disconnect forms inside the card. Put profile information and confirmed disconnection in that dialog. Offer connection on the current computer only when the account has no connection there, and name the target computer explicitly.
- Stack provider CLIs vertically within each computer in Connections. Show each connection's account usage beside its actions on wide windows and below them on narrow windows: 5-hour and Weekly used percentages, resets, pace, and remaining budget. Keep additional reported model-specific limits explicitly labelled with their model. Account usage has no chat-context meter. Query through transport using the exact connection, including remote/WSL connections; skip missing, signed-out, and offline connections. Refresh on opening, focus, the page refresh action, and every minute while visible. Retain last readings on failure/offline, suppress their pace guides, distinguish unknown from zero, and show loading/checked state. Keep snapshots in session memory. Queue native usage reads behind the bounded process limit instead of rejecting accounts when several refresh together.
- Detect installed CLIs and their existing login connections automatically on each locally managed computer. Read the login identity the CLI itself reports (Claude's account email; none for Codex/Gemini) as session-only status metadata, never a credential. Do not add a CLI login connection when that identity is already connected on the same environment through another profile; while the login or a sibling connection is unchecked and the agent is already offered there, wait for the report. Connect a managed WSL terminal login under an existing account label reporting the same identity, reuse an unconnected default CLI login label instead of adding another, and never remove or relabel connections automatically. Add account is exclusively for additional Claude/Codex logins: show Provider and Account name, with the selected computer fixed. Do not show Account, Environment, or Login profile dropdowns or explanatory existing-login paragraphs in this modal. Always create an isolated profile, show Creating account and Opening sign-in progress, save it before launching sign-in automatically, and refresh in the background. Keep launch failures visible and retry the same saved connection rather than creating duplicates. Track sign-in completion by connection, never by another profile's ready status. Gemini remains existing-login only.
- Windows sign-in starts PowerShell in its own console without inheriting the GUI's standard handles, verifies a readiness signal, and retains the console after command failure. A successful process spawn alone does not prove the terminal opened. Keep CLI authentication local and leave existing logins untouched.
- Present each Windows-managed WSL distribution as a separate computer in Connections, the chat computer picker, and conversation groups. Keep persisted physical host, environment, connection, and folder identities intact. Show installation inventory from that environment's own executable lookup, independently of login status; reject Windows PATH shims as Linux installations and show failed checks as unknown or stale. Missing CLIs require installation before account connection or sign-in is available.
- The Windows app manages WSL CLIs directly; no second Agent Studio app or relay pairing inside WSL is required. Inventory uses read-only listing commands. The selected computer determines the CLI and account for both existing logins and separate profiles: Desktop uses only its Windows CLIs, including for WSL folders; a WSL computer uses only that distribution's Linux CLIs. Never fall back to another computer, distribution, or account when a CLI is missing or fails. Checking or executing a WSL CLI can start that distribution.
- WSL launches pass arguments as data to fixed scripts, keep prompts on stdin, use Linux-local profile/runtime directories, and cancel the owned Linux process group as well as wsl.exe. Never terminate a whole distribution to stop a chat. Do not read or copy CLI credentials across environments. Embedded shell scripts are pinned to LF by `.gitattributes` and normalized through `wsl::embedded_script` before they reach bash; a CRLF checkout on a Windows build runner otherwise makes every WSL command fail with a bash syntax error.
- CLI adapters belong in `src-tauri/src/providers.rs`; translate provider JSONL into the typed events in `protocol.rs`. Never render raw CLI output as HTML or emulate a terminal.
- Require verified sign-in for the selected connection before sending. Show a composer warning and Open Connections action for signed-out or unverified accounts, preserving the draft and History state on blocked submissions. Refresh authentication after explicit sign-in. Skip automatic model queries until that connection is ready; catalog requests must only launch the selected provider. Gemini chat and model listing must first pass the headless account query, and authentication-required output must terminate the owned process instead of waiting for interactive login.
- Authentication belongs to the installed CLIs. Never read, copy, or persist their tokens. Prompts go through stdin; do not interpolate user input into shell commands.
- Usage readers use read-only CLI queries. Missing limits are unknown, not zero; preserve stale readings on failures. Claude Fable limits come from server-labelled model scopes and appear only for Fable. The main context meter uses the latest compatible reply's provider-reported request count (`contextInput`), never turn totals or draft estimates. Unmeasured context stays unknown. Serialize frontend builds and browser checks against the reused Vite server, since build-generated files can reload test pages.
- Model context inspection is a separate source and MCP inventory scoped to the selected model, connection, execution environment, and folder. Route it through transport to the owning host and use only the selected computer's CLI and profile. Distinguish CLI-reported metadata from discovered, disabled, overridden, and companion guidance; never claim a startup query is a previous reply's read history. Memories include global entrypoints and current-project/folder sources only, excluding unrelated projects and unscoped archives; Claude's effective memory report replaces guessed directories. Show MCP names and allowlisted status/scope metadata from the selected CLI, distinguishing configured from connected and retaining disabled/unavailable states. Never return configuration bodies, credentials, memory contents, MCP commands/URLs/environment, tool schemas, or raw errors, and never invoke tools during inspection. Keep inspection out of persisted conversations. Use stable selection values and untracked transport calls in Svelte effects so relay polling cannot restart inspection.
- Keep the Model context dialog height stable across loading, cached refreshes, results, filters, and errors. Cap it to the available viewport with no scrollbar on the outer frame. Let the source list fill the remaining space and scroll internally; narrow/short screens use a single scrollable body. Keep the header and footer fixed and retain reachable category tabs and closing controls.
- Reopening Model context shows its latest cached inventory immediately while refreshing. Keep cache keys specific to provider, model, connection, computer, environment, and folder; share pending requests for the same selection. Preserve the last result and its timestamp on refresh failure. Cache only bounded session metadata, with no persistence into conversation storage.
- Model context's Native prompt tab is an explicit exception to metadata-only source inspection: read bounded native instruction text on demand from the saved conversation's host-local binding and selected CLI profile. Show Claude's latest parent prompt snapshot and Codex's recorded base instructions and unique developer messages with capture/version metadata; label historical records and current Agent Studio additions accurately. Never accept native session IDs or paths from the caller, reconstruct missing prompts, launch a model, or expose user messages, tool results, credentials, configuration bodies, or tool schemas. Keep prompt text out of source/skill caches, workspace persistence, exports, checkpoints, and logs; remote inspection uses transient authenticated workspace jobs. Clear prompt text on close, selection changes, and read failures. See docs/NATIVE-INSTRUCTIONS.md.
- All Claude, Codex, and Gemini/Antigravity conversations enable tools and load the selected CLI profile's configured integrations, skills, and project guidance. Build mode launches chat agents with full access and without ordinary execution approval prompts: Claude and Antigravity use --dangerously-skip-permissions; Codex app-server threads use sandbox=danger-full-access and granular approval policy with only mcp_elicitations enabled; sandbox_approval, rules, skill_approval and request_permissions stay disabled. Explicit Claude Plan mode and EnterPlanMode/ExitPlanMode decisions follow docs/PLAN-MODE.md. Antigravity uses accept-edits mode and its default agent; remove only the exact legacy app-generated deny hook from the chat runtime. The selected folder is the CLI working directory. Background title generation and usage queries remain restricted in their separate runtimes. Keep the Agent picker hover description accurate about tool access.
- Codex chat uses persistent app-server threads bound locally to the conversation, account, environment, and folder, so native history survives replies and restarts while sub-agent lifecycle, child results, and tool events reach the UI; restricted titles still use exec. Keep the validated process working directory, profile isolation, stdin requests, and owned process-tree cancellation. Tool activity is bounded, typed metadata stored additively in activity blocks and updated by identity/revision through local runs, relay delivery, and checkpoints. Keep child text/usage out of the main reply. Distinguish available skills from actual skill invocations or observed file reads; adding a skill to the composer does not claim it loaded. Activity records carry commands and bounded inputs, never results or configuration bodies; results follow the tool results rule. Show unconfirmed outcomes after missing completion instead of inventing success.
- Tool calls show what ran and what came back. Records keep the script a shell call ran (Claude Bash/PowerShell/Monitor input; Codex `command` unwrapped from its `powershell.exe -Command '…'` or `bash -lc '…'` line, which may start with lines a configured CLI hook prepended), up to 8,000 characters, plus pretty JSON arguments of connected (MCP), dynamic and unrecognized tools up to 4,000; skill arguments and web page prompts become bounded facts, and Agent Studio's own tools and Write/Edit bodies are never recorded as inputs. Results (shell output and errors, file reads with their first line, search matches, web page text, connected-tool text and images, Claude image reads, Codex image views) stay on the executing computer: `ToolDecoder::take_outputs` hands each to the run's `tool_output::Recorder`, which writes bounded files under app data `tool-output/<run id>/` (256 KB output and 64 KB errors as UTF-8, beginning and end kept; ANSI escapes and carriage-return redraws cleaned; up to eight sniffed PNG/JPEG/GIF/WebP images of 5 MB, 8 MB per call; 256 MB per run). The synced record keeps only its size summary (`output`: lines, bytes, images, stderr, exit code, truncation). Windows read a result when its call is opened through transport (`read_tool_output` here, `toolOutput` relay jobs to the owning host, named only by run UUID and activity ID), render text inertly with line numbers and highlighting and images as data URLs, and cache a bounded set. Never sync, export, relay-checkpoint or prompt-replay results, or read caller-supplied paths; Codex image views read the reported file once at completion (regular file, sniffed type, WSL paths through the distribution's translation). Prune at startup and after each run: runs the saved workspace no longer references after an hour, runs older than 30 days, and the oldest beyond 2 GB. Each call and group gets an icon for what it does (the Git logo for git commands, test, build, package, search and other command kinds; file, code, image, edit, web, connected, browser and agent tools), from reported metadata and the command text only. See docs/CAPABILITIES.md.
- Show progress text and tool activity inline while a reply runs. In live activity and expanded history, group consecutive tool calls into collapsed, quiet rows whose labels give an exact count for every action kind in first-seen order (Read 3 files, ran 2 commands, and used 1 tool), keeping reasoning and progress comments in order between groups. Expand a group for individual calls and a call for its recorded details. Preserve user expansion during streamed revisions and appended calls. Derive labels and counts from reported action metadata: a reported file path counts once however often it is read or edited (pathless calls count separately), a resumed sub-agent once, and every other call once; the activity-limit notice is never counted. Use nouns instead of past tense when a call failed, stopped, or is unconfirmed, and keep those outcomes visible on collapsed groups, including child work. On completion, show a collapsed Work history disclosure directly below the model title and above the final answer, with a divider below expanded history. Keep its label concise: Work history, with no activity counts or category filters. Expand it to review all reasoning, progress comments and tool calls in their recorded order. Keep intermediate progress separate from the final answer and prompt replay, and retain it through saved history and relay updates. Tool headers must identify their command, file, pattern, query, or description when reported, with the icon of their kind; details show the command, input, targets, line ranges, match counts/paths, returned tool names, an edit's own recorded diff, and the result read from the executing computer with its exit code. Missing metadata or results get an explicit explanation, never an empty disclosure or fabricated target.
- Quota pace compares usage with an even-use guide at the reading's timestamp; stale, failed, missing, or expired readings have no pace classification. Context growth uses consecutive compatible measured request counts, stops at gaps/model changes/decreases, and never treats draft forecasts as measured usage.
- Keep quota comparison colors consistent in Connections and chat: below the guide, show a muted green secondary bar behind the current fill; above the guide, render the guide in orange above the overlapping fill and show the excess in red. Use the two colored bar segments without an endpoint marker. Preserve the shared percentage scale and remove the guide when stale or unavailable. Omit Codex's unreported 5-hour window, including placeholder rows, while retaining any actually reported 5-hour limits such as Spark's separate quota.
- Keep usage UI concise: show current numbers, resets, budgets, and actionable warnings. Do not show a next-request estimate or Details section. Use colored trend arrows with hover descriptions and accessible names for limits: On track is blue/right, Below pace green/downward trend, Ahead of pace amber/upward trend, and exhausted limits red/upward trend. Unknown pace uses a gray dash. Quota bars use the green/orange/red comparison colors when a guide is present; otherwise their fill matches the indicator. Context shows measured tokens against its window (35.2K / 1M), not a percentage, without a Reported label or arrow; its hover keeps the exact counts and percentage used before its guidance, and retain expanded warnings.
- Place Usage and context directly below the message input, replacing the Tools enabled caption. Show compact groups for Context, 5-hour, and Weekly, plus Fable weekly whenever Claude Fable is selected (including a CLI default resolving to Fable), with unknown readings visibly distinct from zero; omit an unreported Codex 5-hour window and let the remaining groups fill the row. When a group's heading wraps, keep its value beside its pace arrow. Clicking a bar opens the reset/budget/guidance panel above the input, which also keeps Fable's scoped limit card. Keep automatic refresh and keyboard expansion/closing, but no refresh or gauge buttons. Vertically center the panel title and Checked text with Close. Overlay the current quota fill and a secondary recommendation on the same track and percentage scale, keeping both colored segments visible when current usage passes the guide. Do not show a Recommended label or separate guide row; retain the comparison in hover and accessible text. Use the existing timestamp-based even-use guide; hide it when stale, failed, expired, or unknown, and do not invent a timed context recommendation. Keep tool-access information in the Agent picker hover description. Do not restore the usage strip above the chat.
- Render language-labelled Markdown code fences with bundled syntax highlighting in replies and progress, including streaming and restored history. Use the same colors in the HTML/SVG artifact Source tab, preserving exact source text and downloads in modal and panel views. Keep inline code, unknown/unlabelled languages, and oversized blocks readable as escaped plain code. Sanitize after highlighting and keep HTML snippets inert in chat and source views.
- Claude background launches set an optional `background` flag on their call or sub-agent from explicit CLI signals only: Bash/PowerShell `backgroundTaskId`, Monitor `taskId`, and Agent `isAsync`; the status then follows the task's notification and a late launch result never revives it. History never shows background work running: groups count started background tasks/sub-agents without a spinner or clock, and calls read In background while the reply runs, Left running after a completed reply, and unconfirmed after a stop or failure. A Background work toggle in the footer row of the reply that started the work counts what still runs, during and after that reply; opening it lists each item with its reported description and host-measured elapsed time, never commands. Each Claude chat process carries a watch: the runner hands over what a reply leaves running, the output observer follows task notifications while the process is parked or serving later replies, and a finished task sends its outcome to update that saved reply; releasing the process records Stopped. The list is transient, local to the executing computer and fetched again by a reloaded window; the Viewer shows it only while a reply runs. Codex reports no explicit signal and is unchanged. See docs/CAPABILITIES.md.
- Show provider-reported parent reasoning inside live activity and Work history at its recorded position between progress comments and tool groups: muted, sanitized Markdown beside a brain mark, readable without another disclosure and separate from the final text. Do not restore a separate Reasoning disclosure. Decode Claude thinking blocks/deltas and Codex reasoning items/summary/text deltas; request automatic Codex summaries and prefer a reported summary over the same item's content. Claude Code prints each content block as its own assistant line, so a snapshot never re-reports thinking that already streamed for its message, unstreamed snapshots number blocks by their position across those lines, and empty thinking blocks reserve no item. Older replies saved streamed thinking twice under the same Claude message; show one copy per message and identical text. Preserve bounded, revisioned blocks through saved history, exports and relay checkpoints, including stopped/failed replies. Do not replay reasoning into prompts, extract artifacts from it, expose signatures/encrypted/redacted payloads, or mix child reasoning into the parent reply. Replies without reported reasoning show none.
- Persist portable agent and conversation data through the versioned schema. Surface storage failures. Keep credentials out of exports and tests.
- Chat image attachments support file selection, paste, and drops anywhere in the chat area for Codex and Claude, with previews, removal, and image-only sends. Show a drop hint, clear it on exit/drop/cancellation, preserve the draft, and focus the composer after a drop. Handle file drops once; prevent dropped files from navigating the app outside chat, and retain ordinary text drops. Preserve original PNG/JPEG/WebP bytes in optional workspace v3 user-message images, including exports, relay checkpoints, retries, and later turns. Limit attachments to four per message, 2 MiB each, and 8 MiB per conversation; retain the 20 MB workspace limit and reject oversized drafts before sending. Send Codex image data URLs and Claude base64 image blocks as structured stdin data, never paths or text-encoded image prompts. Keep image bytes out of title generation, text token estimates, and tool activity. Gemini's Antigravity stream input is text-only; explain the unavailable attachment action. Disable Tauri native drag interception so HTML file drops reach the chat.
- Workspace v3 separates computers, environments, account labels, and CLI connections. Installation identity and sync checkpoints are local. Never route a missing/offline connection to a different account; profile scope must cover detection, sign-in, models, usage, titles, and chat.
- New conversations require an execution computer; the project folder is optional. Offer Standalone in the Folder picker to clear a draft folder without changing its text, attachments, agent, model, or reasoning. Computer-scoped drafts default to Standalone, and Agent and Model are usable without selecting a project folder. Retain the selected computer and environment in the standalone location identity and omit the empty project path from provider requests. Persist the folder environment and, when different, the execution environment; keep both and the account connection stable. Desktop may browse its Windows folders and its managed WSL folders; a WSL computer browses its own distribution. Browse only directory names through transport on the owning host. Windows CLIs access a selected WSL folder through its translated Windows/UNC path; a selected WSL CLI uses the original Linux path. Fail if the same folder is unavailable or cannot be represented on the selected computer; never silently substitute the app runtime or another computer. Apply the provider-specific chat tool access policy. Generate application-owned agent definitions/hooks only in app data, never in the selected project. Legacy chats without a folder and title generation retain the isolated runtime. Keep Active and History sections grouped by execution computer then folder. On app startup, move all saved chats to History without changing their messages, settings, or recency. History chats allow typing and next-reply settings immediately; sending a valid message automatically restores the same chat to Active. Opening, typing, empty submissions, and blocked sends must not restore it. Preserve location/archive metadata in storage and relay conflict handling, including one-sided archive changes concurrent with response checkpoints.
- The Choose a folder dialog is a wide, fixed-height browser whose list scrolls internally. It opens at the selected folder of the same environment (otherwise Home) and shows clickable breadcrumbs with an Edit path mode (Ctrl+L), a Quick access column (Recent folders for the selected computer and environment, Home, existing common project folders, Windows drive roots or WSL drive mounts, Linux root), a filter box that ranks prefix matches first and jumps to pasted absolute or `~/` paths on Enter, a Show hidden toggle remembered per device, repository markers for folders containing `.git`, and Arrow/Enter/Backspace navigation. Rows only open folders; Use this folder is the single selection action, with no per-row Use buttons, refresh button, or footer path summary. A failed path keeps its breadcrumbs, shows the error, and disables Use this folder. Listings report only directory names plus hidden and `.git`-presence flags derived from names and attributes; never read folder contents, add file operations, or create folders. Places are optional in listings so older hosts still work, and narrow windows turn Quick access into a chip strip. Keep the toolbar Folder dropdown showing the environment as detail with the full path only on hover.
- Keep Computer, Folder, Agent, Model, and Reasoning inline in one chat toolbar with decorative laptop, folder, bot, chip, and brain icons inside their dropdown triggers. Multiple account connections belong in the Agent picker; do not add a separate Run on row. Show the full folder path only as a hover title in the toolbar and folder choices. The top bar shows only the current page/conversation title, with no Workspace breadcrumb, Local desktop badge, or version text.
- Reselecting the current computer or folder is a no-op and preserves the draft. Apply validated folders without awaiting CLI detection or relay sync. Reuse connection availability within an environment and cache model catalogs per provider/connection; keep Model usable during refresh. Unknown local CLIs may be configured while checking, but sending still requires detected availability. Late checks must never replace newer folder, conversation, or explicit agent/model/reasoning choices.
- Computer, Folder, and Agent are fixed once a conversation exists, starting with its first submitted message. Lock their controls and mutation handlers for existing, restored, and legacy conversations; a new blank conversation can choose all three. The Agent picker of an existing Claude or Codex conversation still offers that agent's other available account connections on the same computer, only while no reply is running, and stays disabled with a single account. An account switch applies to the next submitted message with a next-message hint and a switch notice before the affected reply; each reply records its own connection. The execution host forks the latest verified native parent transcript into the selected profile, preserving tool results and compaction records, source transcripts, and credentials. Resolve source identity and paths only from host bindings and the local account registry; reject missing, corrupt, conflicting, oversized, and incomplete transcripts without silently replaying visible history. Switch back from the latest session, never an old account snapshot. Live terminals, workers, and subagent sidecars are not transferred. A moved computer or folder still fails explicitly, Standalone keeps its working folder, and legacy bindings require an explicit accountSwitch flag plus verified source scope. Model and Reasoning remain editable while a reply runs and apply to the next submitted message, with a small pending hint and a switch notice before the affected reply. Instructions remain editable between replies. Preserve in-flight and historical reply settings, including during relay checkpoints; concurrent deliberate settings edits still require conflict copies. See docs/NATIVE-SESSIONS.md.
- Reply headings show the captured or provider-reported model name, rather than the provider or a former preset name. Keep the CLI/account/computer execution subtitle hidden. Save the model display name with each reply so catalog or conversation changes cannot relabel history; an unreported default remains explicitly unknown as CLI default.
- The conversation sidebar uses Active and History tabs with counts, then muted computer headings, collapsible folder groups indented one level under their computer (including Standalone), and readable conversation rows. Its right divider supports dragging and keyboard resizing, remembers the width locally, and leaves room for the chat area when the window shrinks. Avoid stacked status accordions, repeated conversation icons, or tree guide lines. Keep tab keyboard navigation, folder hover paths, running indicators, and current-conversation semantics. Archive/restore reveals the destination tab and folder; switching tabs does not change the open conversation.
- Each sidebar computer and folder, including Standalone, has a New conversation action in Active and History, even when collapsed. The computer action starts a fresh Active draft on that computer with folder selection available. Folder actions retain the exact computer, environment, and path; Standalone starts without a project folder using that environment's isolated runtime. Pick a compatible CLI using the usual remembered model/reasoning choices and focus the composer. Never inherit the old conversation's messages or archive state, parse the folder identity from a display name, or fall back to the local computer when the selected one is offline. Disable actions whose owning computer or environment cannot be identified.
- Relay connections use HTTPS except on loopback and never forward provider credentials. Windows saves only its own relay pairing in the current user's Windows Credential Manager, scoped to the app and installation, and reconnects automatically after startup or a temporary outage. Restored keys stay in native code and out of renderer storage, workspace files, exports, and logs. Explicit disconnect removes the saved pairing and wins over pending restoration. Other native platforms retain in-memory pairing until an OS-protected store is implemented. Preserve revision checks, deletion baselines, same-run reconciliation, and conflict copies; automatic restoration must not silently merge a replaced relay. In-flight remote work is not automatically replayed after disconnection or relay restart. The execution host must retain response checkpoints. A restarted execution host publishes its own unfinished replies as interrupted over the relay's stale running checkpoints while keeping the newest checkpoint content; restarts of viewers or other computers never cancel a run the host still reports.
- Relay polls every 2.5 seconds must not snapshot, merge, apply or save the whole workspace while nothing changed: that stalled text selection and typing for hundreds of milliseconds in every chat. The page's `revision` counts local shared changes (`persist()` and streamed run events, never data `apply` received). Once a poll leaves both sides equal (`sameShared`, which compares lists by id because this device adds new chats first), later polls only ask `GET /v1/state/revision`, or read the revision from the full state on older relays. Replies running here still sync every poll, and the checkpoint is saved only when it changes. Keep `save_workspace` and `save_sync_state` async native commands off the UI thread, which also handles window input. Routing, ownership, account-update and context-cache lookups read `runtime.fleet()`, a copy of the computer and account registry; `runtime.workspace()` copies every conversation (about 100 ms for a 2.4 MB workspace), which froze Connections for about a second. See docs/VERIFICATION.md.
- Follow docs/PERFORMANCE.md for anything on a hot path (relay polls and timers, effects, streamed events, navigation, typing, scrolling, selection). `src/lib/performance-guards.test.ts` fails on a whole-workspace copy (a zero-argument `.workspace()` call, or `$state.snapshot`, `JSON.stringify` or `structuredClone` of `workspace`) outside `allowedWorkspaceCopies`, on a synchronous `#[tauri::command]` outside `synchronousCommands`, and without the closed `<details>` rule. Declare new commands async and move file, process and network work into `spawn_blocking`; extend an allowlist only with its reason. `tests/performance.spec.ts` pairs a 2.4 MB workspace (`tests/large-workspace.ts`) with a real relay: idle polls must neither transfer `v1/state` nor save, collapsed history stays out of the page, and local runs enforce long-task budgets for idle, Connections and selection.
- Keep Vite from watching artifacts, test-results, and relay data. On Windows, use a separate Cargo target directory and Tauri identifier for native QA when a running app locks the normal debug executable; never terminate the user's active app to unblock a test build. Complete native builds before browser checks as well; concurrent compilation can exhaust the browser startup timeout.
- Pace arrows are bare 16px icons matching the app's usual stroke weight. Do not add borders, colored backgrounds, or badge containers around them.
- Display chats without a project folder as Standalone with a MessageCircle chat-bubble icon in sidebar groups and the selected toolbar value. Explain the absence of a project folder on hover, and label their plus action New standalone conversation. Preserve their existing location identities and execution behavior.
- Show Move to history in the chat toolbar only for active conversations. Omit the Restore conversation action for History chats; sending a valid message restores them automatically.
- Omit Refresh model list and Copy conversation as Markdown from the chat toolbar and mobile actions menu. Keep automatic model catalog loading, caching, and reconnection refresh.
- Conversation deletion belongs in the sidebar row context menu, never the chat header. Support right-click, Shift+F10/context-menu key, and touch long-press in Active and History. Target the clicked row without opening it or clearing another chat's draft, retain confirmation, and allow deletion of running conversations by cancelling the exact local or remote run and waiting for completion first. Keep the conversation if cancellation fails, and merge remote final checkpoints before deleting so late output cannot recreate it. Keep menu dismissal, focus return, and viewport bounds usable.
- Keep the reply timing row below the final answer and artifact cards, readable at 11px and wrapping on narrow windows. Its timing control shows only elapsed time and cumulative total AI time, beside the separate Files edited control. Format both, and native workflow durations, with tenths of a second under a minute (4.6s) and as whole seconds with minutes and, when needed, hours from one minute on (28m 31s, 1h 7m 32s). Keep tokens, the usage breakdown, and provider-reported estimated cost in USD inside its expansion. Missing timing and cost stay unknown, including older replies; never infer charges from subscription limits. Preserve reported cost through saved history and relay. Do not add a copy-response action to that row.
- Show cumulative total AI time through each reply alongside its own elapsed time. Derive it from recorded durations of this and earlier saved assistant replies in the same conversation, including stopped/failed replies. Exclude user messages and running replies; mark totals with missing timing as incomplete and do not add nested tool or child-agent time again.
- Configure provider, model, reasoning, and instructions per conversation; do not reintroduce the preset agent library. Remember models per provider and reasoning per provider/model. Preserve each reply's settings when later model, reasoning, or instruction choices change. Workspace v2 migrates v1 and retains the original desktop file as a backup on first save.
- Validate changes with `npm run verify`, `cargo fmt --manifest-path src-tauri/Cargo.toml --check`, `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`, and `cargo test --manifest-path src-tauri/Cargo.toml`.
- Native smoke testing uses `npm run tauri dev`. Mock browser tests do not prove a provider login or a native process works. Document real-provider checks and any provider still requiring sign-in.

- Claude workflows means Claude Code's native Dynamic Workflows feature, never an app-owned prompt sequencer. Let the selected CLI run its Workflow tool and load native project, personal-profile, and plugin scripts. Invoke workflows through ordinary chat, including saved /name commands. Do not show a separate Claude workflows button or launcher in the desktop toolbar or mobile actions menu. Stamp only current human input with origin.kind=human; inject prior context through shouldQuery=false without human provenance. Keep the owned CLI alive through background completion and later result messages, without an elapsed-time cutoff and with short relay heartbeat expiry. Decode bounded native task/workflow progress into revisioned run, phase, and agent metadata. Keep scripts, arbitrary arguments, agent prompts, and tool output out of progress cards. Save reported progress with replies; retain legacy sequencer data for history/export only and reject its execution. Saving/reuse uses Claude's native .claude/workflows or selected personal-profile files, never a second app library. Claude chats resume their exact CLI-owned session across replies; bootstrap legacy history once, keep bindings host-local, and fail explicitly on a missing or mismatched session. Do not claim terminal /workflows or live workflow pause/resume is available in the app. See docs/NATIVE-SESSIONS.md.
- Claude chats expose the always-loaded, read-only await_background_tasks SDK tool so a turn that ends while waiting for finite background work does not complete the reply. Accept only registered parent calls naming running background tasks that this reply's parent launched; child, foreground, finished and unknown tasks are refused. Keep the reply open while declared tasks run and until the CLI delivers each completed or failed outcome, as a replayed task-notification or the turn it starts after the result; end it if no turn starts within 15 seconds, and never wait for stopped tasks. Undeclared shells, servers and apps left for the user never hold a reply; background agents and workflows still do. Alerts, badges and queued sends follow the actual end, and multi-turn replies sum their token counts. Stop during a wait interrupts in-band and sends stop_task only for declared and delegated tasks; acknowledged stops end the reply as stopped and park the process, while a rejected stop releases it. Tasks from earlier replies cannot be awaited. See docs/NATIVE-SESSIONS.md.
- Settings offers workspace-wide Claude chat instructions, appended to the system prompt of every interactive Claude chat in any folder. The default asks Claude to run tests, builds and type checks in the foreground and to keep run_in_background for servers, watchers and apps left for the user. Store only changed text in the optional synced workspace field claudeInstructions (at most 4,000 characters; absent follows the default, empty turns it off), with explicit save, reset to default, stale-save protection and export. Send the effective non-empty text as a validated top-level run request field and pass it as one literal --append-system-prompt argument; keep it out of conversation history, per-conversation instructions, reply settings and replay. It participates in parked-process identity. Never apply it to Codex, Antigravity, or restricted background requests. See docs/NATIVE-SESSIONS.md.
- Claude and Codex chats keep one CLI process per conversation between replies. After a completed or cleanly interrupted reply the process stays parked on the executing host, and the next reply reuses it only when the provider, account connection and profile, computer and environment, folder, executable, reasoning, and native session all match, plus the model for Codex. Claude model and optional thinking-token budget changes use acknowledged set_model/set_max_thinking_tokens controls before the next prompt; resolve CLI default through the selected profile, keep effort separate, and fail without submitting on rejected or unconfirmed settings. A changed launch setting, an unhealthy process, or an exited process starts a fresh process that resumes the native session as before. Park at most four processes per host, release them after fifteen minutes idle, on conversation deletion, and on app close, and never park background title or usage runtimes. Stop sends an in-band interrupt (Claude control_request interrupt, Codex turn/interrupt), waits up to five seconds for the turn to end, and keeps the process; otherwise it kills the owned process tree, including WSL. Sending while a reply runs queues up to eight messages per conversation in session memory, listed above the composer with a return-to-composer control; after a completed reply the next queued message is sent automatically, while a stopped or failed reply returns queued text and images to the draft. Never persist, export, or relay queued messages. See docs/NATIVE-SESSIONS.md.
- Show a reply's provider plan, native workflow runs and background work as compact toggles in its footer row: after the live elapsed time while it runs, and after timing and Files edited once it ends, wrapping on narrow screens; never as panels above the composer or as separate cards. Each toggle opens its details in a panel below the row, one panel at a time, rendered on first opening and kept across the reply's end. While the reply runs, the Plan toggle names the step in progress when there is room. Keep completed Work history directly below the model title, and Retry last below token/time. Decode Codex plan notifications and successful Claude TodoWrite/Task tool results, preserving assigned task IDs, revision ordering, and unknown completion. Supply Codex's studio_update_plan dynamic tool for CLI/model combinations without a built-in planner; validate bounded parent-thread calls before acknowledging them and route them through the same plan decoder. Enable Claude todo tools only through the chat process's fixed --settings override. Never let child plans replace parent plans or infer completion from final text. Keep native workflow phases and agents separate from the provider plan.
- Completed HTML/SVG answer fences can open an artifact viewer in the existing modal or a right-side panel, with source, restart, and download actions. Keep chat usable beside the docked panel on wide screens and use an accessible right-side drawer on narrow screens. Its left divider supports pointer and keyboard resizing, remembers width locally, and clamps to preserve chat space. Artifact buttons match the height of adjacent collapsed progress cards; expanding progress must not create oversized artifact buttons. Switching presentation preserves the live preview and draft; changing conversations or leaving chat closes the viewer. Keep executable source outside the privileged document: use the constant studio-artifact native renderer or /artifact-preview browser route with independent CSP and sandbox allow-scripts without same-origin privileges. Accept no artifact-origin commands; never read arbitrary file paths to create previews. Preserve the original source in reply Markdown; cap preview source to 512 KB and 12 artifacts per reply. Keep ordinary Markdown sanitized and external resources blocked in artifact previews. Update relay Docker source copies when shared modules change.

Use Vinicius Portela <vinicius.portela.stm@gmail.com> for commits. Do not add coauthor trailers. Use `development` for ongoing changes and merge tested changes into `main` for releases, as requested on 2026-09-21; keep `main` as the stable/default branch. The user authorized automatic commits and pushes after completed Agent Studio work on 2026-09-09, including future sessions; this now targets `development`, with merges to `main` when a release is requested. Do not ask again unless later instructions narrow that authorization. Run the full applicable local pipeline on the final commit candidate before every push, and do not push if it fails or cannot run. Native packages for other operating systems are verified by the corresponding hosted runner. Never force-push without explicit authorization. See docs/DEVELOPMENT.md.
