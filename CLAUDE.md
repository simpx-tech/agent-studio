# Agent Studio

- A shared VPS hosts private workspaces selected only by authenticated pairing keys or sessions. Preserve the existing owner key/root data; put additional users' state, computers, jobs, presence, sessions, notifications, and badges in separate workspace contexts and directories. Never trust a requested workspace ID as authorization. Workspace administration in Connections requires a live server-private admin role. Enforce exactly one enabled admin workspace in the private version 2 registry; new workspaces start as members and creation cannot grant admin. Assigning Administrator to an existing enabled workspace atomically transfers the role and demotes the previous admin. Close its controls immediately and reject stale or concurrent former-admin mutations. Block direct demotion or disablement of the only admin. Upgrade version 1 under the registry lock, keeping the owner when already admin or otherwise the oldest enabled admin, demoting all others without changing keys, sessions, or chat files. Invalid version 2 roles fail closed. Ordinary chat/fleet/job APIs remain scoped even for the admin. Retain data on revocation. Show newly issued keys only transiently, never in workspace storage or logs; clear administration state on disconnect, scope changes, or permission loss. Keep the local CLI for role recovery with the same single-admin invariant. Browser cache and sync state require verified workspace identity; clear visible data on disconnect/change and reject stale-tab requests. Desktop pairing must reject another workspace before changing protected credentials or publishing local data; retain its binding across disconnect. See docs/PRIVATE-WORKSPACES.md.

- Codex and Claude chats expose an Agent Studio visualization tool through Codex dynamic tools and Claude's in-process SDK MCP protocol. Accept bounded parent tool submissions with id, title, and self-contained HTML; never load file paths from reply references or arbitrary tool output. Render accepted visuals between explanatory paragraphs using standalone top-level <!-- visualize:ID --> markers, with fallback below prose for older replies. Match chat typography and colors, use a transparent background with no outer card or repeated title bar, and size frames to content. Keep restart, source, download, modal, and side-panel access through quiet hover/focus controls visible on touch layouts. The sandbox can report only bounded height telemetry matched to its window and per-render token; accept no application commands. Bundle fonts as data without network privileges. Persist exact source and revisions in optional message visualizations through history/export/relay, retaining all visual IDs on checkpoints and replaying completed visual source only as earlier context. Keep source out of activity metadata and title requests. Claude stdin must remain open for SDK tool responses and delegated workflow completion, then close after a result with no delegated work pending; background shell servers must not keep it open. Preserve selected CLI/profile/cwd, other configured integrations, and owned-process cancellation. Visualization source is limited to 512 KB each, 12 and 2 MB total per reply, and 8 MB of source in request history. External resources and host interaction APIs remain unavailable.

Tauri 2 desktop app with Svelte 5, TypeScript, and a static SvelteKit frontend.

- End each completed, stopped, or failed assistant response with an expanded-by-default Files edited summary. Keep individual code diffs collapsed until opened. Label newly created files Added and existing files Edited. Added files omit per-file line-change counts and hunk coordinate headers, and are excluded from the summary line-change totals; retain their expandable source and unavailable-source notices; use project-relative paths or a shared recorded directory when clear, keep full paths on hover, and contain each file in a subtle theme-colored border. Its This response view shows that reply's recorded edits; All chat changes composes the latest recorded file state across the entire chat, including later replies, with reversions omitted. Expand individual files for inert, line-numbered code diffs and additions/deletions. Record only successful native Codex file-change and Claude file-edit results in bounded revisioned message data, separate from activity and prompt replay. Preserve them through save/export/relay, rejecting stale or mismatched run snapshots. Never read paths or infer changes from shell output or prose. Missing, private, oversized, malformed, or discontinuous diffs stay explicitly unavailable; unrecorded history is not zero changes. Keep draft, disclosure, keyboard, and narrow-screen behavior intact.

- New Standalone chats use a persistent per-conversation working folder on their selected execution host, including managed WSL. Preserve it across replies, retries, and restarts; keep account/profile settings, tools, and global hooks intact. Existing chats retain their original shared runtime and native session bindings. Store validated UUID-based working-folder bindings only in host-local app data, never accept arbitrary runtime paths or sync their files. Context and skill inspection must use the same bound folder and cache Standalone inventories per conversation. Retain files on chat deletion and keep restricted background runtimes separate. See docs/CAPABILITIES.md.

- Suppress native lifecycle cards and the chime only for the chat currently viewed in a visible, focused chat window. Consume suppressed events without replay on blur, and keep other-chat, hidden/unfocused, Connections, and manual test alerts. Report revisioned, per-window foreground chat leases through transport to the authenticated relay; suppress that chat's workspace PWA pushes before enqueue and retry delivery, while keeping every received worker push visible. Keep leases transient, bounded, session-aware, and short-lived; never change pending badge counts based on reading.

- Pending chat badges count all Active conversations without a running assistant reply, including completed, failed, stopped, and saved idle chats. Opening/reading a chat, selecting History, or searching must not change that total. Keep the sidebar badge separate from Active/History totals, hide/clear it at zero, and route app-icon updates through transport. Windows uses an application-owned numbered taskbar overlay (99+ above 99); macOS/Linux use native badge counts, and supported PWAs use the Badging API. Mobile lifecycle pushes carry the current relay count at delivery/retry; never increment badges per alert or send extra visible alerts just to change a badge. Notification cards and the custom chime must still trigger when another chat finishes while the app is focused. See docs/NOTIFICATIONS.md.

- Native desktop notifications and the custom chime default to enabled per installation; preserve saved Disable and Mute choices in Connections, independently of mobile push and relay pairing. Request macOS authorization before the first alert and respect the OS decision. Use fixed generic lifecycle alerts, device-local settings/receipts, and the bundled Agent Studio chime through native audio. Silence the OS notification sound (WinRT silent audio, Linux suppress-sound, macOS nil sound), with a mute and test control. Do not ring on startup history, duplicate checkpoints, background queries, or child questions. Route UI through transport, preserve drafts on clicks, keep errors separate from run/storage success, and retain current app-close behavior. See docs/NOTIFICATIONS.md.

- Mobile push notifications are opt-in per paired browser under Connections. Send generic alerts for completed, failed, stopped, and explicit parent question-tool updates through the relay, including desktop-started chats; normal prose questions are covered by reply completion. Keep notification content free of chat text/titles. Persist VAPID keys, session-bound subscriptions, deduplication receipts, and bounded retry queues only in private relay data. Validate push-service endpoints, revoke with browser sessions and pairing-key rotation, deduplicate workspace/job updates by run, and never replay imported history. Every worker push displays a visible alert; clicks open the matching chat after sync and preserve an existing draft. Desktop Tauri never registers a service worker. See docs/MOBILE.md.

- The relay server also hosts the installable mobile PWA at its origin root. Browser clients are controllers: route all execution, folder, model, context, and usage requests through the selected host using transport; never register a browser as a CLI computer or claim worker jobs there. Browser pairing uses the same-origin HttpOnly session endpoint, never persistent pairing keys. Cache only the public app shell, never relay APIs or commands; do not replay offline work. Keep the mobile conversation drawer, touch controls, safe areas, and keyboard-height behavior usable without hiding history or settings. Build the frontend before server/PWA tests and serialize them with native builds. See docs/MOBILE.md.

- Read this file before changes. Keep UI components independent of desktop IPC; use `src/lib/transport.ts`.
- Follow incoming chat updates only while the reader is at the bottom. Scrolling up must preserve the reading position, including small wheel or touch movements near the end; returning to the bottom resumes following. Do not animate automatic scrolling on every streamed event. Keep the full conversation and expanded activity reachable above the composer.
- Codex and Claude question tools display bounded parent questions inline, with single/multiple choices, free text, explicit submission and skip. Use native Codex requestUserInput, Claude AskUserQuestion, and the studio_ask_user dynamic/SDK tool. Keep answer transport pinned to the originating run, connection and request; never submit defaults, restart a reply, or turn answers into shell text. Preserve the composer draft and show recoverable delivery errors. Reject child, stale, duplicate, malformed and secret-input requests. Persist revisioned questions and submitted answers through history/export/relay, replaying only answered context. Close pending questions on stop, completion, expiry or process loss; startup history cannot regain a live answer channel. Keep the provider pipe open while questions wait, allow Claude/Codex runs and user-input waits without an elapsed-time cutoff, retain cancellation, and notify once per question without including its text. Antigravity's current print interface has no supported interactive question response protocol. See docs/CAPABILITIES.md.
- Multi-question calls show one question at a time with a progress counter, numbered navigation, and Back/Next controls. Allow browsing unanswered questions and revisiting answered ones before submission; retain choices and text between steps and relay updates. Require complete valid answers only when sending from the final step. Each question independently uses radios for one answer or checkboxes for multiple answers. Skip questions applies to the whole call. Hide submitted question forms without a Your answers card; retain their data in saved history, exports, and provider context. Single-question calls retain direct submission.
- The composer Templates library stores reusable plain-text prompts with named `{{field name}}` inputs in optional workspace v3 `inputTemplates`. Support create, edit, confirmed deletion, repeated fields, multiline values, and a message preview. Insert into message appends to the existing draft and focuses it; never submit, start a run, restore a History chat, or change attachments/settings on insertion. Keep fill values temporary, preserve draft text on cancel/failure, cap the combined message at 30,000 characters, and close the dialog on conversation/workspace changes or leaving chat. Sync/export definitions within the authenticated workspace, retain divergent edits as conflict copies, and reject stale editor saves. See docs/CAPABILITIES.md.
- The composer offers leading `/` commands and skills through the shared ChoicePicker, with arrows, Enter/Tab completion, Escape dismissal, and touch selection. App shortcuts open the existing controls and preserve remaining draft text. Discover Claude commands from selected-profile initialization and Codex skills from its enabled CLI catalog through the existing context transport/cache. Keep catalog metadata session-only, scoped to connection/model/computer/environment/folder; skip unavailable connections and ignore late selections. Claude commands retain literal arguments on stdin. Save Codex skill name/path metadata with user messages and send only the current invocation as a native skill input; never replay earlier skill inputs or treat selection as a confirmed invocation. Unknown, disabled, unavailable, or unsupported persistent-session commands keep the draft with an explanation; do not silently send them as prose. See docs/CAPABILITIES.md.
- Keep healthy PWA connections quiet: omit the persistent connected banner and its empty row in installed apps. Retain setup/offline/server-error notices, installation access in browser tabs, and detailed sync status in Connections.
- Label the web/PWA logo and installed app as Agent Studio Viewer; retain the native Agent Studio name. Install app opens a chooser with Viewer (PWA) and, on desktop browsers, a clearly labelled Windows desktop installer when available. Phones/tablets only offer Viewer. Keep unsupported-browser guidance and one-shot install prompts across login. Publish installers only through the relay's fixed download allowlist in a dedicated public directory, outside the frontend shell cache and private workspace data; do not offer missing packages or imply Windows packages run natively on other operating systems.
- A paired PWA is connected to the relay, independently of whether an execution computer is online. Show offline computers in the chat picker, selected computer label, and sidebar, using the owning host's heartbeat for both Desktop and its managed WSL distributions. Skip model/usage queries to unavailable remote connections, retain saved choices, and refresh models when the selected connection returns. Treat the relay's `host_offline` rejection as availability state; retain warnings for actual model-query failures on an available connection.
- PWA pairing must survive app updates and relay restarts. Persist only keyed browser-session ID digests in the relay data directory, outside public files and workspace exports; retain the data directory and pairing key across releases. Renew active sessions with their HttpOnly/SameSite/Secure protections, persist explicit disconnects, and invalidate sessions on pairing-key rotation. Retry temporary startup outages without requiring another pairing or replaying commands.
- Always use the design-system `ChoicePicker.svelte` for dropdowns, including forms and management pages. Do not add native HTML `select` elements. Use its `field` variant for full-width form controls and retain keyboard navigation and accessible labels.
- New-chat Agent choices belong to the selected computer, including before folder selection. Exclude connections confirmed unavailable even when their provider was previously selected; leave the options empty when no CLI is available and show the muted placeholder No agents. Show Select agent when choices exist but none is selected, never a literally blank trigger or another option presented as selected. Existing conversations retain their fixed agent label.
- Desktop opens directly in chat. Web/PWA opens on workspace sign-in and keeps the chat shell, Connections, and dialogs unmounted until the server verifies the session and workspace. Sign in with the private workspace key on the current origin; restore valid sessions automatically and retry temporary startup outages. Sign out, session expiry/revocation, and cross-tab workspace changes return to login and clear drafts/dialogs. Retain Install app before sign-in. Center the new-conversation welcome vertically in the space between the toolbar and composer, keep it free of suggestion cards and omit the CLI login/execution subtitle from the composer. Keep the sidebar compact: a small Agent Studio wordmark without a tagline, conversation search and Active/History groups, and the Plug button for Connections in the footer. The entire logo, including its mark and text, is a window drag region. Keep the small conversation and folder plus actions and Ctrl+N. Do not reintroduce Overview, a Workflows navigation page, the large New conversation button, or the Personal workspace footer.
- Keep agent setup, computers, accounts, and relay management together in the Connections view, retaining the Plug icon. Always use the shared dropdown component. Opening Connections and returning through the logo or Plug button must preserve the open chat and draft.
- Organize Connections by computer. Each computer contains its environments, provider CLIs, and account connections; filter connections to that computer. Shared account labels may appear on each computer where they are connected, but never mix another computer's connections into its card. Keep naming, account setup, and relay pairing in contextual dialogs. Explain that existing CLI login shares the environment's login/settings while a separate profile uses the same CLI with independent login/settings; relay sync never copies credentials.
- Keep computer cards concise: no nested environment cards (Here/Managed here), repeated environment/Connected rows, or CLI setup accordions. Show installation state as Installed/Not installed/unknown without repeating the computer name, and expose Open sign-in directly beside account actions. Account naming and editing have no Personal/Work purpose controls or badges; retain legacy stored purpose metadata when editing old accounts.
- Provider cards use a compact header with installation status, a single-line CLI path with the full path on hover, and account rows with Chat and Open sign-in. The account settings icon opens a contextual Manage account dialog; never expand rename/disconnect forms inside the card. Put profile information and confirmed disconnection in that dialog. Offer connection on the current computer only when the account has no connection there, and name the target computer explicitly.
- Stack provider CLIs vertically within each computer in Connections. Show each connection's account usage beside its actions on wide windows and below them on narrow windows: 5-hour and Weekly used percentages, resets, pace, and remaining budget. Keep additional reported model-specific limits explicitly labelled with their model. Account usage has no chat-context meter. Query through transport using the exact connection, including remote/WSL connections; skip missing, signed-out, and offline connections. Refresh on opening, focus, the page refresh action, and every minute while visible. Retain last readings on failure/offline, suppress their pace guides, distinguish unknown from zero, and show loading/checked state. Keep snapshots in session memory. Queue native usage reads behind the bounded process limit instead of rejecting accounts when several refresh together.
- Detect installed CLIs and their existing login connections automatically on each locally managed computer. Add account is exclusively for additional Claude/Codex logins: show Provider and Account name, with the selected computer fixed. Do not show Account, Environment, or Login profile dropdowns or explanatory existing-login paragraphs in this modal. Always create an isolated profile, show Creating account and Opening sign-in progress, save it before launching sign-in automatically, and refresh in the background. Keep launch failures visible and retry the same saved connection rather than creating duplicates. Track sign-in completion by connection, never by another profile's ready status. Gemini remains existing-login only.
- Windows sign-in starts PowerShell in its own console without inheriting the GUI's standard handles, verifies a readiness signal, and retains the console after command failure. A successful process spawn alone does not prove the terminal opened. Keep CLI authentication local and leave existing logins untouched.
- Present each Windows-managed WSL distribution as a separate computer in Connections, the chat computer picker, and conversation groups. Keep persisted physical host, environment, connection, and folder identities intact. Show installation inventory from that environment's own executable lookup, independently of login status; reject Windows PATH shims as Linux installations and show failed checks as unknown or stale. Missing CLIs require installation before account connection or sign-in is available.
- The Windows app manages WSL CLIs directly; no second Agent Studio app or relay pairing inside WSL is required. Inventory uses read-only listing commands. The selected computer determines the CLI and account for both existing logins and separate profiles: Desktop uses only its Windows CLIs, including for WSL folders; a WSL computer uses only that distribution's Linux CLIs. Never fall back to another computer, distribution, or account when a CLI is missing or fails. Checking or executing a WSL CLI can start that distribution.
- WSL launches pass arguments as data to fixed scripts, keep prompts on stdin, use Linux-local profile/runtime directories, and cancel the owned Linux process group as well as wsl.exe. Never terminate a whole distribution to stop a chat. Do not read or copy CLI credentials across environments.
- CLI adapters belong in `src-tauri/src/providers.rs`; translate provider JSONL into the typed events in `protocol.rs`. Never render raw CLI output as HTML or emulate a terminal.
- Require verified sign-in for the selected connection before sending. Show a composer warning and Open Connections action for signed-out or unverified accounts, preserving the draft and History state on blocked submissions. Refresh authentication after explicit sign-in. Skip automatic model queries until that connection is ready; catalog requests must only launch the selected provider. Gemini chat and model listing must first pass the headless account query, and authentication-required output must terminate the owned process instead of waiting for interactive login.
- Authentication belongs to the installed CLIs. Never read, copy, or persist their tokens. Prompts go through stdin; do not interpolate user input into shell commands.
- Usage readers use read-only CLI queries. Missing limits are unknown, not zero; preserve stale readings on failures. Claude Fable limits come from server-labelled model scopes and appear only for Fable. The main context meter uses the latest compatible reply's provider-reported request count (`contextInput`), never turn totals or draft estimates. Unmeasured context stays unknown. Serialize frontend builds and browser checks against the reused Vite server, since build-generated files can reload test pages.
- Model context inspection is a separate source and MCP inventory scoped to the selected model, connection, execution environment, and folder. Route it through transport to the owning host and use only the selected computer's CLI and profile. Distinguish CLI-reported metadata from discovered, disabled, overridden, and companion guidance; never claim a startup query is a previous reply's read history. Memories include global entrypoints and current-project/folder sources only, excluding unrelated projects and unscoped archives; Claude's effective memory report replaces guessed directories. Show MCP names and allowlisted status/scope metadata from the selected CLI, distinguishing configured from connected and retaining disabled/unavailable states. Never return configuration bodies, credentials, memory contents, MCP commands/URLs/environment, tool schemas, or raw errors, and never invoke tools during inspection. Keep inspection out of persisted conversations. Use stable selection values and untracked transport calls in Svelte effects so relay polling cannot restart inspection.
- Keep the Model context dialog height stable across loading, cached refreshes, results, filters, and errors. Cap it to the available viewport with no scrollbar on the outer frame. Let the source list fill the remaining space and scroll internally; narrow/short screens use a single scrollable body. Keep the header and footer fixed and retain reachable category tabs and closing controls.
- Reopening Model context shows its latest cached inventory immediately while refreshing. Keep cache keys specific to provider, model, connection, computer, environment, and folder; share pending requests for the same selection. Preserve the last result and its timestamp on refresh failure. Cache only bounded session metadata, with no persistence into conversation storage.
- Model context's Native prompt tab is an explicit exception to metadata-only source inspection: read bounded native instruction text on demand from the saved conversation's host-local binding and selected CLI profile. Show Claude's latest parent prompt snapshot and Codex's recorded base instructions and unique developer messages with capture/version metadata; label historical records and current Agent Studio additions accurately. Never accept native session IDs or paths from the caller, reconstruct missing prompts, launch a model, or expose user messages, tool results, credentials, configuration bodies, or tool schemas. Keep prompt text out of source/skill caches, workspace persistence, exports, checkpoints, and logs; remote inspection uses transient authenticated workspace jobs. Clear prompt text on close, selection changes, and read failures. See docs/NATIVE-INSTRUCTIONS.md.
- All Claude, Codex, and Gemini/Antigravity conversations enable tools and load the selected CLI profile's configured integrations, skills, and project guidance. Always launch chat agents with full access and without approval prompts: Claude and Antigravity use --dangerously-skip-permissions; Codex app-server threads use approvalPolicy=never and sandbox=danger-full-access. Antigravity uses accept-edits mode and its default agent; remove only the exact legacy app-generated deny hook from the chat runtime. The selected folder is the CLI working directory. Background title generation and usage queries remain restricted in their separate runtimes. Keep the Agent picker hover description accurate about tool access.
- Codex chat uses persistent app-server threads bound locally to the conversation, account, environment, and folder, so native history survives replies and restarts while sub-agent lifecycle, child results, and tool events reach the UI; restricted titles still use exec. Keep the validated process working directory, profile isolation, stdin requests, and owned process-tree cancellation. Tool activity is bounded, typed metadata stored additively in activity blocks and updated by identity/revision through local runs, relay delivery, and checkpoints. Keep child text/usage out of the main reply. Distinguish available skills from actual skill invocations or observed file reads; adding a skill to the composer does not claim it loaded. Never expose raw command output, arbitrary tool arguments, or skill/configuration bodies in activity cards. Show unconfirmed outcomes after missing completion instead of inventing success.
- Show progress text and tool activity inline while a reply runs. In live activity and expanded history, group consecutive tool calls into collapsed, quiet rows with generic action labels, keeping progress comments in order between groups. Expand a group for individual calls and a call for its recorded details. Preserve user expansion during streamed revisions and appended calls. Derive labels from reported action metadata and keep failed, stopped, and unconfirmed outcomes visible on collapsed groups, including child work. On completion, show a collapsed Work history disclosure directly below the model title and above the final answer, with a divider below expanded history. Keep its label concise: Work history, with no activity counts or category filters. Expand it to review all progress comments and tool calls in their recorded order. Keep intermediate progress separate from the final answer and prompt replay, and retain it through saved history and relay updates. Tool headers must identify their file, pattern, query, or description when reported; details show allowlisted targets, line ranges, match counts/paths, returned tool names, and command exit metadata. Missing metadata gets an explicit explanation, never an empty disclosure or fabricated target.
- Quota pace compares usage with an even-use guide at the reading's timestamp; stale, failed, missing, or expired readings have no pace classification. Context growth uses consecutive compatible measured request counts, stops at gaps/model changes/decreases, and never treats draft forecasts as measured usage.
- Keep quota comparison colors consistent in Connections and chat: below the guide, show a muted green secondary bar behind the current fill; above the guide, render the guide in orange above the overlapping fill and show the excess in red. Use the two colored bar segments without an endpoint marker. Preserve the shared percentage scale and remove the guide when stale or unavailable. Omit Codex's unreported 5-hour window, including placeholder rows, while retaining any actually reported 5-hour limits such as Spark's separate quota.
- Keep usage UI concise: show current numbers, resets, budgets, and actionable warnings. Do not show a next-request estimate or Details section. Use colored trend arrows with hover descriptions and accessible names for limits: On track is blue/right, Below pace green/downward trend, Ahead of pace amber/upward trend, and exhausted limits red/upward trend. Unknown pace uses a gray dash. Quota bars use the green/orange/red comparison colors when a guide is present; otherwise their fill matches the indicator. Context shows its value without a Reported label or arrow; retain its hover guidance and expanded warnings.
- Place Usage and context directly below the message input, replacing the Tools enabled caption. Show compact groups for Context, 5-hour, and Weekly, with unknown readings visibly distinct from zero; omit an unreported Codex 5-hour window and let the remaining groups fill the row. Clicking a bar opens the reset/budget/guidance panel above the input; retain Fable's additional scoped limit there. Keep automatic refresh and keyboard expansion/closing, but no refresh or gauge buttons. Vertically center the panel title and Checked text with Close. Overlay the current quota fill and a secondary recommendation on the same track and percentage scale, keeping both colored segments visible when current usage passes the guide. Do not show a Recommended label or separate guide row; retain the comparison in hover and accessible text. Use the existing timestamp-based even-use guide; hide it when stale, failed, expired, or unknown, and do not invent a timed context recommendation. Keep tool-access information in the Agent picker hover description. Do not restore the usage strip above the chat.
- Render language-labelled Markdown code fences with bundled syntax highlighting in replies and progress, including streaming and restored history. Use the same colors in the HTML/SVG artifact Source tab, preserving exact source text and downloads in modal and panel views. Keep inline code, unknown/unlabelled languages, and oversized blocks readable as escaped plain code. Sanitize after highlighting and keep HTML snippets inert in chat and source views.
- Persist portable agent and conversation data through the versioned schema. Surface storage failures. Keep credentials out of exports and tests.
- Chat image attachments support file selection, paste, and drops anywhere in the chat area for Codex and Claude, with previews, removal, and image-only sends. Show a drop hint, clear it on exit/drop/cancellation, preserve the draft, and focus the composer after a drop. Handle file drops once; prevent dropped files from navigating the app outside chat, and retain ordinary text drops. Preserve original PNG/JPEG/WebP bytes in optional workspace v3 user-message images, including exports, relay checkpoints, retries, and later turns. Limit attachments to four per message, 2 MiB each, and 8 MiB per conversation; retain the 20 MB workspace limit and reject oversized drafts before sending. Send Codex image data URLs and Claude base64 image blocks as structured stdin data, never paths or text-encoded image prompts. Keep image bytes out of title generation, text token estimates, and tool activity. Gemini's Antigravity stream input is text-only; explain the unavailable attachment action. Disable Tauri native drag interception so HTML file drops reach the chat.
- Workspace v3 separates computers, environments, account labels, and CLI connections. Installation identity and sync checkpoints are local. Never route a missing/offline connection to a different account; profile scope must cover detection, sign-in, models, usage, titles, and chat.
- New conversations require an execution computer; the project folder is optional. Offer Standalone in the Folder picker to clear a draft folder without changing its text, attachments, agent, model, or reasoning. Computer-scoped drafts default to Standalone, and Agent and Model are usable without selecting a project folder. Retain the selected computer and environment in the standalone location identity and omit the empty project path from provider requests. Persist the folder environment and, when different, the execution environment; keep both and the account connection stable. Desktop may browse its Windows folders and its managed WSL folders; a WSL computer browses its own distribution. Browse only directory names through transport on the owning host. Windows CLIs access a selected WSL folder through its translated Windows/UNC path; a selected WSL CLI uses the original Linux path. Fail if the same folder is unavailable or cannot be represented on the selected computer; never silently substitute the app runtime or another computer. Apply the provider-specific chat tool access policy. Generate application-owned agent definitions/hooks only in app data, never in the selected project. Legacy chats without a folder and title generation retain the isolated runtime. Keep Active and History sections grouped by execution computer then folder. On app startup, move all saved chats to History without changing their messages, settings, or recency. History chats allow typing and next-reply settings immediately; sending a valid message automatically restores the same chat to Active. Opening, typing, empty submissions, and blocked sends must not restore it. Preserve location/archive metadata in storage and relay conflict handling, including one-sided archive changes concurrent with response checkpoints.
- Keep Computer, Folder, Agent, Model, and Reasoning inline in one chat toolbar with decorative laptop, folder, bot, chip, and brain icons inside their dropdown triggers. Multiple account connections belong in the Agent picker; do not add a separate Run on row. Show the full folder path only as a hover title in the toolbar and folder choices. The top bar shows only the current page/conversation title, with no Workspace breadcrumb, Local desktop badge, or version text.
- Reselecting the current computer or folder is a no-op and preserves the draft. Apply validated folders without awaiting CLI detection or relay sync. Reuse connection availability within an environment and cache model catalogs per provider/connection; keep Model usable during refresh. Unknown local CLIs may be configured while checking, but sending still requires detected availability. Late checks must never replace newer folder, conversation, or explicit agent/model/reasoning choices.
- Computer, Folder, and Agent (including its account connection) are fixed once a conversation exists, starting with its first submitted message. Lock their controls and mutation handlers for existing, restored, and legacy conversations. A new blank conversation can choose all three. Model and Reasoning remain editable while a reply runs and apply to the next submitted message, with a small pending hint and a switch notice before the affected reply. Instructions remain editable between replies. Preserve in-flight and historical reply settings, including during relay checkpoints; concurrent deliberate settings edits still require conflict copies.
- Reply headings show the captured or provider-reported model name, rather than the provider or a former preset name. Keep the CLI/account/computer execution subtitle hidden. Save the model display name with each reply so catalog or conversation changes cannot relabel history; an unreported default remains explicitly unknown as CLI default.
- The conversation sidebar uses Active and History tabs with counts, then muted computer headings, collapsible folder groups indented one level under their computer (including Standalone), and readable conversation rows. Its right divider supports dragging and keyboard resizing, remembers the width locally, and leaves room for the chat area when the window shrinks. Avoid stacked status accordions, repeated conversation icons, or tree guide lines. Keep tab keyboard navigation, folder hover paths, running indicators, and current-conversation semantics. Archive/restore reveals the destination tab and folder; switching tabs does not change the open conversation.
- Each sidebar computer and folder, including Standalone, has a New conversation action in Active and History, even when collapsed. The computer action starts a fresh Active draft on that computer with folder selection available. Folder actions retain the exact computer, environment, and path; Standalone starts without a project folder using that environment's isolated runtime. Pick a compatible CLI using the usual remembered model/reasoning choices and focus the composer. Never inherit the old conversation's messages or archive state, parse the folder identity from a display name, or fall back to the local computer when the selected one is offline. Disable actions whose owning computer or environment cannot be identified.
- Relay connections use HTTPS except on loopback and never forward provider credentials. Windows saves only its own relay pairing in the current user's Windows Credential Manager, scoped to the app and installation, and reconnects automatically after startup or a temporary outage. Restored keys stay in native code and out of renderer storage, workspace files, exports, and logs. Explicit disconnect removes the saved pairing and wins over pending restoration. Other native platforms retain in-memory pairing until an OS-protected store is implemented. Preserve revision checks, deletion baselines, same-run reconciliation, and conflict copies; automatic restoration must not silently merge a replaced relay. In-flight remote work is not automatically replayed after disconnection or relay restart. The execution host must retain response checkpoints.
- Keep Vite from watching artifacts, test-results, and relay data. On Windows, use a separate Cargo target directory and Tauri identifier for native QA when a running app locks the normal debug executable; never terminate the user's active app to unblock a test build. Complete native builds before browser checks as well; concurrent compilation can exhaust the browser startup timeout.
- Pace arrows are bare 16px icons matching the app's usual stroke weight. Do not add borders, colored backgrounds, or badge containers around them.
- Display chats without a project folder as Standalone with a MessageCircle chat-bubble icon in sidebar groups and the selected toolbar value. Explain the absence of a project folder on hover, and label their plus action New standalone conversation. Preserve their existing location identities and execution behavior.
- Show Move to history in the chat toolbar only for active conversations. Omit the Restore conversation action for History chats; sending a valid message restores them automatically.
- Omit Refresh model list and Copy conversation as Markdown from the chat toolbar and mobile actions menu. Keep automatic model catalog loading, caching, and reconnection refresh.
- Conversation deletion belongs in the sidebar row context menu, never the chat header. Support right-click, Shift+F10/context-menu key, and touch long-press in Active and History. Target the clicked row without opening it or clearing another chat's draft, retain confirmation, and allow deletion of running conversations by cancelling the exact local or remote run and waiting for completion first. Keep the conversation if cancellation fails, and merge remote final checkpoints before deleting so late output cannot recreate it. Keep menu dismissal, focus return, and viewport bounds usable.
- Keep the reply timing row below the final answer and artifact/progress cards, readable at 11px and wrapping on narrow windows. Its collapsed summary shows only elapsed time and cumulative total AI time. Keep tokens, the usage breakdown, and provider-reported estimated cost in USD inside its expansion. Missing timing and cost stay unknown, including older replies; never infer charges from subscription limits. Preserve reported cost through saved history and relay. Do not add a copy-response action to that row.
- Show cumulative total AI time through each reply alongside its own elapsed time. Derive it from recorded durations of this and earlier saved assistant replies in the same conversation, including stopped/failed replies. Exclude user messages and running replies; mark totals with missing timing as incomplete and do not add nested tool or child-agent time again.
- Configure provider, model, reasoning, and instructions per conversation; do not reintroduce the preset agent library. Remember models per provider and reasoning per provider/model. Preserve each reply's settings when later model, reasoning, or instruction choices change. Workspace v2 migrates v1 and retains the original desktop file as a backup on first save.
- Validate changes with `npm run verify`, `cargo fmt --manifest-path src-tauri/Cargo.toml --check`, `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`, and `cargo test --manifest-path src-tauri/Cargo.toml`.
- Native smoke testing uses `npm run tauri dev`. Mock browser tests do not prove a provider login or a native process works. Document real-provider checks and any provider still requiring sign-in.

- Claude workflows means Claude Code's native Dynamic Workflows feature, never an app-owned prompt sequencer. Let the selected CLI run its Workflow tool and load native project, personal-profile, and plugin scripts. Invoke workflows through ordinary chat, including saved /name commands. Do not show a separate Claude workflows button or launcher in the desktop toolbar or mobile actions menu. Stamp only current human input with origin.kind=human; inject prior context through shouldQuery=false without human provenance. Keep the owned CLI alive through background completion and later result messages, without an elapsed-time cutoff and with short relay heartbeat expiry. Decode bounded native task/workflow progress into revisioned run, phase, and agent metadata. Keep scripts, arbitrary arguments, agent prompts, and tool output out of progress cards. Save reported progress with replies; retain legacy sequencer data for history/export only and reject its execution. Saving/reuse uses Claude's native .claude/workflows or selected personal-profile files, never a second app library. Claude chats resume their exact CLI-owned session across replies; bootstrap legacy history once, keep bindings host-local, and fail explicitly on a missing or mismatched session. Do not claim terminal /workflows or live workflow pause/resume is available in the app. See docs/NATIVE-SESSIONS.md.
- Show provider plans above the composer while running. After a reply ends, place its collapsed plan/TODO and native workflow cards beside artifact cards above the token/time row, wrapping on narrow screens. Keep completed Work history directly below the model title, and Retry last below token/time. Decode Codex plan notifications and successful Claude TodoWrite/Task tool results, preserving assigned task IDs, revision ordering, and unknown completion. Supply Codex's studio_update_plan dynamic tool for CLI/model combinations without a built-in planner; validate bounded parent-thread calls before acknowledging them and route them through the same plan decoder. Enable Claude todo tools only through the chat process's fixed --settings override. Never let child plans replace parent plans or infer completion from final text. Keep native workflow phases and agents separate from the provider plan.
- Completed HTML/SVG answer fences can open an artifact viewer in the existing modal or a right-side panel, with source, restart, and download actions. Keep chat usable beside the docked panel on wide screens and use an accessible right-side drawer on narrow screens. Its left divider supports pointer and keyboard resizing, remembers width locally, and clamps to preserve chat space. Artifact buttons match the height of adjacent collapsed progress cards; expanding progress must not create oversized artifact buttons. Switching presentation preserves the live preview and draft; changing conversations or leaving chat closes the viewer. Keep executable source outside the privileged document: use the constant studio-artifact native renderer or /artifact-preview browser route with independent CSP and sandbox allow-scripts without same-origin privileges. Accept no artifact-origin commands; never read arbitrary file paths to create previews. Preserve the original source in reply Markdown; cap preview source to 512 KB and 12 artifacts per reply. Keep ordinary Markdown sanitized and external resources blocked in artifact previews. Update relay Docker source copies when shared modules change.

Use Vinicius Portela <vinicius.portela.stm@gmail.com> for commits. Do not add coauthor trailers. The user authorized automatic commits and pushes after completed Agent Studio work on 2026-09-09, including future sessions; do not ask again unless later instructions narrow that authorization. Run the full applicable local pipeline on the final commit candidate before every push, and do not push if it fails or cannot run. Never force-push without explicit authorization.
