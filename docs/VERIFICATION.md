# Verification — 2026-09-08

## Stable Model context height — 2026-09-11

The content-sized inspector grew by 119 px and moved upward by 59.5 px when asynchronous profile details and the footer arrived. Its frame now uses a consistent height capped at 800 px and the available viewport, with a stable scrollbar gutter. Content scrolls inside the dialog. Compact category styling also prevents horizontal overflow at 320 px; the inventory cache and query behavior are unchanged.

Rendered browser checks measured zero height/position shift across loading/results, cached refresh, empty filters, and refresh errors at 1380×900, 880×720, 390×844, and 320×460. The dialog remained inside the viewport, with no horizontal overflow or renderer errors; Done remained reachable by scrolling. Screenshots were visually reviewed, and the narrow-width finding was recorded in local QA notes. Evidence: `artifacts/model-context-layout.mjs`, `artifacts/model-context-layout-result.json`, and `artifacts/model-context-{loading,loaded,880x720,390x844,320x460}.png`.

The full gate passed `npm run verify` (zero diagnostics, 80 unit/HTTP tests, production build, 71 browser scenarios), Rust formatting, Clippy with warnings denied, and 65 Rust tests with four existing opt-in integrations ignored. Logs: `artifacts/model-context-height-{verify,rust}.log`. No real provider or native process smoke was repeated for this CSS-only change.

## Active deletion and authentication before sending — 2026-09-11

Active conversations can now be deleted during a response. Confirmation cancels the exact local or remote run and waits for completion before removal; cancellation failures retain the conversation and allow retry. Local storage failures also retain the chat. Remote deletion merges the latest checkpoint, and three-way sync lets deletion win over delayed reply/generated-title updates while preserving deliberate edits as conflict copies. This supersedes the earlier running-conversation restriction below.

Send and Retry require verified authentication for the selected connection. Signed-out or unknown accounts show a warning with Open Connections; blocked submissions preserve drafts, images, and History state. Automatic model queries wait for ready authentication, and native catalogs launch only the requested provider. Gemini performs the existing headless account query before chat or model listing and terminates authentication-required output promptly. [Antigravity's headless documentation](https://antigravity.google/docs/cli/headless/) describes cached-login requirements for noninteractive runs.

The final gate passed `npm run verify` (zero diagnostics, 80 unit/HTTP tests, production build, 71 browser scenarios), Rust formatting, Clippy with warnings denied, and 65 Rust tests with four pre-existing opt-in integrations ignored. Coverage includes active/current/other-chat deletion, cancellation failure/retry, late events and sync checkpoints, signed-out/unknown submission and retry guards, account recovery, retained images/drafts/History, cold detection, and mobile warning bounds. Three older scenarios were updated to wait for authentication before model requests or retry. Logs: `artifacts/active-auth-{verify,rust}-final.log`.

An isolated `npm run tauri dev` app (`com.vinicius.agentstudio.active-auth-qa`, separate Cargo target/WebView profile, CDP 9482) verified real Antigravity 1.2.1 authentication and a completed Gemini reply, then right-clicked and deleted its next running response. The saved workspace ended empty. The final composer assertion was recovered separately: this synthetic workspace has no recent folder, so the next draft correctly requires folder selection. A build was initially started before that final native assertion finished; keep builds serialized through the entire native check. No provider call was repeated for the assertion repair. Evidence: `artifacts/active-auth-native-result.json`, `artifacts/active-delete-native.png`, and `artifacts/auth-required-{browser,mobile}.png`; screenshots were visually reviewed and durable QA learnings recorded locally. Existing user apps were left running. Real signed-out credentials were not changed; signed-out/unknown behavior uses browser and native subprocess fixtures, and remote cancellation/checkpoint behavior uses automated transport/sync tests. Native backend changes require rebuilding/restarting the user's existing app.

## Sidebar conversation deletion — 2026-09-11

Moved Delete conversation out of the header into a context menu on each Active/History sidebar row. Right-click, Shift+F10/the context-menu key, or a touch long-press opens the menu without selecting that chat or changing the current draft. The existing confirmation identifies the target conversation. Cancellation returns focus to the row; running conversations are disabled in the menu and rechecked before removal. The menu stays inside the viewport and dismisses on Escape, Tab, outside interaction, sidebar scrolling, or resize. Confirmation takes precedence over the mobile drawer for stacking and keyboard focus.

The final full gate passed: `npm run verify` (zero diagnostics, 77 unit/HTTP tests, production build, 68 browser scenarios), Rust formatting, Clippy with warnings denied, and 64 Rust tests with four existing opt-in integrations ignored. Browser scenarios cover deletion of the current chat, deletion of a different History chat while preserving the open chat/draft/messages, cancellation, keyboard and touch access, live running-state changes, and late title cancellation without resurrection. Initial checks caught focus being lost during popover removal and menus closing on unrelated reply scrolling; focus now returns after DOM removal and only scrolling the menu's anchor ancestors dismisses it. These learnings were recorded locally. Logs: `artifacts/sidebar-delete-{focused,verify,rust}.log`.

The existing isolated native QA app passed actual CDP right-button, Shift+F10, and touch-start/hold/end input at 1380×900 and an emulated 390×844 viewport, DPR 1.5. Verified the removed header action, exact non-active target, preserved draft and saved conversations, confirmation above the drawer, and zero renderer errors. Settled native screenshots and browser screenshots were visually reviewed. Evidence: `artifacts/sidebar-delete-native-result.json`, `artifacts/sidebar-delete-native.png`, `artifacts/sidebar-delete-native-mobile.png`, `artifacts/sidebar-delete-native-confirmation-mobile.png`, and `artifacts/sidebar-context-menu-{browser,mobile}.png`. Native checks cancelled confirmation and sent no provider prompts; actual deletion was verified with isolated browser fixtures. Physical-phone input was not tested.

## Remove manual model refresh and Markdown copy — 2026-09-11

Removed Refresh model list and Copy conversation from the shared chat toolbar, including the mobile actions menu, and deleted the unused Markdown clipboard handler/imports. Automatic catalog loading, cached choices, and refresh when the selected computer reconnects remain unchanged. Existing tests now exercise automatic loading instead of invoking the removed control; the old native folder smoke checks model availability after folder selection rather than claiming a manual refresh.

The first full browser run exposed a fixture assumption: ordinary New conversation reuses the recent folder, so reopening the same chat did not trigger the expected automatic request. A computer-scoped draft clears the folder and resets readiness before reopening. The corrected mobile test passed, and the final full gate passed `npm run verify` (zero diagnostics, 77 unit/HTTP tests, production build, 66 browser scenarios), Rust formatting, Clippy with warnings denied, and 64 Rust tests with four existing opt-in integrations ignored. The mobile scenario retains real model-error, heartbeat-expiration/409, offline, and reconnect coverage. The test setup learning was recorded in local memory. Logs: `artifacts/header-actions-{verify,rust,mobile}.log`.

The existing isolated native QA app rendered the final toolbar at 1380×900, 880×720, and an emulated 390×844 viewport, all at DPR 1.5. Both removed actions were absent, the History composer and remaining Delete/context/instructions actions were retained, saved conversations were unchanged, and no renderer errors or page overflow occurred. Desktop and phone-width screenshots were visually reviewed. Evidence: `artifacts/header-actions-native-result.json`, `artifacts/header-actions-native.png`, `artifacts/header-actions-native-compact.png`, and `artifacts/header-actions-native-mobile.png`. No provider prompts or sign-in flows were run; phone width was emulated, not checked on a physical device.

## Restore through chatting only — 2026-09-11

Removed the Restore conversation button and ArchiveRestore icon from the chat toolbar. Active conversations retain Move to history; its handler now only archives. History chats keep an enabled composer, and sending a valid message restores the same conversation automatically. Updated the existing browser scenarios and the older native folder smoke script to use the current behavior.

The full required gate passed: `npm run verify` (zero diagnostics, 77 unit/HTTP tests, production build, 66 browser scenarios), Rust formatting, Clippy with warnings denied, and 64 Rust tests with four existing opt-in integrations ignored. Existing browser coverage verifies empty submissions remain in History, valid sends restore the same chat, earlier messages survive, and active chats can still be archived. Logs: `artifacts/remove-restore-{verify,rust}.log`.

The existing isolated Standalone QA app, launched through `npm run tauri dev`, rendered the final frontend at 1380×900 and 880×720 CSS pixels / DPR 1.5. Verified the absence of Restore and archive controls for a History chat, retained Copy/Delete actions, an enabled composer, unchanged saved conversations, and no renderer errors or horizontal overflow. Both screenshots were visually reviewed. Evidence: `artifacts/remove-restore-native-result.json`, `artifacts/remove-restore-native.png`, and `artifacts/remove-restore-native-compact.png`. No provider prompts or sign-in flows were run; the older live-provider folder smoke was updated but not executed for this UI change.

## Standalone chat labels and icons — 2026-09-11

Chats without a project folder now show **Standalone** with a MessageCircle chat-bubble icon in Active/History groups and the selected Folder toolbar value. Hover text explains that they have no project folder, and their plus action is labelled **New standalone conversation**. Project folders retain their folder icon and path title. This changes presentation only; saved location identities, execution routing, and draft creation remain intact.

The full required gate passed: `npm run verify` (zero diagnostics, 77 unit/HTTP tests, production build, 66 browser scenarios), Rust formatting, Clippy with warnings denied, and 64 Rust tests with four existing opt-in integrations ignored. The existing standalone draft scenario still covers collapsed History groups, the correct environment, draft focus, sending, and preservation of prior conversations. Logs: `artifacts/standalone-{verify,rust}.log`.

Native `npm run tauri dev -- --no-watch --config artifacts/standalone.tauri.json` used a separate app identity and WebView profile with the unused mobile QA Cargo target. Synthetic project and standalone chats verified the distinct icons, explanatory titles, a fresh standalone draft, and no horizontal overflow at 1380×900 and 880×720 CSS pixels / DPR 1.5. Screenshots were visually reviewed; compact labels retain the existing ellipsis behavior and full hover text. No renderer errors or provider prompts occurred. Evidence: `artifacts/standalone-native-result.json`, `artifacts/standalone-native.png`, `artifacts/standalone-native-compact.png`, and `artifacts/standalone-sidebar.png`. Existing app processes were preserved; provider execution and sign-in were not retested for this presentation change.

## Agent dropdown placeholders — 2026-09-11

The Agent trigger now shows a muted **No agents** placeholder when the selected computer has no available CLI options, or **Select agent** when options exist but none matches the draft selection. These are display labels, not selectable agents. Existing regression checks retain zero menu options for missing WSL CLIs, disabled sending, keyboard safety, and fixed saved-chat agent labels.

Visually verified the label in the isolated native Ubuntu scenario at 1380×900 CSS pixels: it fits without truncation, the menu has zero options, sending is disabled, and there are no renderer errors. Evidence: `artifacts/agent-placeholder-toolbar.png`, `artifacts/agent-placeholder-native.png`, and `artifacts/agent-placeholder-native-result.json`. No provider replies or sign-in flows were run.

The final required gate passed: `npm run verify` (zero diagnostics, 77 unit/HTTP tests, production build, 66 browser scenarios), Rust formatting, Clippy with warnings denied, and 64 Rust tests with four existing opt-in integrations ignored. Logs: `artifacts/agent-placeholder-{verify,rust}.log`.

## Empty Agent choices for missing CLIs — 2026-09-11

New chats exclude unavailable connections even when that provider was previously selected. Choices are scoped to the selected computer before and after folder selection. A missing draft selection stays blank instead of displaying the first remaining option; saved conversations retain their fixed agent label. The empty menu has no invalid active-descendant reference, and keyboard interaction cannot select a nonexistent option. The first full browser pass caught two unpaired-preview regressions: that preview has no computer or fleet connections, so it still needs demo provider options. Keep that fallback limited to an unpaired browser with no selected computer; both existing preview scenarios and the empty-WSL regression passed after the correction.

Browser regressions cover an empty WSL installation after choosing Desktop Claude, draft preservation, returning to Desktop, a late missing-Claude result while Codex remains installed, and reopening a saved WSL conversation after its CLI is removed. Native `npm run tauri dev -- --no-watch --config artifacts/empty-agent.tauri.json` used a separate app identity, Cargo target, and WebView profile on CDP 9480. Real Ubuntu inventory reported all three CLIs absent; the rendered Agent selection and menu were empty, Send was disabled, and there were no renderer errors. No provider prompts or sign-in flows were run. Screenshots `artifacts/empty-agent-{browser,native}.png` were visually reviewed; native results are in `artifacts/empty-agent-native-result.json`.

Final full gate passed: `npm run verify` with zero Svelte/TypeScript diagnostics, 77 unit/HTTP tests, the production build, and 66 browser scenarios; Rust formatting, Clippy with warnings denied, and 64 Rust tests passed (four existing opt-in integrations remain ignored). The native check was repeated on the final code. Logs: `artifacts/empty-agent-{verify,rust}.log`.

## Windows desktop pairing and automatic reconnect — 2026-09-10

The user's PWA correctly remained Offline after the desktop reopened in **Local only** mode: desktop relay authentication was intentionally memory-only. With the user's approval, Windows now saves its own relay origin/key in Windows Credential Manager, scoped to the current Windows user, app identifier, and installation. Native restoration returns only the origin to the renderer. Startup retries temporary failures, including while minimized; explicit disconnect removes the saved pairing and cannot be undone by an older restoration. Other native platforms keep temporary pairing. No provider credential behavior changed.

The real Windows credential test covers storing, loading, replacing, isolating, refusing oversized replacements, and deleting a synthetic pairing. Transport regressions cover restoration/heartbeat, absent pairing, temporary failure, replaced-server rejection, late restoration after disconnect, and failed deletion. `scripts/relay-reconnect-native-smoke.mjs` used `npm run tauri dev -- --no-watch --config scripts/native-relay.tauri.json`, an isolated app identity/WebView profile and CDP 9473, Vite 1435, and a real HTTP relay. It closed and relaunched the actual executable, held the server unavailable during startup, then observed the same 390×844 PWA page change from Offline to Online without reloading. App/browser identities were preserved; the key was absent from native workspace/checkpoint files and renderer storage. Disconnect remained effective after another process restart. No renderer errors occurred. Screenshots were visually reviewed under `artifacts/relay-reconnect-native/`.

The first complete browser gate exposed a previously unpersisted empty relay identity. A normal restart before any workspace edits generated a different instance ID and tripped the new replacement guard. The relay now atomically saves its initial empty workspace/identity before accepting clients; a real HTTP regression and the existing service-worker/server-restart test pass. Unreadable state is still preserved and rejected.

Final full gate passed: `npm run verify -- -- --config artifacts/reconnect.playwright.config.ts` (only the isolated Vite origin/test-output paths differ from the standard config), zero Svelte/TypeScript diagnostics, 77 unit/HTTP tests, production build, and all 65 browser scenarios. Rust formatting, Clippy with warnings denied, and 64 Rust tests passed; four pre-existing opt-in integrations remain ignored. Logs are `artifacts/relay-reconnect-{verify,clippy,rust}.log`. Native restart/PWA recovery was exercised without sending provider prompts. Physical-phone behavior and new provider replies were not retested. Existing desktops need one pairing after upgrading from the memory-only version.

## Offline execution computers in the PWA — 2026-09-10

The live relay already reported Desktop and its managed WSL environment offline while the app was closed. The chat computer picker and sidebar did not expose that state, and model queries still reached the offline host. The PWA now marks offline computers throughout chat, uses the owning Windows host's heartbeat for managed WSL, skips unavailable remote model/usage queries, and refreshes the selected connection's models on recovery. The relay's `host_offline` response covers the race between a request and the next heartbeat poll. Actual model-query errors on an available connection remain visible.

The real HTTP mobile regression now covers Desktop plus WSL, an online provider error, heartbeat expiry during a request, offline reload and saved choices, disabled sending, both offline Connections cards, zero job submissions while offline, and automatic model recovery on the same account/host. Both mobile scenarios passed in Chromium and Windows WebKit; offline screenshots were visually reviewed. The final full gate passed: `npm run verify` with zero diagnostics, 71 unit/HTTP tests, production build, and 65 browser scenarios; Rust formatting, Clippy with warnings denied, and 63 Rust tests passed (four existing opt-in integrations ignored). Evidence is under `artifacts/pwa-host-presence/`. New native provider replies and physical-phone behavior were not exercised for this change.

The first full browser pass caught a model-loading regression in a legacy saved chat without a folder: its provider/connection cache key matched the initial draft, so the effect never retriggered. Model refresh now also tracks the transition to a selected folder or existing chat as a stable boolean, preserving both initial loading and quiet heartbeat polling. The existing context-history regression and the repeated full gate passed after that correction.

## Quiet mobile connection status — 2026-09-10

Removed the persistent connected message and green dot. A healthy installed PWA now has no status row; browser tabs retain the Install app action. Setup, offline, and server-error notices remain, along with detailed sync state in Connections. Existing mobile regressions now confirm successful heartbeat responses independently of the removed text, verify that the installed row disappears, and retain restart, update, reload, outage, and disconnect coverage. The connected screenshot was visually reviewed. `npm run verify` passed (71 unit/HTTP tests, 65 browser scenarios, zero diagnostics); Rust formatting, Clippy, and 63 Rust tests passed with four existing opt-in tests ignored. Both mobile scenarios also passed in Windows WebKit. Evidence is under `artifacts/pwa-quiet-status/`. No native provider or physical-phone execution was needed for this presentation change.

## PWA pairing across updates — 2026-09-10

- Browser sessions previously lived only in the relay's memory, so a deployment restart discarded every phone's pairing. Session-ID digests now persist separately from workspace/public files, with atomic restricted writes and pairing-key binding. Coverage verifies restart/reload, daily renewal with retained Secure/HttpOnly/SameSite flags, inactivity expiry, durable logout, key rotation including switching back, unreadable-file preservation, failed writes, and rejection of a digest used as a cookie. Older memory-only sessions require one final pairing when first upgrading.
- The PWA automatically retries unavailable session/state endpoints after startup and renews its session while syncing. The regression actually restarts the HTTP server on the same origin/data directory, installs and activates an updated service worker, reopens with the same cookie/device identity, and recovers from server-side 503s without focus/reload/pairing actions. Explicit disconnect and API cache exclusion remain verified. No commands are replayed.
- Final local gate: `npm run verify` passed with zero diagnostics, 71 unit/HTTP tests, production build, and 65 browser scenarios. Rust formatting, Clippy with warnings denied, and 63 tests passed (four existing opt-in integrations ignored). The Linux container built and passed its public-shell/pairing/API-boundary smoke. Both mobile HTTP scenarios passed in Windows WebKit. Chromium and WebKit reconnect screenshots were visually reviewed. Evidence: `artifacts/pwa-session-verify.log`, `artifacts/pwa-session-rust-*.log`, `artifacts/pwa-session/`, and `test-results/mobile-pairing-survives-a--ad39c-tries-an-unavailable-server/`.
- Harness corrections: restart fixtures send `Connection: close` to avoid Node reusing a dead pooled socket (`ECONNRESET`); eight session tests passed five consecutive repeats after this correction. WebKit outages use actual server responses rather than service-worker request interception. Windows WebKit reports Strict cookies as None, an [upstream expected failure](https://github.com/microsoft/playwright/blob/main/tests/library/browsercontext-cookies.spec.ts); the response header is asserted on every engine and stored SameSite remains asserted on supported engines. Stopping the real server verifies cached-shell loading where WinCairo offline emulation raises an internal navigation error. These are browser checks; physical iPhone/Android update behavior and new native provider replies were not retested.

## Automatic phone safe areas — 2026-09-10

- The user's physical iPhone screenshot confirms that the preceding fix removed clipping but left roughly 98 CSS pixels below the usage tracks (1290×2796 screenshot, approximately 430×932 CSS pixels). The screenshot alone does not expose the device's DOM or its reported inset. A Chromium regression supplying real CSS `env()` values through CDP (59px top, 93px bottom) reproduces the 98px gap; the previous 34px-only fixture missed this larger-inset case.
- Switched the viewport from `cover` to `auto`, letting the browser reserve the safe content rectangle. Removed the app's additional device inset padding from the shell, main area, drawer, close button, and composer. The app retains ordinary 10px composer padding, leaving 15px below the 6px tracks within the usable page. The sidebar footer ends at the page boundary. Retained the dynamic-height clipping correction and keyboard height/pan handling. No physical-screen height, negative margin, or device-specific pixel compensation is used.
- The new regression fails before the change with `Expected: 15, Received: 98`. The four focused mobile scenarios pass in Chromium and Windows WebKit. Coverage retains track-edge hit testing, keyboard dismissal/panning, smaller usable-page resizing, drawer bounds, and anchored dropdowns. The new case supplies actual nonzero `env()` values in Chromium; WebKit validates the fitted-page layout without a device-inset override. Chat/drawer WebKit screenshots were visually reviewed under `artifacts/mobile-layout/webkit-results/`; logs are under `artifacts/mobile-auto-insets/`.
- Browser tests establish the app's layout contract, not Safari's system inset behavior on a physical phone. The on-device gap still requires confirmation after the updated viewport configuration loads. This supersedes the manual-inset approach below.

## iOS 26.5.2 footer clipping and page bounds — 2026-09-10

- The user confirmed that the previous bottom-gap patch did not resolve the gap and clipped the usage tracks with the keyboard closed. That report supersedes the simulated success described below. The maximum-height workaround enlarged only the fixed shell while leaving a percentage-height root/body, and could exceed the drawable page. Removed that workaround, sized the root/body and shell with `100dvh`, bounded the main grid row, and replaced the translucent status-bar request with an opaque black status bar. The top safe-area padding now subtracts any visual viewport pan; the drawer, like the composer, removes its bottom inset while the keyboard is open.
- The replacement regression keeps an actual 844px browser viewport while reporting a 906px fullscreen height. Before the fix it failed with an app bottom of 906px. It supplies nonzero 59px top and 34px bottom insets, checks root/body/shell/composer/drawer/footer bounds, and hit-tests both vertical edges of every 6px usage track at rest, after keyboard panning, after dismissal, and after resizing. This replaces the earlier fixture that artificially shortened only the CSS fallback and checked outer boxes. Chromium and Windows WebKit both pass the focused scenarios; the WebKit safe-area chat/drawer screenshots were visually reviewed. Evidence is under `artifacts/mobile-insets/` and `artifacts/mobile-layout/webkit-results/`.
- These are browser regressions, not a physical iPhone verification. [WebKit 301994 comment 12](https://bugs.webkit.org/show_bug.cgi?id=301994#c12) reports a system-owned gap on this exact iOS version; matching symptoms are evidence for investigation, not proof that this phone has that engine bug. App sizing cannot paint into an area owned by iOS. Device confirmation after loading the new app remains necessary, including whether the changed status-bar mode is honored by the existing home-screen installation.

## Installed-app bottom gap — 2026-09-09 (superseded)

- A follow-up iPhone report identified unused space below both the main screen and sidebar on launch and after keyboard dismissal, with the space absent while the keyboard was open. The attempted fix made installed mode use a shared height covering the full window/large viewport. It failed on the user's phone and has been removed; the former assumption that a larger shell would recover that space was not established by the test.
- The added regression models a 34px short CSS-height report and verifies the bottom edges of the shell, composer, sidebar, and backdrop on launch. It also checks keyboard opening/dismissal, replacement of the resting height after window resizing, and return to browser-tab sizing. This is a simulated Safari geometry regression; physical iPhone confirmation remains necessary.

## iPhone keyboard layout and anchored dropdowns — 2026-09-09

- The mobile shell is fixed to the visual viewport's height and vertical offset while the keyboard occludes the layout viewport. Resize, pan, focus, return, and delayed animation updates are handled together. Dismissal removes the stored height/offset, restores the dynamic CSS viewport and home-indicator inset, and clears document scroll. Pinch zoom remains browser-controlled. Message updates and highlighted dropdown options scroll only their own container.
- Shared ChoicePicker menus use the browser top layer and measured trigger coordinates, with horizontal clamping, upward placement when needed, and bounded option scrolling. This replaces the mobile bottom-sheet override and prevents clipping by the horizontally scrolling toolbar and dialogs. Safari pointer activation explicitly focuses the combobox without scrolling.
- New regression scenarios simulate separate keyboard height/pan signals, typing, scroll-only offset changes, stale 44px dismissal geometry, repeated focus, a delayed geometry update without another resize event, and restored screen height. Dropdown checks cover trigger adjacency, screen bounds, horizontal toolbar scrolling, keyboard selection, focus, and no document scroll. Screenshots from Chromium and Windows WebKit 26.6 were reviewed under `artifacts/mobile-layout/` and `test-results/`.
- An additional Windows WebKit diagnostic completed hosted pairing, a synthetic remote reply, stop, session restoration, history, and drawer checks. Its final offline reload failed with `WebKit encountered an internal error`; that additional WebKit offline check remains unverified. That diagnostic confirmed the real `SameSite=Strict` response header because this WebKit automation run reported `None` in cookie metadata. The normal Chromium cookie assertion remains unchanged. Physical iPhone home-screen keyboard animation and home-indicator behavior still require device confirmation; simulated visual viewport signals are not a real software keyboard.
- Required pipeline evidence: `artifacts/mobile-layout/verify.log`, `rust-clippy.log`, and `rust-tests.log`. No provider credentials or native execution routing changed.

## Public VPS deployment — 2026-09-09

- Deployed application commit `12f2d6d` to `72.61.63.95` as a dedicated unprivileged systemd service behind the existing Caddy proxy. Public endpoint: https://studio.72.61.63.95.sslip.io. The VPS production build and all 66 unit tests passed; Node's release archive and the transferred source archive were checksum verified.
- Public browser checks passed for trusted HTTPS, app/manifest/service-worker loading, PWA installability, pairing, Secure/HttpOnly/SameSite session cookies, reload restoration, logout, and offline shell. Unauthorized API calls returned 401; source and workspace-file paths returned 404. Evidence: `artifacts/vps-deploy/public-result.json` and `public-mobile.png`.
- Added and verified a dedicated SSH key with password authentication disabled, while preserving existing keys. Credentials are outside the repository. Existing `deck-server`, Minecraft, and Caddy process IDs were unchanged; `deck.simpx.net` continued returning HTTP 200. See [DEPLOYMENT.md](DEPLOYMENT.md) for operations and remaining physical-phone/public-provider boundaries.

## Hosted mobile PWA — 2026-09-09

- The production frontend is served by the relay on the same origin. `tests/mobile.spec.ts` uses the actual HTTP server and browser cookie authentication with a synthetic desktop executor. It verifies wrong-key rejection, exact host/account routing, folder browsing, replies, streaming/cancellation, session restoration, draft-preserving drawer navigation, 320/390/650/768 px widths, a 460 px keyboard-height viewport, offline shell loading, blocked offline sends, and absence of API responses or pairing keys in browser caches/storage.
- Real-provider smoke: `scripts/mobile-native-smoke.mjs`, with `npm run tauri dev -- --config scripts/native-mobile.tauri.json`, isolated app identifier `com.vinicius.agentstudio.mobile-qa`, separate Cargo target/WebView profile, and CDP 9447. A 390×844 mobile Chromium browser paired to the production PWA over loopback HTTP, browsed a temporary Windows folder, selected its existing CLI accounts, and received exact-marker replies from Codex GPT-5.6-Luna and Claude Haiku. Both completed replies were independently confirmed in the native host’s saved workspace. Evidence: `artifacts/mobile-native/result.json`, `codex-phone.png`, and `claude-phone.png`. Existing desktop sessions were left running.
- Security/server tests cover static file allowlisting, unauthorized API requests, cross-origin pairing/mutations, cookie identity mismatch, session expiry/logout, pairing rate limits, and refusal to claim execution jobs from a browser. A separate plain-Node import regression checks the actual server runtime, without Vite resolving TypeScript imports.
- Built the multistage `relay/Dockerfile` as `agent-studio-mobile-qa:local` with Docker Engine in Ubuntu/WSL. `scripts/relay-container-smoke.mjs` passed inside the image: PWA HTML, manifest, PNG icons, service worker, browser pairing, authenticated state, and denied source/state-file access. Compose configuration validated. The container runs as the Node user; no public deployment was made.
- Full frontend gate: `npm run verify` (Svelte checks, Vitest, production build, Playwright). Rust gate: formatting, Clippy with warnings denied, and 63 passing tests / 4 opt-in tests ignored. A physical iPhone/Android installation, Safari keyboard behavior, public HTTPS proxy deployment, and a real mobile Gemini reply remain unverified.
- Durable setup findings: plain Node 24 cannot resolve extensionless runtime imports that Vite accepts. The relay’s shared schema imports now name `.ts` explicitly, and its runtime image includes transitive activity/image schemas. On this Windows host, Docker is in Ubuntu/WSL; the Windows command is absent and the default WSL user cannot access the daemon socket, so the container checks used the distribution’s root account without changing daemon permissions. Playwright network emulation can leave `navigator.onLine` true after an offline service-worker reload; the UI also reports actual server-request failures and disables control while unpaired.

## Drop images anywhere in chat — 2026-09-09

- Expanded the file drop target from the message input to the entire chat area. A highlighted overlay explains the action, supported formats, or unavailable provider/attachment state. Drops attach once, preserve the text draft, and focus the composer. Leaving the chat, dropping, Escape, drag end, and window blur dismiss the hint. File drops outside chat are prevented from navigating the app, and ordinary text drops remain unaffected.
- `scripts/images-drop-native-smoke.mjs` verified a trusted WebView2 file drop through CDP `Input.dispatchDragEvent` at coordinates inside the message area. The native event delivered the original PNG bytes exactly once, withdrew the overlay, and focused the message input. Moving the file drag out of chat and pressing Escape cleared the hint. No provider request was sent, the persisted QA workspace was unchanged, and renderer exceptions were empty. This exercises native browser file input rather than constructing a DOM drop event or filling the hidden file input; a manual Explorer mouse gesture was not used.
- Evidence: `artifacts/images-native-drop-result.json`, `artifacts/images-native/drop-hint.png`, `artifacts/images-native/drop-attached.png`, and `artifacts/images-chat-drop-hint.png`. Reviewed the native 2070×1350 drop hint and attached preview. Reuses the isolated images QA identity on CDP 9461 and the existing Vite server; no native backend change was required.
- Browser regression coverage includes dropping on the welcome/message area, nested drag transitions, Escape, exactly-once attachment, preserved text/focus, Gemini feedback, and drops outside chat. Full pipeline logs: `artifacts/images-drop-verify.log` and `artifacts/images-drop-rust.log`.
- Native QA detail: CDP `dragCancel` alone did not emit destination DOM `dragleave`/`dragend` in this WebView2 build. Use a native drag-over outside the chat and actual Escape key events to verify hint dismissal; do not treat that protocol acknowledgement as a delivered cancellation event.

## Chat image attachments — 2026-09-09

- Added file selection, screenshot paste, composer file drop, removable thumbnails, keyboard-closeable image previews, and image-only messages for Codex and Claude. PNG/JPEG/WebP bytes persist in optional workspace v3 user-message images. Gemini displays the current Antigravity text-input limitation. No original images are resized or recompressed.
- Frontend and Rust fixtures cover bounded image encoding, MIME/header validation, unsupported provider/role/background requests, image-only prompt replay, image retention during archive/checkpoint merging, and provider-specific structured image payloads. Browser scenarios cover sending/retry/reload, paste/drop/removal, compact layout, invalid and oversized files, count limits, Gemini, and delayed reads after switching drafts. The two-environment real HTTP relay scenario includes an image and checks the exact bytes received and saved by the owning host; its provider IPC remains synthetic.
- Real native smoke passed at **2026-09-09T22:19:22Z** using `npm run tauri dev`, `scripts/native-images.tauri.json`, isolated app identity `com.vinicius.agentstudio.images-qa`, a separate Cargo target/WebView profile, and CDP 9461. Claude Sonnet and Codex GPT-5.6-Sol each identified a blue triangle and the random code printed only in the supplied image. Neither reply used tools. Original bytes and rendered previews survived native save/reload. The Codex PNG was 1,761,543 bytes (base64 above the ordinary 2 MB output-line limit), exercising larger visual payloads. Both replies completed with zero renderer exceptions.
- Evidence: `artifacts/images-native-result.json`, `artifacts/images-native/claude-composer.png`, `artifacts/images-native/claude-reply.png`, `artifacts/images-native/codex-reply.png`, and `artifacts/images-composer-compact.png`. Native captures are 2070×1350 pixels (1380×900 CSS at DPR 1.5); the compact browser capture is 840×640. Visually reviewed the composer and saved reply previews.
- Repeat with `node scripts/images-native-smoke.mjs` after launching the isolated QA app. The script refuses unrelated app identities/workspaces. Windows existing-profile Claude/Codex were exercised; WSL, separate authenticated profiles, physical remote computers, and OS-origin drag/clipboard gestures require their own native checks. Browser drag/paste events and native file-input delivery are automated. The normal desktop session is not used as a test fixture.
- The full required gate passes with zero Svelte/TypeScript diagnostics, 63 frontend/relay tests, the production build, 58 browser scenarios, Rust formatting, Clippy with `-D warnings`, and 63 Rust tests (four pre-existing opt-in integrations excluded). Pipeline results are captured in `artifacts/images-verify.log` and `artifacts/images-rust-tests.log`.

## Green guide and red overflow — 2026-09-09

- Changed the shared quota comparison palette in Connections and chat: the secondary bar is muted green below the guide; above it, the guide is orange and the excess is red. The endpoint marker remains absent. This replaces the previous gray/blue guide colors. Current usage, scale, pace calculations, and stale-reading behavior are unchanged.
- Existing browser checks verify the rendered guide/overflow colors, visible segments, and absent marker. Reviewed fresh Connections and chat captures, including the live native Claude card showing a green secondary background on Weekly and orange/red on Fable weekly.
- `npm run verify` passed with zero diagnostics, 60 frontend/relay unit tests, production build, and 54 browser scenarios. Rust formatting, Clippy with warnings denied, and 60 default tests passed; four opt-in integrations remain excluded. `git diff --check` passed. Log: `artifacts/quota-semantic-colors-verify.log`.
- The isolated native usage smoke passed at **2026-09-09T21:43:43Z**, using read-only CLI queries with no renderer errors and the workspace preserved. No sign-in or paid replies were requested. Evidence: `artifacts/quota-semantic-colors-native.log`, `artifacts/account-usage-claude-native.png`, `artifacts/account-usage-desktop.png`, and `artifacts/pace-browser.png`.

## Quota bars without endpoint markers — 2026-09-09

- Removed the shared endpoint pseudo-element from Connections and chat usage bars. The secondary guide and current/excess colors remain on the same percentage scale. This supersedes the marker styling described below.
- Updated the existing browser comparison helper to verify the marker is absent while retaining its overlap/excess visibility checks. Reviewed fresh chat and Connections screenshots, plus the live native Claude card with usage above the guide.
- `npm run verify` passed: zero Svelte/TypeScript diagnostics, 60 frontend/relay unit tests, production build, and 54 browser scenarios. Rust formatting, Clippy with warnings denied, and 60 default tests passed; four opt-in integrations remain excluded. `git diff --check` passed. Log: `artifacts/quota-no-marker-verify.log`.
- The isolated native usage smoke passed at **2026-09-09T21:21:47Z**, with real read-only account usage, no renderer errors, and the workspace preserved. No sign-in or paid replies were requested. Evidence: `artifacts/quota-no-marker-native.log`, `artifacts/account-usage-claude-native.png`, `artifacts/account-usage-desktop.png`, and `artifacts/pace-browser.png`.

## Visible quota comparison and Codex windows — 2026-09-09

- Corrected the shared meter rendering in Connections and chat. Previously the current fill painted over the recommendation when usage passed it, leaving only a low-contrast pale marker on orange. The overlapping guide now paints above the current fill in muted blue; the amount beyond it retains the current pace color. Its endpoint has a dark 3px core with pale edges. Both bars retain their actual widths and the same percentage scale; stale/error states still remove the guide.
- An unreported Codex 5-hour window no longer creates a placeholder in the account list, compact chat strip, or expanded chat panel. Actually reported 5-hour values, including zero and separately labelled Spark limits, remain visible. Compact chat columns expand to fit the remaining groups. Other providers retain their unknown-value semantics.
- Existing browser checks now verify the visible layer at both the overlap and excess, plus marker contrast of at least 3:1 against both fill colors. They cover below/at/above the guide, account and chat layouts, failure/stale removal, missing Codex rows, and preserved reported/Spark values. Shared helper: `tests/quota-helper.ts`. Reviewed desktop/narrow captures and real native Codex/Claude cards; the native Codex card shows Weekly and reported Spark quotas without the absent primary 5-hour row.
- Native `node scripts/wsl-computers-native-smoke.mjs --usage` passed at **2026-09-09T20:29:35Z**, with live Codex/Claude/Gemini readings, no renderer exceptions, and the isolated workspace preserved. No sign-in or paid replies were requested. Evidence: `artifacts/quota-contrast-native.log`, `artifacts/wsl-computers-native-result.json`, `artifacts/account-usage-{codex,claude}-native.png`, `artifacts/pace-browser.png`, and `artifacts/pace-compact.png`. The existing development apps remain running; no native rebuild or installer was needed for these frontend changes.
- Final `npm run verify` passed with zero Svelte/TypeScript diagnostics, all 60 frontend/relay unit tests, the production build, and 54 browser scenarios. Rust formatting, Clippy with warnings denied, and 60 default tests passed; four unrelated opt-in integrations were not repeated. `git diff --check` passed. Log: `artifacts/quota-contrast-verify.log`. The rendering/visibility lesson and Codex window policy were recorded in the requested memory notes.

## Account usage in Connections — 2026-09-09

- Provider CLIs now stack vertically within each computer. Each account shows 5-hour and Weekly consumption, reset times, pace arrows, and remaining hourly/daily budget. Additional reported model scopes retain explicit labels (including Fable and Spark); chat context is omitted from the account view. The same percentage, reset, stale-reading, and pace calculations used by chat drive this view. Desktop layouts place usage beside account actions; narrower layouts place it below them, with quota columns adapting to available width.
- Connections requests usage without requiring a chat or folder, through the existing transport with each exact connection ID. Only installed, ready local or online remote accounts are queried. Opening/focusing Connections, its refresh action, and a visible-page minute timer update readings. Loading and checked states are per account, failures retain earlier values, offline/sign-in states are explicit, and stale/unavailable readings lose pace/budget guides. Nothing is persisted in workspace snapshots. Native reads now await the three-process semaphore rather than returning `Usage refresh is already busy` for additional accounts.
- `node scripts/wsl-computers-native-smoke.mjs --usage` passed at **2026-09-09T20:20:06Z** in the isolated Tauri QA app. Real CLI queries rendered Codex, Claude, and Gemini limits, including explicitly labelled Spark/Fable windows and unknown primary windows. Four concurrent forced read-only requests completed successfully through the bounded native queue. No sign-in, provider prompt, or paid reply was requested. Workspace preservation and renderer exception checks passed. The main and QA native apps were rebuilt serially with their existing development watchers and remain running.
- Browser coverage checks two Claude connections with distinct readings, zero versus missing values, reset/budget/pace display, delayed refresh progress, failed refresh retention, signed-out query suppression, periodic refresh, no chat creation, and narrow layouts. The HTTP relay scenario verifies a remote WSL account's usage executes on its Windows host, does not execute against the requesting computer's login, and stays visible without a pace guide after disconnecting. Live Linux usage could not be checked because Ubuntu has no Linux Codex/Claude installation; the remote/WSL usage scenario uses controlled provider fixtures with a real HTTP relay. Existing saved chats/accounts were preserved.
- Reviewed `artifacts/account-usage-desktop.png`, `artifacts/account-usage-narrow.png`, and `artifacts/account-usage-{codex,claude,gemini}-native.png`. Native results/log: `artifacts/wsl-computers-native-result.json` and `artifacts/account-usage-native.log`. Native assertions wait for account availability checks before accepting idle usage state; an initially idle widget may not have started its request. Large screenshot targets must fit inside the app's scrolling viewport or be captured per provider; otherwise the offscreen portion is clipped. Tests scope the global sign-in notice separately from per-account usage status regions and open Sync settings before testing relay disconnection.
- Final `npm run verify` passed: zero Svelte/TypeScript diagnostics, 60 frontend/relay unit tests, production build, and all 54 browser scenarios. Rust formatting, Clippy with warnings denied, and all 60 default Rust tests passed; four unrelated opt-in integrations were not repeated. `git diff --check` passed. Full frontend log: `artifacts/account-usage-verify.log`. No installer, commit, or push was created.

## Automatic discovery and additional-account sign-in — 2026-09-09

- This replaces the preceding account-picker behavior recorded below. Each locally managed computer automatically registers its installed CLIs' existing login connections and checks authentication through the CLI. Add account contains only Provider and Account name, uses the selected computer, and always creates an isolated Claude/Codex profile. It shows Creating account and Opening sign-in progress, persists before launching, and opens sign-in automatically. Launch errors remain in the dialog; retry uses the same saved connection. Completion polling checks that connection rather than another profile's ready state. Gemini remains existing-login only.
- Reproduced the flashing terminal from the actual isolated Tauri GUI: the former conhost launcher returned success, but conhost exited 0 and PowerShell exited `0xC000013A` before Claude started. A tool-shell reproduction survived and was therefore insufficient evidence. Windows now launches full-path PowerShell with `CreateProcessW`, `CREATE_NEW_CONSOLE`, and no inherited standard handles. A named event confirms shell readiness; process-exit checks expose startup failures. The shell retains command errors with `-NoExit`. This follows Microsoft's [console creation semantics](https://learn.microsoft.com/en-us/windows/console/creation-of-a-console).
- Native `node scripts/wsl-computers-native-smoke.mjs --second-account` passed at **2026-09-09T19:57:31Z** in the isolated WSL Computers QA app. Adding a temporary Claude account opened a live terminal; the new profile reported login required while the original remained ready. Closing only that test console and clicking Open sign-in opened another terminal for the same saved profile. Workspace contents were restored and no renderer exceptions occurred. The actual GUI process-tree probe also observed PowerShell, conhost, and Claude still running after nine seconds. The opt-in Rust console test verified interactive stdin and retained the shell after a synthetic command failure. No OAuth completion or paid provider reply was attempted; additional-account authentication remains the user's step. Codex's creation and sign-in targeting are covered by browser fixtures, while real second-account OAuth was not tested for either provider.
- Reviewed native dialog/account screenshots and the browser loading state: `artifacts/second-account-dialog-native.png`, `artifacts/second-account-awaiting-sign-in-native.png`, and `artifacts/add-account-progress-browser.png`. Native results and log: `artifacts/wsl-computers-native-result.json` and `artifacts/add-account-native.log`. The main development app and isolated QA app were rebuilt serially with their existing dev watchers and remain running; no installer was built.
- Regression coverage verifies automatic discovery, installed-provider filtering, Claude/Codex isolated profiles, progress while save/launch are delayed, retry without duplicates, connection-specific authentication, original profile preservation, and reload persistence. Older cold-CLI fixtures now delay requests before startup discovery and assert provider-specific pending work instead of obsolete aggregate request counts; folder/model/agent race protections remain asserted. A stale Start chat selector now targets the automatically detected account's Chat action. Explicit bindings cover the workspace and connection status objects mutated by FleetManager.
- Final `npm run verify` passed with zero Svelte/TypeScript diagnostics, 59 frontend/relay unit tests, the production build, and all 53 browser scenarios. Rust formatting, Clippy with warnings denied, and all 60 default Rust tests passed. Four opt-in tests are excluded by the default run; the new visible-console test was run separately and passed, while the three unrelated provider/WSL integrations were not repeated. `git diff --check` passed. Full frontend log: `artifacts/add-account-verify.log`.

## Additional account login selection — 2026-09-09

- Fixed the reported duplicate-existing-login error before Claude sign-in. The form previously offered both an occupied existing CLI login and account labels already connected on the target environment, then rejected submission. Additional Claude/Codex accounts now display and save a separate login when the existing login is occupied, including after changing providers. The form names the existing connection, excludes already-connected labels, and blocks unsupported duplicate Gemini logins before submission. Existing connections and sign-ins are preserved.
- Regression coverage checks separate Claude and Codex connection identities, saved profiles after provider changes, sign-in transport targeting the newly created connection, unchanged original account/connection data, and persistence after reload. It also checks that an account connected on Desktop remains selectable on Ubuntu, while duplicates on the same environment are absent. An older browser test intentionally selected an already-connected label; it now exercises valid reuse on Ubuntu instead.
- Native `node scripts/wsl-computers-native-smoke.mjs --second-account` passed at **2026-09-09T19:19:26Z** in the isolated WSL Computers QA app. It created a temporary separate Claude connection through the real UI, verified native detection reports installed but requiring login, and verified the original connection still reports ready. No sign-in launcher or provider reply was requested. The complete isolated workspace was restored afterward. Reviewed `artifacts/second-account-dialog-native.png` and `artifacts/second-account-awaiting-sign-in-native.png`; result and log are `artifacts/wsl-computers-native-result.json` and `artifacts/second-account-native.log`. Completing OAuth for an actual second account remains a user sign-in step.
- QA fixture corrections: read the current workspace immediately before checking newly initialized connections; the initial restoration snapshot can predate the app's automatic CLI connection creation. Account display names can match across environments, so tests must account for distinct identities instead of asserting that every option with the same name disappears. The final native check passed with both corrections and no renderer errors.
- Final `npm run verify` passed with zero Svelte/TypeScript diagnostics, 59 frontend/relay unit tests, the production build, and all 52 browser scenarios. Rust formatting, Clippy with warnings denied, and all 60 default Rust tests passed; three existing opt-in integrations were not repeated. Log: `artifacts/second-account-verify.log`. The development app remains running with the updated frontend; no installer was rebuilt.

## Organized provider and account cards — 2026-09-09

- Provider name, account count, and installation state share one compact header. The CLI path stays on one line with the full path available on hover. Each account row contains its name, Chat, Open sign-in, and a settings icon. Rename, profile details, and confirmed disconnection now live in the contextual Manage account dialog. An account already connected to the current computer no longer offers a redundant connection action; other accounts name the destination computer explicitly.
- Browser coverage verifies rename persistence, discarded edits on Escape, restored focus, cancellation of disconnection, account removal, local/remote separation, and narrow layouts. Moving the form into a dialog exposed a Svelte `ownership_invalid_mutation` warning; binding the existing workspace prop explicitly through FleetManager resolved it. The final `npm run verify` passed with zero Svelte/TypeScript diagnostics, 59 frontend/relay unit tests, the production build, and all 49 browser scenarios. Rust formatting, Clippy with warnings denied, and 60 default tests also passed; three opt-in integrations were not repeated for this UI change. Log: `artifacts/provider-card-verify.log`.
- Native inspection passed in the existing isolated WSL Computers QA app at **2026-09-09T18:50:30Z** with no renderer errors. Reviewed `artifacts/provider-card-organized-native.png`, `artifacts/account-management-native.png`, `artifacts/connections-accounts-narrow.png`, and `artifacts/account-management-narrow.png`. The smoke also rechecked strict Desktop/WSL routing and missing Linux CLI behavior, restored its workspace, and sent no sign-in requests or provider prompts. Existing app processes and the development server remain running; no installer was rebuilt.
- Native tooling encountered Playwright 1.63 asserting on a WebView2 `shared_worker` target without `browserContextId`. The smoke uses the supported custom CDP transport to exclude shared workers from automatic attachment, leaving those workers and the app running. The completed real DOM/IPC/screenshot check validates this workaround; older Playwright versions retain the ordinary CDP connection.

## Simpler Connections cards — 2026-09-09

- Removed the nested Windows/Here and WSL/Managed here environment cards, the CLI setup accordions and command blocks, ready-state Connected badges, and repeated environment/Connected account rows. Installation inventory now says Installed or Not installed without repeating the enclosing computer; failures still remain unknown or stale.
- Open sign-in is directly visible beside the current CLI or named account's chat actions, including accounts already signed in. Missing CLIs still disable sign-in. Account creation and editing no longer ask for Personal/Work or display category badges. Existing stored purpose metadata is preserved when renaming accounts; no schema migration or execution-routing change was needed.
- Updated the existing browser coverage for compact cards, account creation/editing, authentication changes on focus and polling, local/remote account separation, missing WSL CLIs, and keyboard-accessible dialogs. Reviewed wide native cards and the narrow 840px browser layout. Native inspection uses the isolated WSL Computers QA app, restores its workspace, and sends no sign-in request or provider prompt. Evidence: `artifacts/connections-simplified-native.png`, `artifacts/wsl-computers-native.png`, `artifacts/connections-organized-narrow.png`, and `artifacts/wsl-computers-native-result.json`.
- Final `npm run verify` passed with zero Svelte/TypeScript diagnostics, 59 frontend/relay unit tests, a production build, and all 49 browser scenarios. Formatting, Clippy with warnings denied, and all 60 default Rust tests passed; the three opt-in provider/process tests were not repeated for this UI change. Final native smoke passed at **2026-09-09T18:34:36Z**, with no renderer errors and the isolated workspace restored. Older tests waiting for the removed Connected label were updated to check direct sign-in availability, settled remote status, and actual mock-provider chat completion. Native screenshots now wait for every account check to settle so a pending Gemini check does not stand in for the completed state. Log: `artifacts/connections-simplified-verify.log`.

## Selected computer controls execution — 2026-09-09

- This policy supersedes the historical Windows-first entries below. Desktop uses only its native CLI, including when its folder is in a managed WSL distribution. Selecting a WSL computer uses only that distribution's Linux CLI for existing logins and separate profiles. Missing executables and failed operations never select another computer or account.
- Folder ownership remains stable. An additive `executionEnvironmentId` records Desktop execution for a WSL folder; omitted values retain the folder's environment. Connection validation rejects other computers and sibling distributions. Recent folders, history, sidebar actions, context cache keys, and relay snapshots preserve both identities. Starting chat from a computer card explicitly selects that computer instead of inheriting a recent location on another one.
- Each WSL distribution has its own computer card and installation inventory. Missing CLIs show installation guidance and cannot connect/sign in; failed inventory checks remain unknown or stale. Native executable lookup and Linux PATH inventory are independent of authentication and never read credentials.
- Automated coverage includes restored Desktop/WSL choices for the same Linux folder, Linux-only provider choices, missing installation actions, direct computer-card navigation, invalid location/connection combinations, scoped context caching, and preservation through sync. Rust formatting and Clippy with warnings denied passed, as did all 60 default Rust tests. Both opt-in process fixtures passed: a real Windows PowerShell process opened the selected WSL directory, and the real WSL bridge preserved stdin, profile isolation, Linux cwd, and owned descendant cancellation.
- Final `npm run verify` passed: zero Svelte/TypeScript errors or warnings, 59 frontend/relay unit tests, production build, and all 49 browser scenarios. Log: `artifacts/selected-computer-verify.log`. The final native smoke passed at **2026-09-09T18:20:05Z**. A test initially expected the wrong empty-folder label (`Choose folder`); it was corrected to the existing `Browse folders…` label, and the focused regression plus complete verification then passed.
- Native `npm run tauri dev` QA used a separate Cargo target, app identifier, and WebView profile on CDP 9440. `scripts/wsl-computers-native-smoke.mjs` verified real Windows Codex detection for a WSL folder and missing Ubuntu Codex without fallback; missing Linux CLIs disabled Add account and Send and hid Connect account. The script restored its isolated workspace, sent no provider prompts, and recorded no renderer errors. Visually reviewed `artifacts/wsl-computers-native.png`, `artifacts/desktop-wsl-folder-native.png`, `artifacts/wsl-missing-cli-native.png`, and the complete Ubuntu card capture. Machine-readable results: `artifacts/wsl-computers-native-result.json`.
- Windows Codex, Claude, and Gemini installations and existing login readiness were observed through native queries. Ubuntu currently has no discoverable Linux agent CLI, so a real Linux provider sign-in/reply remains untested. Process fixtures prove routing/cwd behavior; they do not claim a new real-provider reply. The normal development app remains running with the updated code; no installer or release build was produced for this change.

## Header icons and simpler context display

- Added decorative 16px laptop, folder, bot, chip, and brain icons inside the Computer, Folder, Agent, Model, and Reasoning dropdowns through the shared ChoicePicker. Accessible control names, keyboard behavior, full folder paths on hover, and existing-conversation locks are preserved. Adjusted compact toolbar proportions so the icons and typical values fit at 880px.
- Removed the visible Reported labels and arrows from both the context chip and expanded context card. Retained exact measured values, unknown states, hover guidance, and actionable expanded warnings. Empty guidance no longer leaves a divider. Limit indicators now use bare Lucide TrendingDown, MoveRight, and TrendingUp icons with their existing colors, accessible meanings, and hover descriptions.
- Final `npm run verify` passed: zero Svelte/TypeScript diagnostics, 37 unit/HTTP relay tests, production build, and all 34 browser scenarios. Rust formatting, Clippy with warnings denied, and 35 default Rust tests passed; the three opt-in provider/process tests were not repeated for this presentation change. Visually reviewed expanded usage at 1380px and 880px, including unknown and measured context, different quota paces, and enabled/locked selectors (`artifacts/pace-browser.png`, `artifacts/pace-compact.png`, and `artifacts/context-reported-browser.png`).
- Built and opened the final optimized executable with embedded production assets using `npm run tauri build -- --no-bundle --config scripts/native-locations.tauri.json`, preserving the Folder QA identity and data. Native page/IPC verification at **2026-09-09T01:33:05Z** confirmed all five decorative icons, identity locks, no context label/arrow, unchanged saved workspace, and no renderer errors. All five current values fit without truncation, including Desktop QA and Extra high. Restored Overview. Visually reviewed `artifacts/header-icons-native.png`; results are in `artifacts/header-icons-native-result.json`. No provider prompts were sent and no installer was built.
- Native restart observation: an intermediate debug build exceeded both 10-second and 30-second graceful-close waits (`App is still waiting for native cleanup`). Before recovering, rechecked the app identity, idle Overview, absence of running replies/drafts, and the saved-workspace hash. Stopped only that verified debug process, then opened the already-built release and verified the workspace hash again. The close-delay cause remains unverified; the app waits for owned native work during shutdown, so do not assume a successful CloseMainWindow call means the process has exited or open two instances against the same saved data.

## Windows Claude first for WSL folders

- Fixed the reported `Claude needs to be set up` warning for a WSL folder when Claude is installed on Windows. The connection resolver previously returned the selected WSL distribution before checking for the native executable. Existing WSL Codex/Claude logins now resolve Windows first and fall back only to that distribution when the Windows executable is missing. All detection, login, usage, title, model-query, and response calls share this resolver. Separate WSL account profiles remain scoped to their own login; authentication or response errors do not replay on another account.
- The folder and stored connection identity stay unchanged. Before a Windows response, a fixed WSL script receives the Linux folder as a positional argument, validates it by changing directory, and returns its Windows/UNC equivalent. Windows starts in that directory; the Linux fallback retains the original Linux cwd. No project files or credentials are generated/copied. Missing folders and names Windows cannot represent fail before the provider starts. Directory resolution is cancellable before launch.
- Full `npm run verify` passed: zero Svelte/TypeScript diagnostics, 37 unit/HTTP relay tests, production build, and all 34 browser scenarios. Rust formatting, Clippy with warnings denied, and 35 default tests passed. Resolver tests cover Windows winning without a WSL probe, fallback to the selected distribution, missing CLIs, and separate-profile isolation. The new browser scenario covers Windows-reported availability for a WSL folder, no setup warning, ready sending, stable location, restored identity locks, and the sidebar new-chat action.
- Both opt-in process checks passed: `cargo test --manifest-path src-tauri/Cargo.toml real_windows_process_keeps -- --ignored --nocapture` launched a real Windows process in a WSL `/tmp` fixture with spaces, quotes, shell characters, and Unicode, and checked original-location preservation and missing-folder rejection. `cargo test --manifest-path src-tauri/Cargo.toml real_wsl_bridge -- --ignored --nocapture` verified the actual Linux bridge, stdin, cwd, profile isolation, and cancellation. The unrelated real Gemini check was not repeated.
- `node scripts/windows-first-native-smoke.mjs` passed at **2026-09-09T01:08:24Z** in the separate `windows-first-qa` identity on CDP 9434. Native IPC reported Claude installed on **Windows** for an existing WSL connection, the setup warning was absent, and Send was enabled. One real Windows Claude Haiku reply identified **`\\wsl.localhost\Ubuntu\tmp`** as its working directory. No workspace conversations were added; the isolated workspace was restored. No renderer errors. Evidence: `artifacts/windows-first-native-result.json` and `artifacts/windows-first-wsl-ready-native.png` (visually reviewed). Actual Linux Claude authentication/replies were not exercised; fallback selection and Linux process execution were verified separately.
- Built the optimized desktop executable with `npm run tauri build -- --no-bundle --config scripts/native-locations.tauri.json`. The previous Folder QA app was already closed; opened the verified release with its existing app identity/data. At **2026-09-09T01:12:32Z**, the sidebar's **olympus** folder opened a new Claude draft at **`/home/vinicius/olympus`**, detected Windows Claude, and showed no setup warning. Saved conversations were unchanged, and no prompt was sent from this user workspace. Evidence: `artifacts/windows-first-current-app-result.json` and `artifacts/windows-first-current-app.png`. The updated release is running; no installer was built.
- Durable correction: folder ownership identifies the project and machine, not necessarily the CLI's operating system. Keep executable preference separate from directory routing. The previous folder-only rule in `CLAUDE.md` incorrectly overrode Windows-first behavior; guidance and account setup copy now reflect the requested policy. The native CLI-readiness timeout recorded in the preceding change is resolved by this correction.

## Existing conversations keep their computer, folder, and agent

- Computer, Folder, and Agent/account controls now lock once the first submitted message creates a conversation. Mutation handlers enforce the same rule, including settings callbacks and pending folder work; existing conversation locations are never reassigned by the folder picker. The lock also applies after cancellation, reload, and restore, and to legacy conversations. Model, Reasoning, and instructions remain editable between replies. New conversations retain all three choices and the folder's hover path is preserved.
- `npm run verify` passed: zero Svelte/TypeScript diagnostics, 37 unit/HTTP relay tests, production build, and all 33 browser scenarios. Updated scenarios verify locked existing controls, editable new drafts, model changes sent through the original connection and folder, retained earlier reply settings, cancellation, reload, and restore. The legacy context scenario now changes models within its fixed provider. Rust formatting, Clippy with warnings denied, and 34 default tests passed; two opt-in integration tests were not repeated for this UI change.
- `node scripts/folder-selection-native-smoke.mjs --lock-only` passed at **2026-09-09T00:46:20Z** against the running Windows QA app. Actual existing-conversation controls are disabled and do not open menus, Model remains editable, and the sidebar folder action opens an editable new draft. Saved workspace unchanged, no provider prompts, no renderer exceptions, and the original page restored. Visually reviewed the toolbar capture `artifacts/conversation-settings-locked-native.png`; result: `artifacts/conversation-settings-locked-native-result.json`. The change is live in the development app; no installer was built.
- Native QA scope/learning: the optional older timing check reached its CLI-readiness timeout after the lock assertions passed; cold native provider readiness was not established by this run. The dedicated `--lock-only` mode verifies the requested UI behavior without requiring CLI readiness or writing the workspace. Also fixed cleanup's `Control unavailable: #conversation-tab-active` race: `Page.reload` returns before navigation, so the general timing mode now waits for a new document marker and the hydrated tab instead of accepting the old page's tab. The no-reload lock check completed successfully; the general timing mode was not repeated for this change.

## Responsive folder and model selection

- Removed the serial all-connection CLI checks and relay sync from folder selection. Folder acceptance validates the path, persists the selection, and lets missing connection checks finish in the background. Known availability is reused for folders in the same environment; concurrent detection requests are deduplicated. Reselecting the current computer or folder preserves the folder, draft, and settings without another lookup.
- Model catalogs are cached for five minutes per provider and connection configuration. Cached/default and saved model choices remain selectable during requests, including manual refresh. Native queries wait for newly created connections to be saved. Late detection/catalog replies cannot change newer selections, and unknown local availability never enables sending. Offline remote routing remains pinned.
- Full `npm run verify` passed: zero Svelte/TypeScript diagnostics, 37 unit/HTTP relay tests, production build, and all 33 browser scenarios. Three new scenarios deliberately hold CLI replies to verify responsive cold selection, cache reuse, same-computer preservation, model refresh interaction, environment isolation, unavailable explicit choices, and stale results. Rust formatting, Clippy with warnings denied, and 34 default tests passed; two opt-in integration tests were not repeated for this frontend change.
- Real Windows desktop UI/IPC check passed at **2026-09-09T00:34:53Z**, 1380×900 CSS pixels / DPR 1.5. With known CLI/model results, accepting a newly browsed folder made Agent and Model usable in **39 ms**, selecting a saved folder in **35 ms**, and reselecting the same computer in **3 ms**. Model choices remained usable during an actual catalog refresh. These are click-to-controls-ready timings after browsing/validation, not cold CLI discovery or remote-folder latency guarantees. Saved conversations were unchanged, original preferences restored, zero provider prompts sent, and no renderer exceptions. Visually reviewed `artifacts/folder-selection-native.png`; timings are in `artifacts/folder-selection-native-result.json`. Reproduce with `node scripts/folder-selection-native-smoke.mjs`. The frontend is live in the running QA desktop app; no new installer was built.
- Native QA learning: the installed Playwright 1.63 browser-wide CDP attachment asserted on a WebView2 `shared_worker` target without `browserContextId` (`Error: targetInfo: ... type: shared_worker`). The new timing smoke attaches directly to the observed page WebSocket from port 9432, retaining real DOM/IPC and screenshot checks without stopping the app or worker. Wait for the folder browser's **Go** button to become enabled before entering a path; the input can appear while initial directory loading is still pending. The direct page check passed with preferences restored afterward.

## New conversation from a sidebar folder

- Added a visible **+** action beside every actual folder in Active and History, including collapsed folder groups. It starts a fresh Active draft with the exact computer/environment/path, clears the sidebar search, reveals its active group, and focuses the composer. CLI selection shares the same compatible-provider and remembered-account/model/reasoning logic as the folder picker. Existing chats and archived state are not copied or modified. No folder groups without a path expose this action. Offline destinations remain selected with sending disabled; native launch validation still rejects missing directories.
- Full `npm run verify` passed: zero Svelte/TypeScript diagnostics, 37 unit/HTTP relay tests, production build, and all 30 browser scenarios. Expanded the existing folder/history scenario to check Windows versus WSL folders with the same name, keyboard activation, a collapsed History folder, compatible provider selection, empty drafts, first-message routing, and unchanged older conversations. The paired-computer scenario starts a second chat from the sidebar after selecting a different computer, verifies the exact Windows-hosted WSL connection, and proves it did not execute on the initiating Mac fixture. The offline scenario verifies no local fallback. These paired hosts use synthetic CLI IPC and a real HTTP relay, not physical remote hardware.
- Rust fmt, Clippy with warnings denied, and all 34 default tests passed. The two opt-in native provider/WSL tests were not repeated for this frontend change. No native launcher code changed.
- `node scripts/sidebar-native-smoke.mjs --new-chat` passed at **2026-09-09T00:15:37Z** in the existing folder QA app (1380×900 CSS / DPR 1.5). Actual keyboard activation opened a blank draft with the saved computer and folder selected, no folder dialog, and focus in the composer. The saved workspace was unchanged, no provider prompts were sent, and there were no renderer errors. The original page was restored after inspection. Evidence: `artifacts/folder-new-chat-native.png`, `artifacts/sidebar-native-result.json`, and refreshed sidebar preview images. The updated action is live in the running desktop app; no installer or restart was needed.
- QA learning: adding contextual New conversation controls made the older substring selector fail with `strict mode violation` because it matched the global button and the folder action. The global button now has the exact accessible label `New conversation` plus shortcut metadata; tests and native scripts use exact accessible-name matching. Folder controls retain descriptive names including folder and computer. The focused scenarios and final full suite passed after this correction.

## Sidebar hierarchy

- Replaced the stacked Active/History accordions with a compact tab control and counts. Each tab uses muted computer headers, collapsible folder groups, and more readable conversation titles. Removed repeated chat icons and tree guide lines. The selected conversation has a distinct background and a small leading accent; full folder paths remain in hover titles.
- Tabs support Left/Right, Home/End, roving focus, labelled panels, and selected state. Conversation links expose `aria-current`. Archiving or restoring reveals the destination tab and reopens its computer/folder, including a previously collapsed group. Switching status tabs preserves the open conversation. Search filters both sets and updates their counts; empty-state text appears only inside the selected tab.
- Full `npm run verify` passed: zero Svelte/TypeScript diagnostics, 37 unit/HTTP relay tests, the production build, and all 30 browser scenarios. The folder/history scenario now exercises tab keyboard behavior, hidden panels, current-conversation semantics, restoring into a collapsed folder, reload, scoped search counts, and the existing CLI routing/message-preservation behavior. Rust fmt, Clippy with warnings denied, and 34 default tests passed; the two opt-in real-provider/WSL tests were not repeated for this frontend change.
- Synthetic visual QA covers one conversation, two computers with three folder groups, long titles, populated History, and an 880×720 compact layout. Visually reviewed the results. Evidence: `artifacts/sidebar-single-browser.png`, `artifacts/sidebar-multiple-browser.png`, `artifacts/sidebar-history-browser.png`, `artifacts/sidebar-compact-browser.png`, and `artifacts/sidebar-browser-result.json`. Reproduce with `node scripts/sidebar-browser-preview.mjs`; it uses isolated browser storage and sends no provider prompts.
- Read-only native UI verification passed at **2026-09-09T00:04:24Z**, at 1380×900 CSS pixels / DPR 1.5, in the existing folder QA app. Verified tab switching, keyboard focus, computer/folder collapsing and restoration, unchanged open page and saved workspace, and zero renderer errors. No prompts or workspace writes. Evidence: `artifacts/sidebar-native.png` and `artifacts/sidebar-native-result.json`; reproduce with `node scripts/sidebar-native-smoke.mjs`. The frontend update is live in the open app; no native rebuild or installer was needed.
- QA learnings: Svelte flags a `<section role="tabpanel">` as a noninteractive-element/interactive-role mismatch; use a focusable `<div role="tabpanel">` with its tab label. For reliable selection screenshots, disable finite CSS transitions during capture and move the pointer off folder controls, so a hover background is not confused with the current conversation.

## Selected folder is the CLI working directory

- Fixed a mismatch reported after selecting `agent-studio`: the reply identified `chat-runtime` because folder metadata was used for routing/grouping but never applied by the process launcher. Native providers now start in the selected directory. The WSL bridge receives the Linux path as a separate argument and changes directory inside that distribution. Unavailable folders fail without starting the provider or substituting another directory. Legacy chats without a folder and title generation keep their runtime directories.
- Permission design retains the existing conversational restrictions: Codex read-only/shell-and-patch disabled, Claude tools disabled, and Gemini wildcard tool denial. Generated Gemini agent files/hooks remain in app data; they are never written into the selected project. Antigravity print mode also needs the selected folder registered with `--add-dir`, before the separate runtime workspace. Setting only process cwd left it unable to identify the selected folder; the installed CLI help confirms repeatable workspace arguments, and a real adapter reply verified the correction.
- Regression coverage checks each native adapter's cwd, unchanged restriction flags, no generated project files, missing/file/relative-path rejection, legacy runtime behavior, and literal WSL argument boundaries. The real WSL integration test passed, including actual cwd with spaces, quotes, Unicode and shell characters, failure before launch for missing paths, and existing cancellation/profile checks.
- `npm run verify` passed with zero Svelte/TypeScript diagnostics, 37 unit/HTTP relay tests, a production build, and all 30 browser scenarios. Final Rust formatting, Clippy with warnings denied, and all 34 default tests passed. Both opt-in tests (real WSL bridge and real Gemini selected workspace) passed separately. No remote routing protocol changed; a physical Mac or new relay deployment was not exercised.
- Native UI/IPC smoke at **2026-09-08T23:46:33Z** selected the repository through the actual folder picker: real Codex and Claude replies reported that selected path, missing-folder IPC was rejected, and there were no renderer errors. The temporary QA workspace was restored. Evidence: `artifacts/selected-cwd-native.png` and `artifacts/selected-cwd-native-result.json`. That first run exposed Gemini's extra workspace-registration requirement (the artifact correctly records its initial false result); the final adapter was then verified by the opt-in `real_gemini_reports_selected_workspace_with_external_runtime_hooks` Rust test using temporary directories and a real Gemini reply. It passed and the project directory remained empty.
- `scripts/antigravity-permissions-smoke.mjs` now tests an external runtime and a separate selected project. The real `write_to_file` attempt was denied by the generated hook, the process finished successfully, and the target remained absent. Evidence: `artifacts/antigravity-permissions-smoke.json`. Pass an optional runtime path to test another QA identity's generated hook. The native UI smoke now requires each available provider to report its selected directory.
- Durable QA learning: a real reply that only echoes a routing marker does not establish the process working directory. Ask for the directory from session context without including the expected path in the prompt, and separately test the launch cwd and workspace registrations. On Windows, `rg scripts/*.mjs` treats the wildcard as a literal path; use `rg -g '*.mjs' scripts`.
- The original folder QA and separate cwd QA processes were subsequently observed closed, so the final native binary could be rebuilt without stopping either app. Reopened the original `com.vinicius.agentstudio.locations-qa` identity with its existing WebView profile and saved data. At **2026-09-08T23:52:15Z**, the final app rendered and native IPC confirmed the repository folder was available; this read-only check sent no prompts or workspace writes. Evidence: `artifacts/selected-cwd-reopened.json`. The final folder QA app and Vite remain running. No installer or regular-user data migration was performed.

## Inline conversation toolbar

- Computer, Folder, Agent, Model, and Reasoning now occupy one row, including at 880px width. Removed the separate Run on control and persistent full-path text. The folder trigger and saved-folder options expose paths through hover titles. The top bar contains only the page/conversation title; Workspace breadcrumbs, Local desktop, and version labels are absent.
- Providers with multiple available account connections expose those accounts inside Agent. The existing paired-computer browser scenario selects a named account there and verifies that replies still run on its exact remote connection.
- Final `npm run verify` passed with zero type/Svelte diagnostics, 37 unit/HTTP relay tests, the production build, and 30 browser scenarios. Rust fmt, Clippy, and 32 default tests passed; the pre-existing opt-in WSL process test remains ignored. The folder scenario checks one-row geometry at desktop/compact widths and hover-only paths. Its first tooltip assertion selected by list position; it now identifies the WSL environment because recent-folder ordering places the most recent folder first.
- Native verification passed at **2026-09-08T23:33:08Z**, at 1380×900 CSS pixels / DPR 1.5, in the existing isolated folder QA app. Verified all five controls on one row, title-only top bar, absence of Run on, hover-path metadata, and saved state restored after a temporary conversation preview. No provider prompts were sent. The workspace had no conversations, so the check used an owned temporary preview instead of assuming an earlier smoke conversation still existed. Evidence: `artifacts/inline-chat-toolbar-native-result.json`, `artifacts/inline-chat-toolbar-native.png`, `artifacts/inline-chat-folder-menu-native.png`, and the desktop/compact `folder-groups` screenshots. No native routing code or packaged installer changed.

## Computer and folder selection; Active and History

- New chats choose a computer and then a folder. Native directory browsing supports Windows and managed WSL, with remote browsing routed through the selected computer’s relay host. Folder selections pin environment connections and filter provider/account choices. Existing recent folders are checked again before reuse. An offline/missing environment cannot fall back to the initiating computer. Provider/model/reasoning choices and historical replies remain per conversation.
- The sidebar splits Active and History, each nested by computer and folder. Archive/restore persists and syncs without removing messages; archived controls are locked. Location/archive changes are included in conflict handling. Legacy chats retain their owner under No folder.
- Permission scope: folder browsing reads directory names only. Chat providers continue running in the isolated app runtime with tools disabled. This does not add project file access or enable running tools in the selected folder.
- Final `npm run verify` passed: zero Svelte/TypeScript errors or warnings, **37 unit/HTTP relay tests**, production build, and **30 Edge browser tests**. Rust formatting, Clippy with warnings denied, and **32 Rust tests** passed; the pre-existing opt-in WSL process integration test remains ignored. Coverage includes different CLI inventories per environment, invalid folder paths, offline computers, exact remote/WSL routing, persistence, archive/restore, search by folder, conflict preservation, and desktop/compact layouts.
- Native smoke: `scripts/locations-native-smoke.mjs` passed at **2026-09-08T23:07:53Z** in the separate `com.vinicius.agentstudio.locations-qa` app identity (CDP 9432). It verified actual Windows and Ubuntu directory listings with spaces, quotes, and literal shell characters; a real Windows Codex GPT-5.6-Sol/Low reply; archived history after reload; restored messages; environment-filtered CLIs; and native rejection of a Windows connection paired with a WSL folder. No renderer errors. Evidence: `artifacts/locations-native-result.json`, `artifacts/folder-picker-native.png`, `artifacts/folder-conversation-native.png`, `artifacts/folder-wsl-native.png`, `artifacts/folder-groups-browser.png`, and `artifacts/folder-groups-compact.png`.
- QA learnings: adding optional schema fields can change JSON key order after restoration. A byte-string comparison reported changed storage although every saved value was identical; the preservation assertion now compares parsed objects. The native model label is `GPT-5.6-Sol`, while the synthetic browser fixture uses `GPT-5.6 Sol`; inspect the real option list before choosing native smoke selectors. Native refresh can be slower than rendering after reload; show Checking while status is unknown and wait for ready controls before capturing the settled UI.
- Final native UI verification at **2026-09-08T23:11:59Z** confirmed ready controls after reload, all archived configuration controls disabled, cancellation preserving the chosen folder/settings, and unchanged messages. No additional provider prompts were sent. Evidence: `artifacts/locations-native-final-result.json` and `artifacts/folder-history-native-final.png`.
- Native WSL provider responses, physical macOS, public relay deployment, and a packaged installer were not repeated. The Windows provider reply and WSL directory browsing were real; cross-computer end-to-end checks use separate browser contexts, a real HTTP relay, and synthetic CLI IPC. The isolated native QA app and development server remain running. Restart older native app builds to load `list_folders`.

## Computer-first Connections and login explanations

- Connections now lists computers first. Each computer contains its environment inventory, provider CLIs, and only the account connections belonging to it. Shared account labels remain shared; their settings editor is scoped to the selected card. Disconnected labels remain accessible under Accounts without a computer. Add-account dialogs identify their target computer and offer only its locally managed environments.
- Added explanations for existing login (shares the CLI's current sign-in and settings) and separate login (same installed CLI, independent configuration and sign-in). The explanation changes with the choice and environment; Gemini explains its existing-login-only restriction. The underlying profile and relay behavior was preserved.
- Final `npm run verify` passed: no Svelte/TypeScript errors or warnings, 34 unit/HTTP relay tests, production build, and **28 Edge browser tests**. Rust fmt, Clippy with warnings denied, and 29 default tests passed. Browser coverage includes two computer cards with different account memberships, one shared label connected on both, editing only the selected card, offline-host controls, connecting a remote label locally, orphan removal, duplicate-login rejection, Windows/WSL grouping, and responsive layout.
- The first browser run exposed an ambiguous Cancel selector in the new scenario and two unrelated 5-second startup timeouts under 16 workers. The selector now targets the dialog. The unchanged startup checks passed individually; limiting Playwright to four workers produced a full green run in 17.3 seconds without increasing assertion timeouts.
- `node scripts/connections-native-smoke.mjs` passed at **2026-09-08T22:42:07Z**, confirming native computer-scoped controls, both login explanations, design-system dropdowns, Escape/focus restoration, no renderer errors, and preserved fleet/conversations after cancelling. Evidence: `artifacts/connections-native-result.json`, `artifacts/connections-computers-native.png`, `artifacts/connections-existing-login-native.png`, and `artifacts/connections-account-dialog-native.png`. The two-computer browser view is `artifacts/connections-computer-list-browser.png`.
- Remote profile creation/sign-in remains an action on the owning computer; this change does not add remote interactive login. Provider replies, a physical MacBook, and VPS deployment were not repeated for this UI change. Development services remain running; no installer was produced.

## Connections page organization

- Reorganized the page around one group per provider, with each saved account listed once and its environments underneath. Computers and sync have their own adjacent panels. Account creation, computer naming/grouping, and relay pairing now open contextual dialogs. CLI installation instructions, account maintenance, and privacy details are disclosed when needed. All dropdowns remain `ChoicePicker` controls.
- Final `npm run verify` passed: zero Svelte/TypeScript errors or warnings, 34 unit/HTTP relay tests, a production build, and 27 Edge browser tests. Rust formatting, Clippy with warnings denied, and 29 default Rust tests also passed (the existing opt-in WSL process integration test was not repeated for this frontend change).
- The new browser scenario covers three separate Claude accounts, one account connected on Windows and Ubuntu without another account row, computer rename/cancel and persistence, duplicate existing-login rejection, keyboard focus restoration, contextual forms, and no horizontal overflow at the 840×640 minimum window size. Existing relay, WSL sign-in/run/stop, provider login polling, and conversation tests remain green.
- `node scripts/connections-native-smoke.mjs` passed at **2026-09-08T22:18:53Z** in the isolated Windows QA app. Real inventory/status results rendered before screenshots. Account, computer, and sync dialogs opened; custom dropdown Escape handling and focus return passed; cancelling preserved the saved fleet and conversations. No renderer errors or provider prompts. Evidence: `artifacts/connections-native-result.json`, `artifacts/connections-organized-native.png`, `artifacts/connections-account-dialog-native.png`, `artifacts/connections-computer-dialog-native.png`, and `artifacts/connections-sync-dialog-native.png`. Browser evidence includes `artifacts/connections-organized-browser.png` and `artifacts/connections-organized-narrow.png`.
- Vite and the isolated development app remain running. This UI change did not create an installer, exercise a physical MacBook/VPS, or repeat real provider replies. Earlier sections record those backend checks and their boundaries.

## Direct WSL execution and unified Connections

- Final `npm run verify` passed: zero Svelte/TypeScript errors or warnings, 34 unit/HTTP relay tests, static production build, and 26 Edge browser tests. Rust formatting, Clippy with warnings denied, and 29 default Rust tests passed. The additional opt-in real WSL integration test passed separately.
- Browser scenarios cover a single Connections navigation item with the Plug icon, retained provider setup, shared design-system dropdowns, selecting a WSL account on Windows, sign-in routing, chat/stop without relay pairing, and a remote Mac fixture reaching a WSL connection through the Windows host over a real HTTP relay. The two existing source/host cancellation scenarios remain green.
- Rust resolver tests prove Windows-first selection without probing WSL when the native executable exists, fallback when it is missing, and pinned Windows/WSL account choices. Errors never replay a submitted response on another account.
- `cargo test --manifest-path src-tauri/Cargo.toml real_wsl_bridge -- --ignored --nocapture` ran real Bash processes inside Ubuntu. It verified literal Unicode/metacharacter stdin, Linux-local isolated profile paths, terminating both a process and its descendant, and cancellation before the wrapper had started. The test uses synthetic processes, submits no provider prompt, and removes its own temporary Linux namespace.
- `node scripts/wsl-bridge-native-smoke.mjs --reply` passed against the isolated native QA app at 2026-09-08T20:07:30Z. Automatic Codex selected **Windows** and returned the real reply `windows bridge ready`. The reply retained its Windows execution label. A temporary WSL account was checked through native IPC and removed afterward, preserving other accounts and chats. Ubuntu's Linux Codex CLI was not discoverable, and the app reported it as missing. No renderer errors. Evidence: `artifacts/wsl-bridge-native-result.json`, `artifacts/connections-unified-native.png`, and `artifacts/windows-first-native-reply.png`.
- Actual Linux Codex/Claude authentication and provider replies remain unverified because those Linux CLIs were not found in this Ubuntu environment. The native WSL process bridge itself is verified. A physical MacBook, VPS deployment, and a new installer were not exercised. Development services remain running.
- A browser preview assertion timed out when a native compilation overlapped cold browser startup; the failure snapshot subsequently showed the expected banner. Completing native builds first and rerunning the full unchanged application suite against the persistent Vite server passed. Native smoke selectors now account for a real model option such as `CLI default · GPT-6-Astra`, rather than assuming the fixture's exact `CLI default` text.

Use the isolated QA app setup below. Run `node scripts/wsl-bridge-native-smoke.mjs` for detection/UI checks only; add `--reply` for one real Codex response. Older sections below record the preceding implementation; the previous requirement for a WSL app instance has been replaced by the direct Windows bridge.

## Automatic WSL discovery and design-system dropdowns

- `npm run verify`: passed with zero Svelte/TypeScript errors or warnings, 34 unit/HTTP relay tests, production build, and 24 Edge browser tests. Rust formatting, Clippy with warnings denied, and all 27 Rust tests passed.
- The new browser scenario covers automatic running/stopped WSL entries under one Windows computer, stable entries across refresh/reload, retained inventory on discovery failure, state recovery, keyboard and pointer selection, account creation/editing, and zero native `select` controls. Unit tests cover reconciliation when the WSL app joins the same computer and prevent merging distributions from different computers.
- `node scripts/wsl-native-smoke.mjs` passed against the isolated Windows QA app at 2026-09-08T19:36:10Z. Actual Rust IPC detected this machine's **Ubuntu** distribution as running, returned stable IDs on repeated scans, saved the inventory without creating an executable account connection, and rendered the shared picker with working Escape/focus behavior. No renderer errors. Evidence: `artifacts/wsl-native-result.json`, `artifacts/wsl-discovery-native.png`, and `artifacts/wsl-picker-native.png`.
- Discovery was verified on Windows against real WSL. Native Linux execution and WSL-to-relay pairing remain untested. Discovery does not launch or authenticate the WSL app. The updated development QA app and Vite server remain running; no new installer was built.
- The mandatory shared-dropdown convention is recorded in `CLAUDE.md`. Form pickers use the full-width field variant of `ChoicePicker.svelte`.

Repeat the native check using the isolated app setup below, then run `node scripts/wsl-native-smoke.mjs`. This check preserves existing chats and accounts and does not submit provider prompts.

## Multi-computer and account relay (workspace v3)

Final checks for the multi-computer implementation:

- `npm run verify`: passed, with zero Svelte/TypeScript errors or warnings, 31 unit/HTTP relay tests, a static production build, and 23 Edge browser tests.
- `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test`: passed; 25 Rust tests, including concurrent profile scoping and relay URL restrictions.
- The two-environment browser test uses a real HTTP relay with separate isolated browser contexts and synthetic CLI IPC. It verifies pairing, shared account labels, exact host/account routing, progress, result retention, stopping from both source and execution host, and no duplicate conversations across follow-up turns. Browser fixtures do not establish physical Mac/WSL or provider login support.
- Native Windows QA used a separate Cargo target, WebView2 profile, and Tauri app-data identifier. An actual HTTP relay request was accepted by the native host and produced a real Codex reply, `native relay ready`, using its existing CLI login. The host saved the completed result. A fresh isolated Claude profile reported `login`, and a provider/connection mismatch was rejected. No renderer page errors were observed. Evidence: `artifacts/native-federation-result.json` and `artifacts/native-federation-chat.png`.
- The new profile directories were not authenticated by the agent. Completing real Claude personal/work sign-ins remains a user action. macOS/WSL native execution, Docker image startup, public TLS/reverse-proxy configuration, and VPS deployment have not been run here. Docker is unavailable in this Windows shell.
- A new packaged release/installer was not built for this change. Start/restart the development app to load the new Rust commands. The pre-existing running app was not terminated; QA used its own app-data identity.

To repeat the opt-in native check (one small real Codex prompt), first run `npm run dev` in one terminal. In another:

```powershell
$env:CARGO_TARGET_DIR = Join-Path $PWD 'artifacts/native-federation-target'
$env:WEBVIEW2_USER_DATA_FOLDER = Join-Path $PWD 'artifacts/native-federation-webview'
$env:WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS = '--remote-debugging-port=9431'
npm run tauri dev -- --config scripts/native-federation.tauri.json --no-watch
# A third terminal:
node scripts/federation-native-smoke.mjs
```

The script rejects a QA workspace containing unrelated conversation titles and never targets the regular app-data identifier. The script's synthetic HTTP relay closes after its assertions; the native QA app and development server remain open. See [MULTI-COMPUTER.md](MULTI-COMPUTER.md) for user setup and relay deployment.

Windows QA fixes: opening the relay's temporary file with `r+` is necessary before `fsyncSync`; flushing a read-only handle failed the first HTTP save. Vite must ignore `artifacts/` because watching a live Cargo build executable produced `EBUSY` and killed the dev server. A stale reused Vite optimizer initially served `504 (Outdated Optimize Dep)`; restarting with `npm run dev -- --force` restored normal operation. Builds and browser checks remain serialized. Earlier verification below describes preceding features and builds.

## Automated checks

- Svelte / TypeScript check: zero errors and zero warnings.
- Vitest: 20 passing domain/persistence and usage tests, including workspace migration, remembered choices, reported context, model-specific capacity, quota pace calculations, reset/freshness boundaries, and consecutive measured context growth.
- Playwright: 22 passing browser tests using Edge and controlled Tauri IPC fixtures, including chat configuration, sign-in status, asynchronous titles, Fable-only limits, quota pace guidance, measured-context growth/persistence, separate draft warnings, and missing/stale readings.
- Rust: 23 passing tests for provider event decoding, failed outcomes, prompt validation, retired-CLI rejection, Antigravity stdin, login-command quoting, account status, model catalogs, reasoning arguments, title requests, account quota parsing, and request-level context/cache token semantics.
- `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check`: passing.
- `npm audit`: zero known vulnerabilities. A compatible `cookie` override avoids an advisory in SvelteKit's development dependency tree.
- Static Svelte build: passing, with bundled fonts and icons.
- Windows release executable and NSIS installer: built successfully. The packaged app returned a real Codex reply and passed reload, persistence, and CSP checks after allowing bundled data-URL fonts.

## Real Windows runtime

Tested in the native Tauri WebView2, through the actual Rust process bridge:

| Check                                          | Outcome                                                                                                    |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| Codex 0.153.4 with existing ChatGPT login      | Reply and multi-turn context verified                                                                      |
| Claude Code 2.1.263 with existing Claude login | Reply and multi-turn context verified                                                                      |
| Gemini through Antigravity CLI 1.1.27          | Google sign-in completed; real reply, follow-up context, and Connected status verified in the packaged app |
| Real cancellation                              | Stopped a Codex run after its startup event; backend acknowledged cleanup                                  |
| Rich response                                  | Claude response rendered headings, bullets, a table, and a code block                                      |
| Persistence                                    | Native messages retained after a WebView reload                                                            |
| Export                                         | Native JSON export created in Downloads                                                                    |
| Renderer runtime                               | No page errors observed in provider smoke tests                                                            |

Evidence is in ignored `artifacts/`: native provider screenshots, `native-rich-chat.png`, `native-behavior.json`, `release-overview.png`, `release-smoke.json`, and smoke results. Synthetic test conversations were removed from the live workspace after verification. Mock tests do not establish real provider authentication.

## Sign-in launcher correction

The initial direct CLI launch closed immediately without a persistent interactive shell. Routing PowerShell through an existing Windows Terminal process then produced `-File ... does not exist` and `MODULE_NOT_FOUND`, even though those files were accessible to the app and command tools.

The launcher owns a dedicated `conhost.exe` process, launches PowerShell with `-NoExit -EncodedCommand`, and quotes every executable/path argument as a literal. This removes the temporary-script dependency and the existing-terminal handoff.

## Gemini subscription migration

The user's subsequent screenshot showed browser authentication succeeded, followed by “This client is no longer supported for Gemini Code Assist for individuals.” The original login-needed diagnosis was incomplete: Google [retired individual Gemini CLI access on June 18, 2026](https://github.com/google-gemini/gemini-cli/discussions/28017). Surviving console processes did not establish usable authentication.

Installed Antigravity CLI 1.1.27 using Google's installer with its SHA-512 check. The user completed its Google sign-in. The Gemini adapter now resolves `agy`, including `%LOCALAPPDATA%/agy/bin` before a PATH refresh, uses its NDJSON input and output protocol, and never falls back to the retired CLI. Workspace version and provider IDs are unchanged, preserving existing data.

Live packaged-app evidence: `artifacts/gemini-release-smoke.json` and `gemini-release-chat.png`. Gemini replied “Noted.”, then correctly recalled “maple” in a follow-up; the Connections badge changed to Connected. Synthetic conversations from this check are removed using a unique run marker; the user's other conversations are preserved.

Tool restrictions required a runtime check. An empty primary-agent tool list did not remove the advertised tool set. More importantly, `current_dir` alone did not load workspace hooks: `agy -p /hooks` returned an empty list, and a benign test write succeeded. Adding `--add-dir` for the app-owned directory loaded the hook. With the wildcard `PreToolUse` denial active, a real `write_to_file` attempt was blocked and its target remained absent. The opt-in `scripts/antigravity-permissions-smoke.mjs` repeats this check against the hook generated by the running app. No global credentials or permission settings are edited.

For WebView2 QA, an existing profile coincided with a blank/unresponsive startup during this session. A separate `WEBVIEW2_USER_DATA_FOLDER` allowed intermediate packaged-app verification. The final executable was then restarted with the normal profile and passed the real Gemini reply and follow-up checks again at 14:17 UTC. Authentication remains in the external CLI and was unaffected. The temporary profile workaround is not evidence of a general production startup fix.

## Sign-in status correction

The migrated adapter could chat, but detection still returned `unknown` for Gemini. Only a completed chat set `ready`, and Refresh or restart reverted that status. This left Connections showing Installed after a successful CLI login. The previous real-chat smoke did not cover this acceptance condition.

Detection now runs `agy --print /usage --output-format json` with closed stdin and a 20-second timeout. A live CLI probe returned `status: SUCCESS`, `command: usage`, zero turns, and zero input/output model tokens. The app requires both a successful process exit and that specific response before showing Connected. It stops at the headless authentication-required message and distinguishes an unverified connection from a signed-out account. Raw account output and credentials are not persisted or displayed.

The UI refreshes on startup, window focus, and visibility changes. After Open sign-in it polls every five seconds for up to ten minutes, so it does not depend on the interactive CLI exiting. Browser regressions verify that launching a console alone does not mark an account connected, and that completing sign-in updates the badge without a chat.

Opt-in native check: `node scripts/login-status-smoke.mjs` with the packaged app on the QA CDP port. It verifies Connected before a chat, after Refresh, after reload, and after focus, while asserting the saved workspace is unchanged.

The rebuilt release passed this check with the normal WebView2 profile at 14:31 UTC: Gemini showed Connected before sending any chat, and remained Connected after manual Refresh, reload, and focus. The workspace was unchanged and no page errors occurred. Evidence: `artifacts/login-status-smoke.json` and `artifacts/login-status-connections.png`. Signed-out behavior was covered by parser and browser fixtures; the user's live CLI account was not logged out for testing.

## Agent picker appearance

Replaced the conversation toolbar's native select with a Svelte combobox and styled HTML listbox. The menu matches the dark theme, shows provider icons and model details, and marks the current selection. Existing conversations retain their original agent.

Verified the rendered menu in Edge at 1380 and 880 pixels wide and in the rebuilt Windows release with the normal WebView2 profile. Mouse selection, arrow keys, Home/End, Enter, Escape without changing selection, typeahead, outside-click dismissal, and Tab focus movement passed. The native workspace was unchanged; no model prompts were sent and no page errors occurred. All 24 existing automated tests, Svelte checks, Rust formatting and Clippy passed. Evidence: `artifacts/agent-picker-native.png`, `agent-picker-native.json`, `agent-picker-browser.png`, and `agent-picker-compact.png`.

## Chat configuration replaces preset agents

The separate agent library is removed. Every conversation has Agent, Model, and Reasoning selectors in the chat header, editable between replies. Latest model is stored per provider; reasoning is stored independently per provider/model, including the Default selection. Reopening a chat preserves its own settings. Each reply carries its generating configuration, so changing providers does not relabel historical messages. Optional instructions are scoped to a chat.

Codex model metadata comes from its installed app-server `model/list`. Gemini model variants from `agy models` are grouped by base model and supported effort. Claude choices use its documented aliases and supported effort values; its account/organization policy can still restrict availability or cap effort. References: [Codex model listing](https://learn.chatgpt.com/docs/app-server#list-models-modellist), [Claude model and effort configuration](https://code.claude.com/docs/en/model-config), [Antigravity model and effort selection](https://www.antigravity.google/docs/cli/headless/).

Workspace v2 migration preserves all existing chat IDs, messages, instructions, and authors. Retired presets are retained in `legacyAgents`; the first desktop save also backs up the exact v1 file to `workspace-v1-backup.json`. Browser preview remains on its existing storage key so prior preview data can migrate too.

The packaged app passed `scripts/chat-settings-smoke.mjs` at 15:00 UTC. One synthetic conversation received real replies from Codex / GPT-5.6 Sol / Low, Claude / Sonnet / Low, and Gemini / 3.8 Flash / High. Both provider switches retained the earlier context. Configurations reached the CLI, were saved with each response, and the latest choices survived new-chat creation and reload. The original chats were unchanged after migration, the v1 backup matched the original bytes, and the test restored the user's preferences and removed its unique synthetic chat. Evidence: `artifacts/chat-settings-smoke.json`, `chat-settings-native.png`, `chat-settings-browser.png`, and `chat-settings-compact.png`. No page errors were observed. Other model/effort combinations were not exhaustively tested against live accounts.

The final package also passed a read-only startup/model-picker/reload check after the model-error wording was updated to point to the chat header. Evidence: `artifacts/chat-settings-final-check.json` and `chat-settings-final.png`. It was reopened normally without a debugging listener.

## Automatic titles

Each new conversation makes one background title request from the first 2,000 characters of its first message. Fixed small-model choices are Codex GPT-5.6 Luna / Low, Claude Haiku, and Gemini 3.8 Flash / Low. The Rust adapter retains all chat tool restrictions and uses a separate runtime directory, cancellation registry, 30-second deadline, and bounded output. Failure preserves the first-message excerpt. Restarts do not retry unfinished title requests; existing conversations are not renamed. Deleting a chat cancels its title job, and a late result cannot recreate it. Window close also cancels title processes.

Browser regressions verify the chat can finish before its title, generated names survive reload, provider/model/reasoning preferences stay unchanged, follow-up messages do not generate another title, failures preserve working chats, and late results remain tied to the original conversation. Domain restoration converts interrupted title jobs to fallback. Rust checks validate fixed model selection, Unicode-safe context limits, and short single-line output.

Build-order finding: do not run browser checks against the reused Vite server while the Tauri frontend build rewrites `.svelte-kit`; that triggers unexpected full-page reloads in tests. The unchanged browser suite passed after serializing those operations. The durable finding was also recorded in the local memory notes.

The packaged release passed `scripts/titles-smoke.mjs` at 15:32 UTC. A real Codex/Sol conversation was named **Balcony Herb Garden Planning** by Luna in parallel with its reply, retained its Sol/Low settings, and kept the title after reload. Direct native title requests also succeeded with Claude Haiku and Gemini 3.8 Flash/Low, both returning Portuguese titles from a Portuguese first message. Original conversations were unchanged, preferences were restored, and the unique synthetic chat was removed. Evidence: `artifacts/titles-smoke.json`, `titles-native.png`, and `titles-verify.log`. All 36 tests passed; no native page errors occurred. The screenshot was taken just after reload, before provider detection completed, so its transient setup label is not an authentication result.

## Usage and context tracking

The expandable chat strip reports account 5-hour/weekly limits, reset countdowns, and the separate Fable weekly limit only for Fable (including a resolved CLI default). Hidden read-only CLI queries refresh on selection, once a minute while the chat is visible, after replies/titles, and on returning to the app. Missing windows remain unknown, failed queries preserve old readings, and elapsed reset times require a new reading. No API keys, token-file access, or model generation are used for quota refreshes. See [data sources and context semantics](USAGE.md).

Native verification through the packaged WebView2 passed at 16:12 UTC with all three existing subscriptions. Codex reported its general weekly bucket and a distinct Spark 5-hour/weekly bucket; its general 5-hour window was absent. Claude returned 5-hour, weekly, and server-labelled Fable weekly readings, plus a one-million-token Fable capacity. Gemini returned a weekly window and no 5-hour window. The app keeps these omissions and model scopes intact. Fable's card disappeared when switching to Sonnet.

Small synthetic conversations with Codex/Luna, Claude/Haiku, and Gemini/3.8 Flash produced real input/output counts, calibrated context estimates, and persisted metrics after reloading. Claude's last input and 200,000-token Haiku capacity also arrived in its response. Cached input remained a subset of total input. The original chats and preferences were restored, synthetic conversations removed, and no native page errors were observed. Evidence: `artifacts/usage-smoke.json`, `usage-fable-native.png`, and `usage-{codex,claude,gemini}-native.png`. The final frontend also preserves unknown input/output independently when only one count is supplied; the full 50-test suite passed after this adjustment.

Browser tests cover missing quotas, refresh failure with preserved readings, provider-switch races, model-specific capacity, draft growth, and reload persistence. Context occupancy is explicitly an estimate of the next replayed request, not an exact tokenizer reading or a sum of historical billed tokens. The first reply calibrates otherwise-unmeasured CLI overhead. Unknown capacity and unmeasured older replies are labelled accordingly.

The final rebuilt installer also passed `scripts/usage-final-check.mjs`: live Fable meter, draft-dependent context growth, unknown empty totals, Fable visibility when switching models, and exact workspace preservation. It sent no model prompts and produced no page errors. Evidence: `artifacts/usage-final-check.json`, `usage-final-native.png`, and the refreshed installer SHA-256 in `build-manifest.json`. The app was then closed normally and reopened with its regular profile and no debugging listener. Durable CLI schema and missing-data findings were recorded in the user's local memory notes.

## Reported context correction

The initial usage implementation placed a next-request estimate in the primary context meter. After the user asked for Claude's actual count, the main meter now uses the last compatible response's `contextInput` and reported window. It no longer requires a saved local prompt estimate, changes while typing, or substitutes cumulative billing totals. The response's measured window takes priority over later catalog data. Changing provider/model/reasoning/instructions requires a matching new measurement. A pending reply keeps the previous reading; a later completed reply without context data clears it. An explicit zero is preserved.

Claude's request count includes uncached, cache-read, and cache-creation input once, matching its documented status-line semantics. The parser clears its previous request count if the latest assistant message omits usage. The current Codex exec and Antigravity result adapters still expose aggregate totals; their main context meter says **Not measured yet**. This is a current adapter boundary, not a claim that those providers cannot expose context. A separate labelled next-request forecast includes the draft and remains available for all providers.

All 56 automated tests passed: 13 Vitest, 20 browser, and 23 Rust. Browser checks assert exact percentages, draft independence, model-change invalidation, pending and missing readings, saved token persistence, and no cache double-counting. Svelte/TypeScript, static build, formatting, and Clippy also passed. Evidence: `artifacts/context-verify.log`, `context-reported-browser.png`, and `context-reported-compact.png`. The earlier usage section records historical verification of the estimate-based UI; this section describes the current behavior.

The rebuilt packaged app passed live verification at 16:34 UTC. Claude/Haiku reported 3,665 input tokens and a 200,000-token window; the main meter matched that ratio exactly, stayed fixed while a draft changed the separate estimate, and retained the same count after reload. Codex/Luna and Gemini/3.8 Flash both completed real replies and retained their token totals while their unmeasured main context stayed unknown. Live subscription readings and Fable-only visibility still passed. The final read-only check verified that a new Fable chat stays unmeasured even while a draft forecast grows. No native page errors occurred; original chats and preferences were preserved and all synthetic chats removed. Evidence: `artifacts/context-smoke.json`, `context-{claude,codex,gemini}-native.png`, and `usage-final-check.json`. The normal app was reopened after QA with no debugging listener. The context-display correction was recorded in local memory notes.

## Quota pace and context guidance

Quota pace compares used percentage with the elapsed share of the reported window, aligned to the reading's timestamp. A five-percentage-point tolerance defines **On track**; lower readings show **Below pace**, higher readings **Ahead of pace**, and 100% **Limit reached**. Guidance includes a marker on the usage bar, projected use at reset after 5% of the window has elapsed, remaining hourly/daily allowance, and advice to ease usage when ahead. Stale, failed, missing, expired, and inconsistent readings withhold the pace classification. The guide explicitly assumes evenly spread use over the full window.

Context guidance uses actual measured room and up to five consecutive growth increments, requiring three compatible readings. It stops at missing data, changed settings/model/window, or a decrease. Room/watch/near-full/full thresholds use measured percentages; fast growth warns when about three or fewer similar exchanges would reach 80%. Draft estimates have separate warnings and never move measured context or its status. These calculations use existing readings and saved replies without extra provider queries.

All 65 automated tests passed, along with Svelte/TypeScript, static build, Rust formatting, and Clippy. The browser suite checks all three pace labels, reduction/allowance text, Fable scope, withdrawal of guidance on refresh failure, responsive layout, context growth after reload, and independent draft warnings. Evidence: `artifacts/pace-verify.log`, `pace-browser.png`, and `pace-compact.png`. Source semantics and examples are documented in [USAGE.md](USAGE.md).

The final packaged release passed live checks at 17:39 UTC. Its independently calculated Claude five-hour and weekly labels were On track, while Fable's separate weekly quota was Ahead of pace. Three Haiku replies reported 3,668, 3,728, and 3,788 context tokens, producing a measured growth trend of 60 tokens per exchange. The exact context ratio and guidance stayed fixed while typing and survived reload. Distant projections use qualitative wording beyond 20 exchanges instead of implying thousands of available turns. The original workspace and preferences were preserved, synthetic chats removed, and no page errors occurred. Evidence: `artifacts/pace-smoke.json`, `pace-quotas-native.png`, and `pace-context-native.png`.

Native QA now waits for packaged-page navigation after CDP connection; attaching during `about:blank` previously caused `Packaged app not found`. The startup race and guidance semantics were recorded in the user's local memory notes. The rebuilt executable and NSIS installer are recorded in `artifacts/build-manifest.json`. The final app was reopened with its normal profile, responded normally, and had no QA debugging listener.

## Usage panel readability

On track now uses blue, Below pace green, Ahead of pace amber, and exhausted limits red. Stronger badge borders and matching quota bars make the states easier to distinguish. The default view retains current usage, reset countdowns, remaining budgets, actionable warnings, and a single next-request estimate row. Explanations, projections, detailed growth, and saved token totals are behind a native keyboard-accessible Details disclosure, closed initially.

All 65 tests passed after the presentation changes, along with Svelte/TypeScript, static build, Rust formatting, and Clippy. Existing browser coverage now checks distinct rendered colors, bars matching badges, initially hidden details, keyboard disclosure access, and retained projections and context data. Wide and compact screenshots were visually reviewed. Evidence: `artifacts/usage-cleanup-verify.log`, `pace-browser.png`, and `pace-compact.png`.

The rebuilt Windows package passed the read-only native usage check at 17:54 UTC: live Below pace, On track, and Ahead of pace states had distinct colors, Details began collapsed and opened correctly, Fable stayed model-specific, and the draft estimate remained separate from unknown measured context. Badge text/background contrast ranges from 6.79:1 to 8.19:1. No model prompts were sent, the saved workspace and preferences were unchanged after cleanup, and no page errors occurred. Evidence: `artifacts/usage-final-check.json` and `usage-final-native.png`. The final screenshot waits for provider detection to finish to avoid a transient setup label. The app was reopened normally and was responsive with no QA debugging listener.

## Compact arrows and removed sections

The next-request estimate, its draft warnings, and the Details section were removed at the user's request. Colored arrow icons replace full pace labels in both the strip and cards. Green/down means below pace, blue/right means on track, amber/up means ahead, and red/up means exhausted; unknown readings show a gray dash. Context uses green/right for room and amber/red up arrows for warnings. Hover descriptions and accessible names retain each meaning. The measured context, reset countdowns, budgets, warnings, and saved reply usage remain intact.

All 65 tests passed after the change, together with Svelte/TypeScript, static build, Rust formatting, and Clippy. Existing regressions verify accessible arrow names, different rendered colors, absence of the removed sections, measured-context independence from drafts, and persisted reply counts. Wide and compact screenshots were reviewed. Evidence: `artifacts/usage-arrows-verify.log`, `pace-browser.png`, and `pace-compact.png`. The installed Svelte types reject `aria-description`; button labels and labelled image roles provide the status instead. This compatibility finding and the user's display preference were recorded in local memory notes.

The rebuilt package passed read-only native verification at 18:13 UTC with live Claude quotas. The arrows and removed sections matched the browser result, Fable remained conditional, and typing did not fabricate a context reading. No model prompts were sent; the workspace and preferences were restored exactly and no page errors occurred. Evidence: `artifacts/usage-final-check.json`, `usage-final-native.png`, and the installer hash in `build-manifest.json`. The app was reopened normally with no debugging listener. Native QA now waits for Send to become enabled with an unsent draft before capturing the panel; absence of a setup label alone could pass before initial rendering finished.

Pace icon styling was subsequently simplified at the user's request: no borders or colored backgrounds, 16px icons, and the normal Lucide stroke weight. All 65 tests and required checks passed; browser screenshots were reviewed. The rebuilt native app passed the read-only usage check at 18:18 UTC and its borderless arrows were visually inspected. Workspace and preferences were preserved; no prompts were sent or page errors observed. The app was reopened normally without a QA debugging listener. The borderless-icon preference was recorded in CLAUDE.md and local memory notes. Evidence: `artifacts/usage-icons-verify.log`, `pace-browser.png`, `pace-compact.png`, `usage-final-check.json`, and `usage-final-native.png`.

## Boundaries

- Saved workflow execution, native CLI session resumption, file attachments, and executable HTML artifacts are future work. Claude and Codex chat tools are enabled as described below.
- The web build is an honest UI preview; a future browser version needs an authenticated process backend.
- Windows was tested. macOS and Linux were not; use their terminal sign-in commands.
- The installer is unsigned. Building it does not establish successful installation or uninstallation on another machine.
- A real provider quota/timeout condition was not deliberately induced. Provider error-event fixtures were tested.
- One response runs at a time. Full conversation replay is bounded to 200 messages / 400 KB and may use more tokens than CLI session resumption.

## Claude and Codex chat tools (2026-09-08)

Claude and Codex chat launches enable file access, edits, commands, and the selected CLI profile's configured integrations and project guidance, with full access and no approval prompts. The injected prompt permits tool use, including when replaying older messages that described tools as disabled. Background titles keep the previous restricted launches through an internal, non-deserialized request flag; usage queries and Gemini restrictions remain unchanged. Tool activity appears as short status text without exposing raw arguments or command output.

The isolated native `tools-qa` app completed live Claude Sonnet and Codex GPT-5.6-Sol runs. Each read a random value absent from its prompt, wrote the matching `answer.txt`, and executed `node verify.cjs`, which checked the answer and wrote its own proof file. Native activity recorded Claude Read/Write/Bash and Codex command/file-edit events. Both runs included earlier assistant history claiming tools were disabled. No page errors occurred. Evidence: `artifacts/tools-native-smoke.json`, `artifacts/tools-enabled-native.png`, and the fixture files under `artifacts/tools-smoke/`. Reproduce with `scripts/native-tools.tauri.json` and `scripts/tools-native-smoke.mjs` using an empty isolated QA workspace.

Regression coverage checks enabled chat flags, restricted title flags/prompts, selected working directories, tool activity, and provider-specific captions. Live verification covers existing Windows logins; WSL execution and individual configured MCP integrations were not exercised by this tool-access smoke test.

Final validation passed `npm run verify` (37 frontend unit tests, 35 browser tests, Svelte checks, production frontend build), Rust formatting, Clippy with warnings denied, and 37 Rust tests (three unrelated opt-in integration tests remained ignored). The standalone Windows build is `artifacts/tools-enabled-app/agent-studio.exe`. It opened with the normal `com.vinicius.agentstudio` identity, loaded six saved conversations, and displayed the enabled-tools caption at `http://tauri.localhost`. It was reopened without a debugging listener after verification. The pre-existing Folder QA app was left running. Evidence: `artifacts/tools-verify.log`, `artifacts/tools-native-build.log`, `artifacts/tools-opened-app.json`, and `artifacts/tools-opened-app-check.json`.

## Model context inspector — 2026-09-09

- Added a book-icon Model context inspector with Instructions, Skills, and Memories categories, file paths/scopes/statuses, search, path copying, refresh, inline conversation instructions, and keyboard/focus handling. Inventories follow the selected model/connection/folder and stay out of saved conversations. Display labels distinguish discovered resources, reported sources, disabled skills, overridden instructions, and companion guidance. No instruction/memory bodies, credentials, settings payloads, or CLI account metadata leave the backend.
- Codex uses a read-only skills/list catalog; Claude uses a fresh get_context_usage report with hooks disabled and MCP startup restricted. Claude-reported files describe the fresh query, not the read history of a previous answer. Antigravity has candidate-file discovery only, with actual loading and memory availability explicitly unknown. Discovery limitations and official source links are in docs/CONTEXT.md.
- Final frontend verification passed with zero Svelte diagnostics, 42 unit/HTTP relay tests, a production build, and 38 browser scenarios. Rust formatting, Clippy with warnings denied, and 43 default Rust tests passed; 3 existing opt-in tests were not repeated. New tests cover scoped metadata, fallback/override precedence, profile isolation, bounded traversal, stale query completion, filtering, retry/unknown states, responsive layout, and real HTTP relay routing to the Windows host.
- Native metadata/UI checks passed at 2026-09-09T03:16:40Z in the isolated com.vinicius.agentstudio.context-qa app: Sonnet, Opus, GPT-5.6 Sol, GPT-6 Astra, Gemini file discovery, a separate Windows Codex profile, and rejected mismatched/missing selections. No generated model replies were requested. Visually reviewed artifacts/context-native.png, context-instructions-browser.png, and context-memories-narrow-browser.png. Native result counts are in artifacts/context-native-result.json. The original running desktop process was preserved; the Context QA app has the new native command.
- A real WSL-folder query used Windows context for the existing-login connection. Ubuntu has no discoverable Linux Codex CLI, so separate Linux profile inspection is unverified; it correctly rejects the missing CLI instead of falling back to another account. This boundary is recorded by scripts/context-wsl-native-smoke.mjs.
- Durable learnings recorded in the Codex memory update note 2026-09-09T0015-agent-studio-context-inspection.md: isolate transport reads from Svelte effect tracking using untrack and a stable selection key, and parse TOML configuration documents with toml::from_str rather than Value::from_str. Both fixes are covered by passing checks.

## Context cache while refreshing — 2026-09-09

- Reopening Model context now renders the latest matching cached inventory while a fresh inspection runs. Entries, counts, paths, filtering, and the prior check time remain usable with a small Updating indicator. A failed refresh retains the result with an explicit message. Cold selections retain the initial loading state.
- The cache lives in transport for the app session, shares pending requests across inspector instances, and retains up to 32 recent selections. Keys include provider, model, connection, computer, environment, and folder. Late responses update only their original selection; no inventory is written to conversations or disk.
- npm run verify passed: zero Svelte/TypeScript diagnostics, 46 unit/HTTP relay tests, production build, and 39 browser scenarios. Rust fmt, Clippy with warnings denied, and 43 default Rust tests passed; three existing opt-in integrations were not repeated. New tests hold refreshes across repeated opens, verify a single shared request, check fresh data replacement, retain previous data on failure, keep uncached models separate, and verify bounded cache retention. Visually reviewed artifacts/context-cached-refresh-browser.png. Provider/native execution was unchanged and was not repeated for this frontend cache change.

## Skills, web search, and sub-agent activity — 2026-09-09

- Claude and Codex replies now show structured skill, search, and sub-agent cards, including source links, individual child states/results, and attributed child tools. Model context offers a Use in next message action for available skills. Activity survives saved history, exports, relay delivery, and same-run checkpoints. Gemini remains conversation-only. See [CAPABILITIES.md](CAPABILITIES.md) for behavior and limits.
- Codex chat now uses ephemeral app-server threads, with live search and multi-agent tools enabled. The installed CLI's exec stream omitted modern child lifecycle events; the app-server stream supplied them. Restricted background titles retain exec. Selected profiles, validated working directories, stdin prompts, and owned process cancellation are preserved.
- Final `npm run verify` passed: zero Svelte/TypeScript diagnostics, 52 frontend/HTTP relay tests, a production build, and 44 browser scenarios. Rust formatting, Clippy with warnings denied, and 54 default Rust tests passed; three existing opt-in integration tests remain ignored. Coverage includes partial inputs, repeated operations, failed/cancelled/unconfirmed states, safe links/results, parent/child attribution, measured parent usage, bounded retention, stale relay events, narrow layouts, keyboard interaction, and reload.
- The isolated `com.vinicius.agentstudio.capabilities-qa` native app completed real Windows Claude Sonnet and Codex GPT-5.6-Sol runs through the composer, IPC, installed CLI, event decoder, and rendered/saved history. Both read a unique fixture skill marker, performed web search with source links, and returned a second marker read by a sub-agent. Claude's final child-result cleanup was rebuilt and rechecked with a real reply. Results contain the child answer without harness usage or continuation instructions. The final Claude probe recovered from choosing the skill directory for its first child read; the failed operation and subsequent successful result were represented separately.
- Visually reviewed `artifacts/capabilities-claude-native.png`, `capabilities-codex-native.png`, `capabilities-complete-browser.png`, and `capabilities-narrow-browser.png`. Native assertions are in `artifacts/capabilities-native-result.json`; reproduce with `scripts/capabilities-native-smoke.mjs` and `scripts/native-capabilities.tauri.json`. The regular app/profile was preserved; the isolated QA app and Vite service remain running.
- The real HTTP two-host relay test uses synthetic provider IPC. Separate Linux CLI profiles, physical remote execution, and individual configured MCP integrations were not exercised for this change. No installer was rebuilt. Protocol and QA learnings are recorded in CAPABILITIES.md: use live notifications for ephemeral child results, recognize direct skill reads after configured command hooks, and wait for a new rendered document after native reload.

## Inline activity and completed summaries — 2026-09-09

- Read, Glob, Grep, and tool-search cards identify their file, pattern, folder, or query. Expanded cards retain reported line ranges/counts, matching file names, returned tool names, command descriptions, and exit metadata. Empty disclosures now explain that no details were recorded. Saved calls from older versions cannot recover metadata that was discarded.
- Replies show progress messages and tools inline while running. Completion moves intermediate activity into a collapsed summary below the final answer, with counts and filters for tool calls, web searches, sub-agents, and command runs. Searches and runs are subsets of recorded tool calls; child-agent lifecycle updates and resumed Claude sessions do not inflate the child count. Legacy status-only messages retain an Activity disclosure without inventing tool counts.
- Final validation passed `npm run verify`: zero Svelte/TypeScript diagnostics, 54 frontend/HTTP relay tests, production build, and 45 browser scenarios. Rust formatting, Clippy with warnings denied, and 57 default Rust tests passed; three existing opt-in integrations remain ignored. Regression coverage includes ordinary and combined skill/file reads, allowlisted metadata without file bodies, progress/final separation, stale progress revisions, restore and prompt exclusion, counts, resumed child identities, filters, keyboard expansion, cancellation, narrow layout, and a real HTTP relay with synthetic provider IPC.
- The final isolated native build passed fresh Windows Claude Sonnet and Codex GPT-5.6-Sol runs. Both verified ordinary file targets, fixture skills, web search, a child-read marker, progress history, initially collapsed summaries, and persisted activity after reload. Claude supplied real Read line counts, Glob matches, and ToolSearch names. Codex's verified summary showed four recorded tool calls, one web search, one sub-agent, and three command runs. The earlier Codex fixture reported a rejected child launch and failed its success assertion; a subsequent fixture using the valid underscore task name completed. No failed attempt was reported as a successful verification.
- Visually reviewed `artifacts/activity-inline-browser.png`, `activity-summary-browser.png`, `activity-details-narrow-browser.png`, `activity-claude-summary-native.png`, and `activity-codex-summary-native.png`. Detailed native cards and assertions remain in `capabilities-{claude,codex}-native.png` and `capabilities-native-result.json`. The native smoke now waits for the New conversation control to become enabled after reload, so workspace initialization cannot reset its chosen chat before sending. Combined-read and startup findings are documented in CAPABILITIES.md.
- The regular app/profile was preserved; the isolated QA app and Vite service remain running. Separate Linux CLI execution, physical remote provider execution, and individual configured MCP integrations were not retested. No installer or package was rebuilt.

## Reply footer readability — 2026-09-09

The token counts and elapsed time beneath the activity summary increased from 8px to 11px and wrap on narrow windows. The Copy response button and its clipboard handler were removed; link-error reporting remains available.

All required checks passed: Svelte/TypeScript, production build, 54 frontend/relay tests, 45 browser scenarios, Rust formatting, Clippy, and 57 Rust tests (three existing opt-in integrations ignored). Read-only native checks confirmed 11px text, no copy-response button, and no footer overflow at 1380px and 880px viewport widths. Both screenshots were visually reviewed: `artifacts/reply-footer-native.png` and `reply-footer-narrow-native.png`; measurements are in `reply-footer-check.json`. No new provider replies were requested for this presentation change.

## Expandable reply usage and cost — 2026-09-09

The token/time row now sits above the completed activity summary and expands into token, cache, reasoning, elapsed-time, and USD cost details. It retains the 11px summary and has no copy-response action. Claude's final `total_cost_usd` becomes an optional `usage.costUsd` field in typed events and saved messages. Missing, invalid, older, and unsupported provider costs remain unknown. Zero and tiny positive values remain distinct. Child/model totals are not added to the reported total.

All required checks passed: `npm run verify` (zero Svelte/TypeScript diagnostics, 54 frontend/HTTP unit tests, production build, 46 browser tests), Rust formatting, Clippy with warnings denied, and 58 Rust tests; three existing opt-in integrations remain ignored. Coverage includes keyboard expansion, row ordering, narrow layouts, reload persistence, real HTTP relay delivery with synthetic provider IPC, zero/tiny/missing costs, error-result costs, and child cost exclusion.

A fresh native build launched with `npm run tauri dev -- --config scripts/native-cost.tauri.json --no-watch` using the dedicated `com.vinicius.agentstudio.cost-qa` identity, WebView profile `artifacts/cost-webview`, and CDP port 9458. The normal Cargo target was unused by running apps; the existing app in `artifacts/native-federation-target` was preserved. `scripts/reply-cost-native-smoke.mjs` completed one real Claude Sonnet file-read reply through the composer and persisted a reported cost of $0.059443. It verified expansion, 11px text, placement above activity, absent Copy response, no overflow at 1380px and 880px, and the saved cost after reload. Screenshots `artifacts/reply-cost-wide-native.png` and `reply-cost-narrow-native.png` were visually reviewed; measurements are in `reply-cost-native-result.json`. Rerunning the smoke reuses its saved reply instead of generating another one.

The existing app and its chats were left running; its older native process requires a rebuilt/restarted app to capture costs on future replies. The separate cost QA app contains the updated native adapter. No installer was rebuilt. Codex's generated `ThreadTokenUsage`/`TokenUsageBreakdown` contract was inspected and has no monetary field; no new Codex or Gemini provider reply was needed for this cost-only adapter addition.

## Cumulative AI reply time — 2026-09-09

Each reply's usage row now includes total AI time through that reply, with the same value in the expanded breakdown. A single pass over the selected conversation sums recorded completed, failed, and stopped assistant reply durations. User messages, running replies, and other conversations are excluded. Missing historical timing produces a lower-bound total with an explanation; all-missing timing remains unknown. Totals use unrounded milliseconds before formatting with minutes/hours and are derived from saved messages rather than persisting another counter. Retried messages that the existing retry action removes are no longer part of saved history.

All required checks passed: `npm run verify` with zero diagnostics, 57 frontend/HTTP unit tests, production build, and 47 browser tests; Rust formatting, Clippy with warnings denied, and 58 Rust tests passed (three existing opt-in tests ignored). New coverage checks cumulative values across model/status changes, missing/zero timing, formatting boundaries, conversation isolation, live completion updates, unchanged earlier totals, and reopening saved history.

Native verification reused the existing saved Claude reply in the isolated cost QA app on port 9458, generated no new provider replies, and confirmed its 4583.3ms stored duration displays as both 4.6s elapsed and 4.6s total. Summary/expanded values and no overflow at 1380px and 880px were checked. Evidence: `artifacts/reply-time-native-result.json`, `reply-time-wide-native.png`, `reply-time-narrow-native.png`, and browser fixture `reply-time-total-narrow-browser.png`; the narrow native layout and multi-reply browser state were visually reviewed. No native adapter or storage schema changes were needed; both existing app processes were left running.

## Usage bars below the composer — 2026-09-09

Moved Usage and context from above the conversation to below the message input, replacing the Tools enabled caption with three aligned progress bars: Context, 5-hour, and Weekly. The compact row preserves measured values, quota colors and bare pace arrows, stale labels, unknown readings, and refresh. Unknown progress uses a striped track without a numeric value. Each bar opens the existing reset/budget/guidance panel above the input; the input and Send button remain available. Gauge, Close, Enter, and Escape controls work. Fable's additional limit remains available in the expansion. Tool-access policy moved to the account-line hover, and keyboard shortcuts to the message-field hover.

All required checks passed: `npm run verify` (zero Svelte/TypeScript diagnostics, 57 frontend/HTTP unit tests, production build, 47 browser tests), Rust formatting, Clippy with warnings denied, and 58 Rust tests (three opt-in tests ignored). Updated browser coverage verifies placement, exactly three progress bars across providers and Fable, unknown/stale progress attributes, keyboard opening/closing, preserved tool-access descriptions, and narrow layouts. Existing context and quota behavior checks pass unchanged.

Native checks reused the isolated cost QA app and its saved Claude reply without generating a response. Live read-only subscription values and saved context rendered as Context 3.5%, 5-hour 1%, and Weekly 47%. At 1380×900 and 880×720, all three bars were below the input with aligned tracks and no overflow; the expanded panel stayed on screen above the input and closed with Escape. Visually reviewed `artifacts/usage-bars-wide-native.png`, `usage-bars-expanded-narrow-native.png`, and `composer-usage-bars-narrow-browser.png`. Measurements are in `artifacts/usage-bars-native-result.json`. Existing app processes and conversations were preserved; no native adapter or installer change was required.

Documentation-edit note: PowerShell treats typographic apostrophes as quote delimiters inside single-quoted strings. An inline replacement containing one failed before writing with `Missing ')' in method call.` Rewording the replacement without the embedded apostrophe succeeded and the resulting README sentence was verified with `rg`. Use `apply_patch` or a literal here-string for prose containing apostrophes.

## Current and recommended usage comparison — 2026-09-09

Removed the composer refresh and gauge buttons. The existing automatic usage polling, focus refresh, and completion refresh remain active; clicking any of the three usage groups opens the panel. The panel title and Checked text are vertically centered with Close. Quota bars in the composer and expanded cards now show a colored current fill and a neutral secondary recommendation on the same percentage scale and left edge. Separate thin fills keep both values visible when current usage is above, below, or equal to the guide, with an explicit Recommended percentage and accessible comparison text. The guide uses the existing reading-time calculation and disappears for stale, failed, expired, or unknown readings. Context retains its measured bar without a fabricated time recommendation.

All required checks passed: `npm run verify` (zero diagnostics, 57 frontend/HTTP unit tests, production build, 47 browser tests), Rust formatting, Clippy with warnings denied, and 58 Rust tests (three existing opt-in tests ignored). Updated tests open the panel through Context, verify both removed controls, header alignment, 70%/50%, 25%/50%, and 50%/50% comparisons, matched bar scales, and guide removal after an automatic focus refresh fails. Existing native smoke selectors were updated; legacy provider-generating smoke scripts were not rerun.

Read-only native verification reused the isolated cost QA app and its saved reply. It confirmed no removed buttons, three aligned primary groups, independently visible recommendation fills, centered header content, and an on-screen panel above the input at 1380×900 and 880×720. Native comparisons were 1% current versus 44.1% recommended for 5-hour, and 47% versus 49.9% for Weekly. Visually reviewed `artifacts/usage-recommendation-wide-native.png`, `usage-recommendation-expanded-narrow-native.png`, and the browser above/below-guide fixture `pace-compact.png`. Measurements are in `artifacts/usage-recommendation-native-result.json`. No provider reply, app restart, or installer rebuild was needed.

## Shared usage track — 2026-09-09

Current quota usage and the neutral recommendation now overlap on one 6px track in both the composer and expanded cards. The recommendation endpoint stays visible above the current fill. Removed the separate Recommended/Guide unavailable row and its spacing; comparison values remain available through hover and accessible text. All three composer tracks align without a caption spacer.

All required checks passed: `npm run verify` (zero diagnostics, 57 frontend/HTTP unit tests, production build, 47 browser tests), Rust formatting, Clippy with warnings denied, and 58 Rust tests (three existing opt-in integrations ignored). Updated browser assertions verify matching top/bottom coordinates and scales for above, below, and equal usage, absent recommendation labels, and recommendation removal on failed refresh.

Native verification reused the saved reply in the running isolated cost QA app without generating a provider response. At 1380×900 and 880×720, it confirmed shared tracks, no recommendation text, three aligned bars below the input, no overflow, and centered panel headings. Visually reviewed `artifacts/usage-overlay-wide-native.png`, `usage-overlay-expanded-narrow-native.png`, and browser fixture `pace-compact.png`. Measurements are in `artifacts/usage-overlay-native-result.json`. Existing apps remained running; no native adapter or installer rebuild was needed.

## Simplified new conversation and composer — 2026-09-09

Removed the three prompt suggestion cards and the CLI login/computer/execution subtitle inside the composer. Send and Stop remain right-aligned. The Agent picker hover now carries the provider-specific tool-access description alongside the existing conversation-lock explanation. Removed the unused suggestion and subtitle styles and aligned the project guidance.

All required checks passed: `npm run verify` (zero Svelte/TypeScript diagnostics, 60 frontend/HTTP tests, production build, and 54 browser scenarios), Rust formatting, Clippy with warnings denied, and 60 Rust tests; four existing opt-in integrations remain ignored. The existing browser scenario verifies the relocated hover for Claude, Codex, and Gemini. Visually reviewed the updated empty conversation and composer at 1380×900 and 880×720 in `artifacts/composer-usage-bars-browser.png` and `artifacts/composer-usage-bars-narrow-browser.png`. Both removed elements are absent, with the welcome text, right-aligned Send button, and usage bars intact. Native/provider execution was not rerun for this presentation-only change; the running app was preserved.

## Vertically centered new-conversation welcome — 2026-09-09

The empty conversation column now fills the available chat area and centers the provider icon and welcome text between the toolbar and composer. Symmetric 16px vertical padding keeps the content centered at the minimum window size, including the browser preview and folder-selection notices. The former fixed top padding and narrow-window override were removed. Existing conversations retain their normal message layout.

All required checks passed on the final code: `npm run verify` (zero Svelte/TypeScript diagnostics, 60 frontend/HTTP tests, production build, 54 browser scenarios), Rust formatting, Clippy with warnings denied, and 60 Rust tests with four existing opt-in integrations ignored. Browser geometry checks measured zero vertical center offset at 1380×900, 880×720, and 840×640, with the welcome and composer visible, no horizontal overflow, and no page errors. The initial 32px padding produced a 13.375px offset at 840×640 because it made the welcome taller than the available area; reducing it to 16px resolved that check. Measurements are in `artifacts/welcome-centered-layout.json`. Visually reviewed `artifacts/composer-usage-bars-browser.png` and `artifacts/welcome-centered-840.png`. Native/provider execution was not rerun for this layout-only change.
